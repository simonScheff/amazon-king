import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Connecting Amazon Ads","description":"How to get Amazon Ads API access, create a Login with Amazon app, and connect it to your self-hosted amazon-king instance.","frontmatter":{"title":"Connecting Amazon Ads","description":"How to get Amazon Ads API access, create a Login with Amazon app, and connect it to your self-hosted amazon-king instance."},"headers":[],"relativePath":"guide/connecting-amazon.md","filePath":"guide/connecting-amazon.md","lastUpdated":1787252819000}');
const _sfc_main = { name: "guide/connecting-amazon.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="connecting-amazon-ads" tabindex="-1">Connecting Amazon Ads <a class="header-anchor" href="#connecting-amazon-ads" aria-label="Permalink to &quot;Connecting Amazon Ads&quot;">​</a></h1><p>amazon-king never ships with Amazon credentials. Every self-hoster connects their own Amazon Ads account through their own Login with Amazon (LWA) application. This page walks through the whole path: API access, the LWA app, the connect flow, and what happens to your tokens afterwards.</p><h2 id="getting-amazon-ads-api-access" tabindex="-1">Getting Amazon Ads API access <a class="header-anchor" href="#getting-amazon-ads-api-access" aria-label="Permalink to &quot;Getting Amazon Ads API access&quot;">​</a></h2><p>You must apply for Amazon Ads API access for your own advertiser account; approval is per developer and cannot be shared through this repository or between unrelated self-hosters.</p><p>Amazon distinguishes two kinds of API consumers:</p><ul><li><strong>Direct advertisers</strong> use the API for their own advertising account. This is the category a self-hosted single-owner amazon-king deployment falls into.</li><li><strong>Partners</strong> operate software on behalf of other advertisers. Running a centrally hosted service for other users requires the appropriate Amazon Partner registration and is outside what amazon-king provides.</li></ul><p>Apply at <a href="https://advertising.amazon.com/about-api" target="_blank" rel="noreferrer">Amazon Ads API access</a> and review the <a href="https://advertising.amazon.com/resources/ad-policy/partner-network-policies" target="_blank" rel="noreferrer">Amazon Ads Partner Network policies</a> if you are unsure which category you are in.</p><h2 id="creating-the-login-with-amazon-app" tabindex="-1">Creating the Login with Amazon app <a class="header-anchor" href="#creating-the-login-with-amazon-app" aria-label="Permalink to &quot;Creating the Login with Amazon app&quot;">​</a></h2><p>Once your API access is approved, create a Login with Amazon application in the Amazon developer console and note its client ID and client secret. Three things must line up exactly:</p><ul><li><strong>Authorization endpoint.</strong> amazon-king sends the browser to <code>https://www.amazon.com/ap/oa</code> to start consent. You do not configure this; it is built into the OAuth client.</li><li><strong>Scope.</strong> The consent request asks for <code>advertising::campaign_management</code> — the scope that covers Sponsored Products structure, reporting, and campaign management.</li><li><strong>Redirect URI.</strong> Register exactly the value of your <code>AMAZON_REDIRECT_URI</code> environment variable in the LWA app&#39;s allowed return URLs. It must end in <code>/api/integrations/amazon/callback</code>, for example <code>https://ads.example.com/api/integrations/amazon/callback</code> in production or <code>http://localhost:3000/api/integrations/amazon/callback</code> in development. A mismatch — even a trailing slash or a different host — makes Amazon reject the callback.</li></ul><p>Put the client ID and secret in <code>LWA_CLIENT_ID</code> and <code>LWA_CLIENT_SECRET</code> (see <a href="/amazon-king/guide/configuration">Configuration</a> and the <a href="/amazon-king/reference/environment-variables">environment variable reference</a>).</p><h2 id="the-connect-flow" tabindex="-1">The connect flow <a class="header-anchor" href="#the-connect-flow" aria-label="Permalink to &quot;The connect flow&quot;">​</a></h2><p>With credentials configured, connecting is a UI flow:</p><ol><li>Sign in to the dashboard and open <code>/connect</code>.</li><li>Click <strong>Connect Amazon Ads</strong>. The API creates a one-time OAuth state and returns the Amazon consent URL; the browser navigates to Amazon.</li><li>Approve the consent screen on Amazon.</li><li>Amazon redirects back to <code>AMAZON_REDIRECT_URI</code> with a code. The API validates the state, exchanges the code server-side, encrypts and stores the refresh token, and runs an initial profile discovery.</li><li>The browser lands on <code>/connect?connected=1</code> on success, or <code>/connect?error=&lt;code&gt;</code> on failure.</li></ol><p>The browser only ever sees the consent URL and these redirects. The authorization code, access token, refresh token, and LWA client secret stay server-side; the app session never contains an Amazon token.</p><h3 id="callback-error-codes" tabindex="-1">Callback error codes <a class="header-anchor" href="#callback-error-codes" aria-label="Permalink to &quot;Callback error codes&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>invalid_callback</code></td><td>The callback arrived without a <code>state</code> or <code>code</code> parameter.</td></tr><tr><td><code>invalid_state</code></td><td>The state is unknown, expired (10-minute TTL), or already used. Retry the connect flow.</td></tr><tr><td><code>session_required</code></td><td>The state was valid but you were not signed in. Sign in and start again.</td></tr><tr><td><code>foreign_state</code></td><td>The state was issued to a different signed-in user. Refused.</td></tr><tr><td><code>exchange_failed</code></td><td>Amazon rejected the code exchange (bad client credentials, redirect mismatch, expired code). Check <code>LWA_CLIENT_ID</code>, <code>LWA_CLIENT_SECRET</code>, and <code>AMAZON_REDIRECT_URI</code>.</td></tr><tr><td><code>profile_discovery_failed</code></td><td>The token was stored but listing profiles failed. Check the API logs, then disconnect and reconnect.</td></tr></tbody></table><h2 id="profiles-and-discovery" tabindex="-1">Profiles and discovery <a class="header-anchor" href="#profiles-and-discovery" aria-label="Permalink to &quot;Profiles and discovery&quot;">​</a></h2><p>One Amazon account can contain many <strong>profiles</strong> (roughly one per marketplace or account type). Profiles are mirrored into the database twice:</p><ul><li>once inline during the OAuth callback, so <code>/connect</code> shows them immediately, and</li><li>daily by the worker&#39;s <code>profile_discovery</code> job, which picks up profiles added to your Amazon account later.</li></ul><p><strong>New profiles start disabled.</strong> Nothing syncs until you opt in per profile.</p><h2 id="enabling-a-profile-sync-vs-write-access" tabindex="-1">Enabling a profile: sync vs. write access <a class="header-anchor" href="#enabling-a-profile-sync-vs-write-access" aria-label="Permalink to &quot;Enabling a profile: sync vs. write access&quot;">​</a></h2><p>Open <strong>Settings → Profiles &amp; sync</strong>. Each discovered profile has two independent toggles:</p><ul><li><strong>Read (sync)</strong> — the <code>enabled</code> flag. When on, the worker syncs structure and metrics for this profile on schedule (and on demand via <strong>Sync now</strong>).</li><li><strong>Writes</strong> — the <code>writeEnabled</code> flag. When on, approved change sets for this profile may be applied to Amazon. Profiles are read-only by default, and writes additionally require the global <code>KILL_SWITCH=false</code>; see <a href="/amazon-king/guide/applying-changes">Applying &amp; rolling back changes</a>.</li></ul><p>Keep profiles read-only until you have completed the <a href="/amazon-king/guide/self-hosting#validate-read-only-operation">read-only validation checklist</a>.</p><h2 id="disconnecting" tabindex="-1">Disconnecting <a class="header-anchor" href="#disconnecting" aria-label="Permalink to &quot;Disconnecting&quot;">​</a></h2><p><strong>Disconnect</strong> on <code>/connect</code> (behind a confirmation dialog):</p><ul><li>destroys the stored refresh-token ciphertext, so the app can no longer call Amazon for this account,</li><li>invalidates any cached access token, and</li><li>dead-letters pending queue jobs for the connection&#39;s profiles.</li></ul><p>Disconnecting is idempotent and you can reconnect at any time — reconnecting runs the same OAuth flow and stores a fresh grant. Imported metrics and audit records are kept; see <a href="/amazon-king/guide/operations#data-export-and-deletion">Operations</a> for what that means for data deletion. To also revoke the grant on Amazon&#39;s side, remove the application in your Amazon account&#39;s app permissions.</p><h2 id="the-reconnect-required-state" tabindex="-1">The <code>reconnect_required</code> state <a class="header-anchor" href="#the-reconnect-required-state" aria-label="Permalink to &quot;The \`reconnect_required\` state&quot;">​</a></h2><p>Token refresh is serialized per connection through a circuit breaker. If Amazon answers a refresh with <code>invalid_grant</code> — the grant was revoked, expired, or superseded — the token manager stops retrying, marks the connection <code>reconnect_required</code>, and dead-letters its pending sync jobs. <code>/connect</code> shows the status and the last error code.</p><p>Recovery is a normal reconnect: click <strong>Connect Amazon Ads</strong> again and approve the consent screen. The new grant replaces the dead one and scheduled syncs resume on the next tick.</p><h2 id="security-properties" tabindex="-1">Security properties <a class="header-anchor" href="#security-properties" aria-label="Permalink to &quot;Security properties&quot;">​</a></h2><ul><li><strong>OAuth state</strong> is a 256-bit random token. Only its SHA-256 hash is stored, tied to the signed-in user, with a 10-minute TTL. The state is marked used <em>before</em> the code exchange, so a replayed callback can never exchange twice.</li><li><strong>Refresh tokens</strong> are stored only envelope-encrypted (AES-256-GCM) in the <code>amazon_connections</code> table, with the key version embedded in the ciphertext so keys can be rotated. They are decrypted in the backend/worker only, immediately before a refresh.</li><li>Codes, tokens, and the client secret are never logged; Amazon auth errors are sanitized before they reach the log.</li></ul><p>For the full model see <a href="/amazon-king/architecture/security">Security</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/connecting-amazon.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const connectingAmazon = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  connectingAmazon as default
};
