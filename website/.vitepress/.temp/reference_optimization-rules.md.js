import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Optimization Rules","description":"The nine deterministic optimizer rules of amazon-king — exact trigger thresholds, proposal math, suppression conditions, guardrails, and ranking.","frontmatter":{"title":"Optimization Rules","description":"The nine deterministic optimizer rules of amazon-king — exact trigger thresholds, proposal math, suppression conditions, guardrails, and ranking."},"headers":[],"relativePath":"reference/optimization-rules.md","filePath":"reference/optimization-rules.md","lastUpdated":1787252819000}');
const _sfc_main = { name: "reference/optimization-rules.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="optimization-rules" tabindex="-1">Optimization Rules <a class="header-anchor" href="#optimization-rules" aria-label="Permalink to &quot;Optimization Rules&quot;">​</a></h1><p>The optimizer (<code>packages/optimizer</code>) is pure and deterministic: no I/O, no wall clock (time is injected), money in integer micro-units internally with string decimals at the boundaries. Every rule is versioned (<code>&lt;name&gt;@&lt;n&gt;</code>), stores its exact inputs as evidence, and expires when data goes stale. Rules run over 7/14/30/60-day windows after each complete metrics sync.</p><p>All thresholds below are the defaults from <code>DEFAULT_OPTIMIZER_CONFIG</code> (<code>packages/optimizer/src/config.ts</code>); a deployment or test can override them via <code>resolveOptimizerConfig</code>. Profit-based rules require user-entered <a href="/amazon-king/guide/book-economics">KDP book economics</a> and are <strong>disabled — never guessed — when economics are missing</strong>.</p><h2 id="the-nine-rules" tabindex="-1">The nine rules <a class="header-anchor" href="#the-nine-rules" aria-label="Permalink to &quot;The nine rules&quot;">​</a></h2><h3 id="_1-expensive-target-—-bid-down-a-losing-target" tabindex="-1">1. <code>expensive_target</code> — bid down a losing target <a class="header-anchor" href="#_1-expensive-target-—-bid-down-a-losing-target" aria-label="Permalink to &quot;1. \`expensive_target\` — bid down a losing target&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>A target has orders but loses money or runs materially above target ACoS → lower the bid.</td></tr><tr><td>Trigger</td><td><code>clicks ≥ 10</code> <strong>and</strong> <code>orders ≥ 1</code> <strong>and</strong> (<code>observed ACoS ≥ 1.2 × target ACoS</code> <strong>or</strong> <code>estimated ad profit &lt; 0</code>). When there are no attributed sales, the observed ACoS is treated as <code>1.2 × target</code> for the proposal.</td></tr><tr><td>Proposal</td><td><code>proposedBid</code> (see <a href="#shared-math">Shared math</a>), floored at <code>current bid × 0.85</code> so the cut never exceeds the −15% clamp band.</td></tr><tr><td>Suppressed</td><td>Launch goal mode; missing KDP economics; protected campaign; bid cooldown active on the target. Also rejected when the computed bid is not below the current bid.</td></tr><tr><td>Impact</td><td>\`max(</td></tr><tr><td>Human review</td><td>No. Writable: <code>update_bid</code>.</td></tr><tr><td>Version</td><td><code>expensive_target@2</code> (v2 values royalty per copy sold instead of per order)</td></tr></tbody></table><h3 id="_2-profitable-target-—-bid-up-a-proven-winner" tabindex="-1">2. <code>profitable_target</code> — bid up a proven winner <a class="header-anchor" href="#_2-profitable-target-—-bid-up-a-proven-winner" aria-label="Permalink to &quot;2. \`profitable_target\` — bid up a proven winner&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>A target converts profitably with ACoS safely below target → raise the bid cautiously.</td></tr><tr><td>Trigger</td><td><code>clicks ≥ 10</code> <strong>and</strong> <code>orders ≥ 2</code> <strong>and</strong> <code>observed ACoS ≤ 0.8 × target ACoS</code> <strong>and</strong> <code>estimated ad profit &gt; 0</code>.</td></tr><tr><td>Proposal</td><td><code>proposedBid</code>, clamped to at most +15% and capped by the profit-ceiling CPC and the book&#39;s max bid. Rejected when the result is not above the current bid.</td></tr><tr><td>Suppressed</td><td>Missing KDP economics; protected campaign; bid cooldown active on the target.</td></tr><tr><td>Impact</td><td>The estimated ad profit over the window.</td></tr><tr><td>Human review</td><td>No. Writable: <code>update_bid</code>.</td></tr><tr><td>Version</td><td><code>profitable_target@2</code> (v2 values royalty per copy sold instead of per order)</td></tr></tbody></table><h3 id="_3-wasteful-search-term-—-negative-a-zero-order-term" tabindex="-1">3. <code>wasteful_search_term</code> — negative a zero-order term <a class="header-anchor" href="#_3-wasteful-search-term-—-negative-a-zero-order-term" aria-label="Permalink to &quot;3. \`wasteful_search_term\` — negative a zero-order term&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>A shopper term accumulates meaningful clicks with zero orders → add it as a negative exact keyword.</td></tr><tr><td>Trigger</td><td><code>clicks ≥ 20</code> <strong>and</strong> <code>orders = 0</code>.</td></tr><tr><td>Proposal</td><td>None (no scalar value); the action is <code>add_negative_exact</code> for the term.</td></tr><tr><td>Suppressed</td><td>Protected search term; launch goal mode (exploration spend is expected); a negative for this term was already added within the cooldown window; a synced negative keyword or negative ASIN target already blocks this campaign for the term. Does not require economics.</td></tr><tr><td>Impact</td><td>The term&#39;s spend over the window (the stopped waste).</td></tr><tr><td>Human review</td><td>No. Writable: <code>add_negative_exact</code>.</td></tr><tr><td>Version</td><td><code>wasteful_search_term@2</code> (v2 treats a synced negative as already resolved, so historical clicks do not keep proposing the same exclusion)</td></tr></tbody></table><h3 id="_4-search-term-harvest-—-promote-a-converting-term" tabindex="-1">4. <code>search_term_harvest</code> — promote a converting term <a class="header-anchor" href="#_4-search-term-harvest-—-promote-a-converting-term" aria-label="Permalink to &quot;4. \`search_term_harvest\` — promote a converting term&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>A term converts repeatedly inside auto/broad targeting → add it as an exact keyword in a controlled manual campaign for direct bid control.</td></tr><tr><td>Trigger</td><td><code>orders ≥ 2</code> from an <code>auto</code> or <code>broad</code> source, and the term is <strong>not</strong> already exact-targeted in a manual campaign.</td></tr><tr><td>Proposal</td><td>The estimated break-even CPC (smoothed CVR × royalty) when economics exist; otherwise null and the human sets the starting bid.</td></tr><tr><td>Suppressed</td><td>Source targeting is phrase/exact; term already targeted exactly; a synced negative already blocks this campaign for the term. Does not require economics to fire.</td></tr><tr><td>Impact</td><td>The term&#39;s attributed sales over the window.</td></tr><tr><td>Human review</td><td><strong>Yes.</strong> Advisory-only — cannot enter a change set automatically.</td></tr><tr><td>Version</td><td><code>search_term_harvest@1</code></td></tr></tbody></table><h3 id="_5-budget-constrained-winner-—-raise-a-capped-budget" tabindex="-1">5. <code>budget_constrained_winner</code> — raise a capped budget <a class="header-anchor" href="#_5-budget-constrained-winner-—-raise-a-capped-budget" aria-label="Permalink to &quot;5. \`budget_constrained_winner\` — raise a capped budget&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>A profitable campaign keeps hitting its daily budget → stop capping the winner.</td></tr><tr><td>Trigger</td><td>Spend <code>≥ 90%</code> of daily budget on <code>≥ 3</code> days of the window <strong>and</strong> <code>estimated ad profit &gt; 0</code>.</td></tr><tr><td>Proposal</td><td><code>daily budget × 1.2</code>, capped by the profile&#39;s <code>maxDailyBudget</code> when configured. Rejected when the capped proposal is not above the current budget.</td></tr><tr><td>Suppressed</td><td>Missing KDP economics (royalty null); protected campaign; budget ≤ 0.</td></tr><tr><td>Impact</td><td>The estimated ad profit over the window.</td></tr><tr><td>Human review</td><td>No. Advisory-only in the MVP (no automatic budget write).</td></tr><tr><td>Version</td><td><code>budget_constrained_winner@2</code> (v2 values royalty per copy sold instead of per order)</td></tr></tbody></table><h3 id="_6-high-ctr-poor-conversion-—-listing-problem-diagnostic" tabindex="-1">6. <code>high_ctr_poor_conversion</code> — listing problem diagnostic <a class="header-anchor" href="#_6-high-ctr-poor-conversion-—-listing-problem-diagnostic" aria-label="Permalink to &quot;6. \`high_ctr_poor_conversion\` — listing problem diagnostic&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>The ad attracts clicks but the book does not sell — flag cover, price, subtitle, listing copy, or audience mismatch. The Ads API cannot fix the KDP listing.</td></tr><tr><td>Trigger</td><td><code>impressions ≥ 1000</code> <strong>and</strong> <code>CTR ≥ 0.3%</code> <strong>and</strong> <code>CVR &lt; 0.5%</code>.</td></tr><tr><td>Proposal</td><td>None — diagnostic only.</td></tr><tr><td>Suppressed</td><td>Remaining unblocked search-term traffic no longer meets the trigger (the wasted clicks are already negatives). No economics needed.</td></tr><tr><td>Impact</td><td>The spend over the window.</td></tr><tr><td>Human review</td><td><strong>Yes.</strong> No single Amazon write, but the finding opens a <a href="/amazon-king/guide/recommendations#clicked-but-not-bought">resolution screen</a> offering a listing checklist with a 30-day snooze, negatives for zero-order terms, a Max CPC ceiling, or pausing the campaign.</td></tr><tr><td>Version</td><td><code>high_ctr_poor_conversion@1</code></td></tr></tbody></table><h3 id="_7-low-impressions-—-no-traffic-diagnostic" tabindex="-1">7. <code>low_impressions</code> — no-traffic diagnostic <a class="header-anchor" href="#_7-low-impressions-—-no-traffic-diagnostic" aria-label="Permalink to &quot;7. \`low_impressions\` — no-traffic diagnostic&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>An active target receives almost no traffic → review bid, match type, indexing, and targeting relevance.</td></tr><tr><td>Trigger</td><td>Target state <code>enabled</code>/<code>active</code> <strong>and</strong> <code>impressions &lt; 100</code> over the window.</td></tr><tr><td>Proposal</td><td>None — the bid is never raised automatically without relevance evidence. Confidence grows as impressions approach zero (<code>min(1, 1 − impressions/100 + 0.5)</code>).</td></tr><tr><td>Suppressed</td><td>Target not enabled/active.</td></tr><tr><td>Impact</td><td>0 (diagnostic).</td></tr><tr><td>Human review</td><td><strong>Yes.</strong> Advisory-only.</td></tr><tr><td>Version</td><td><code>low_impressions@1</code></td></tr></tbody></table><h3 id="_8-placement-opportunity-—-raise-a-winning-placement" tabindex="-1">8. <code>placement_opportunity</code> — raise a winning placement <a class="header-anchor" href="#_8-placement-opportunity-—-raise-a-winning-placement" aria-label="Permalink to &quot;8. \`placement_opportunity\` — raise a winning placement&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>A placement is consistently profitable with enough volume → suggest a placement bid-modifier increase.</td></tr><tr><td>Trigger</td><td><code>clicks ≥ 20</code> <strong>and</strong> <code>orders ≥ 2</code> <strong>and</strong> <code>observed ACoS ≤ 0.8 × target ACoS</code> <strong>and</strong> <code>estimated ad profit &gt; 0</code>.</td></tr><tr><td>Proposal</td><td><code>current modifier + 0.10</code> — a single fixed step, so repeated approvals cannot compound beyond the step in one recommendation. Values are expressed as fractions (e.g. <code>0.1000</code>).</td></tr><tr><td>Suppressed</td><td>Missing KDP economics; protected campaign.</td></tr><tr><td>Impact</td><td>The estimated ad profit over the window.</td></tr><tr><td>Human review</td><td>No. Advisory-only in the MVP.</td></tr><tr><td>Version</td><td><code>placement_opportunity@2</code> (v2 values royalty per copy sold instead of per order)</td></tr></tbody></table><h3 id="_9-cannibalization-conflict-—-overlapping-campaigns" tabindex="-1">9. <code>cannibalization_conflict</code> — overlapping campaigns <a class="header-anchor" href="#_9-cannibalization-conflict-—-overlapping-campaigns" aria-label="Permalink to &quot;9. \`cannibalization_conflict\` — overlapping campaigns&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Aspect</th><th>Detail</th></tr></thead><tbody><tr><td>Purpose</td><td>The same shopper term has spend/orders in <code>≥ 2</code> campaigns → campaigns bid against each other; consolidate or separate intent.</td></tr><tr><td>Trigger</td><td><code>campaigns ≥ 2</code> carrying the same search term, counting only spend/orders <strong>inside the evidence window</strong>.</td></tr><tr><td>Proposal</td><td>None — resolution is a human decision (route to an existing campaign or create a new one via the <a href="/amazon-king/guide/campaign-tools">campaign-creation flow</a>, which then drafts locked negatives).</td></tr><tr><td>Suppressed</td><td>Campaigns that can no longer serve the term are excluded before the <code>≥ 2</code> check, so a conflict you already resolved with a negative stops being raised (see below). No economics needed.</td></tr><tr><td>Impact</td><td>The combined spend across the still-competing campaigns.</td></tr><tr><td>Human review</td><td><strong>Yes.</strong> Advisory-only.</td></tr><tr><td>Version</td><td><code>cannibalization_conflict@2</code></td></tr></tbody></table><p><strong>Negative awareness.</strong> Historical spend stays in the 60-day evidence window long after a negative is applied, so the rule checks the negatives synced from Amazon (<code>negative_keywords</code> and <code>negative_targets</code>) rather than the metrics alone. A campaign counts as blocked when an enabled campaign-level negative exact or negative phrase matches the term, when an enabled campaign-level negative ASIN target matches an ASIN-shaped shopper term, or when every ad group that served the term has a matching ad-group-level negative. Blocked campaigns are recorded under <code>excludedCampaigns</code> in the evidence and do not count toward the threshold; when that leaves fewer than two competing campaigns, any pending or approved finding an earlier run raised for the term is expired instead of re-raised.</p><h2 id="shared-math-src-calc-ts" tabindex="-1">Shared math (<code>src/calc.ts</code>) <a class="header-anchor" href="#shared-math-src-calc-ts" aria-label="Permalink to &quot;Shared math (\`src/calc.ts\`)&quot;">​</a></h2><p>Ratios return <code>null</code> on division by zero — never Infinity/NaN. Genuinely invalid inputs (negative counts, out-of-range rates) throw, because that indicates a caller bug.</p><table tabindex="0"><thead><tr><th>Function</th><th>Formula</th></tr></thead><tbody><tr><td><code>acos</code></td><td><code>cost / sales</code>; null when sales = 0. ACoS is ad-spend-over-retail-revenue, <strong>not</strong> author profit.</td></tr><tr><td><code>roas</code></td><td><code>sales / cost</code>; null when cost = 0.</td></tr><tr><td><code>conversionRate</code></td><td><code>orders / clicks</code>; null when clicks = 0.</td></tr><tr><td><code>estimatedAdProfit</code></td><td><code>copies × royaltyPerSale − cost</code>, exact integer micro-units; may be negative.</td></tr><tr><td><code>royaltyCopies</code></td><td><code>max(orders, units)</code> — the copies a royalty is earned on. KDP pays per copy, so one order of three copies earns three royalties. Facts imported before the <code>units</code> columns existed report 0 units, and Amazon never reports fewer units than orders, so this degrades to <code>orders</code> there rather than erasing the royalty.</td></tr><tr><td><code>breakEvenCpc</code></td><td><code>smoothed CVR × royaltyPerSale</code>, rounded half-up to 4 dp — the profit ceiling for a bid.</td></tr><tr><td><code>smoothedConversionRate</code></td><td><code>(orders + 0.05 × 20) / (clicks + 20)</code> — a beta-style prior (rate 0.05, weight 20). At zero clicks it returns 0.05; with volume it converges to the observed rate.</td></tr></tbody></table><h3 id="proposedbid" tabindex="-1"><code>proposedBid</code> <a class="header-anchor" href="#proposedbid" aria-label="Permalink to &quot;\`proposedBid\`&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>raw multiplier     = targetAcos / observedAcos</span></span>
<span class="line"><span>guarded multiplier = clamp(raw, 0.85, 1.15)</span></span>
<span class="line"><span>profit ceiling CPC = smoothedCvr × royaltyPerSale × safetyFactor (default 1)</span></span>
<span class="line"><span>proposed bid       = min(current × guarded, ceiling, maxBid?)   — rounded half-up to 4 dp</span></span></code></pre></div><p>Returns null (the proposal is rejected) when economics are missing (<code>royaltyPerSale</code> or <code>targetAcos</code> null), inputs are invalid (non-positive bid, target, or observed ACoS), the result equals the current bid, or the relative change is below 1% (<code>minBidRelativeChange</code>) — sub-1% moves are treated as noise.</p><h3 id="confidence" tabindex="-1">Confidence <a class="header-anchor" href="#confidence" aria-label="Permalink to &quot;Confidence&quot;">​</a></h3><p><code>evidenceConfidence(observed, required) = min(1, observed / (2 × required))</code> — 0.5 at exactly the required threshold, growing toward 1.0 at 2× the requirement. Rules with several evidence dimensions combine them with <code>Math.min</code> (weakest link wins).</p><h3 id="ranking-and-priority" tabindex="-1">Ranking and priority <a class="header-anchor" href="#ranking-and-priority" aria-label="Permalink to &quot;Ranking and priority&quot;">​</a></h3><p>Drafts sort by <code>impactMicros</code> (absolute estimated profit delta / spend at stake, in micros), then <code>confidence</code>, then deterministically by <code>ruleVersion</code> and entity reference (<code>targetId</code>, then <code>searchTerm</code>, then <code>campaignId</code>) so equal drafts always order the same way. <code>priority</code> maps rank onto the 1–5 contract scale by quintile: the top fifth of a batch is priority 1, the bottom fifth priority 5.</p><h2 id="guardrails-src-guardrails-ts" tabindex="-1">Guardrails (<code>src/guardrails.ts</code>) <a class="header-anchor" href="#guardrails-src-guardrails-ts" aria-label="Permalink to &quot;Guardrails (\`src/guardrails.ts\`)&quot;">​</a></h2><p>Guardrails run before any write is sent to Amazon — at preview time and again at apply time. Defaults from <code>DEFAULT_GUARDRAIL_CONFIG</code>:</p><table tabindex="0"><thead><tr><th>Guardrail</th><th>Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>maxBidChangePct</code></td><td>0.15</td><td>Max relative bid change per action within a cooldown period.</td></tr><tr><td><code>bidCooldownDays</code></td><td>7</td><td>A target changed within this window cannot be changed again.</td></tr><tr><td><code>maxDailyBudgetMicros</code></td><td>null</td><td>Absolute daily-budget ceiling; null disables the check.</td></tr><tr><td><code>maxBudgetIncreasePct</code></td><td>0.25</td><td>Max single budget increase as a fraction of the current budget.</td></tr><tr><td><code>maxActionsPerChangeSet</code></td><td>20</td><td>Max actions in one change set.</td></tr><tr><td><code>maxExposureMicros</code></td><td>null</td><td>Max summed monetary exposure per change set; null disables. Exposure = Σ |bid deltas| + budget increases.</td></tr><tr><td><code>stalenessDays</code></td><td>3</td><td>Evidence older than this refuses the write.</td></tr><tr><td><code>protectedSearchTerms</code></td><td>[]</td><td>Lower-cased terms that may never receive a negative.</td></tr><tr><td><code>protectedCampaignIds</code></td><td>[]</td><td>Campaigns that may never be modified.</td></tr></tbody></table><p>Violation codes: <code>KILL_SWITCH_ENABLED</code>, <code>PROFILE_READ_ONLY</code>, <code>BID_CHANGE_TOO_LARGE</code>, <code>BID_COOLDOWN_ACTIVE</code>, <code>BUDGET_EXCEEDS_MAX</code>, <code>BUDGET_INCREASE_TOO_LARGE</code>, <code>TOO_MANY_ACTIONS</code>, <code>EXPOSURE_TOO_LARGE</code>, <code>PROTECTED_ENTITY</code>, <code>STALE_EVIDENCE</code>. Any violation blocks the write; at preview the violations come back in the response&#39;s <code>guardrails</code> warnings array (HTTP 200 — preview never writes), while apply rejects the set with <code>409 GUARDRAIL_VIOLATION</code>.</p><h2 id="cooldowns-launch-mode-staleness" tabindex="-1">Cooldowns, launch mode, staleness <a class="header-anchor" href="#cooldowns-launch-mode-staleness" aria-label="Permalink to &quot;Cooldowns, launch mode, staleness&quot;">​</a></h2><ul><li><strong>Cooldowns.</strong> After a related write, contradictory rules stay quiet for <code>cooldownDays</code> (7). A change matches when every field given in the rule&#39;s match clause (action type, target, campaign, search term) equals the change&#39;s fields. The apply-time guardrail independently enforces one bid change per target per <code>bidCooldownDays</code> (7).</li><li><strong>Launch mode.</strong> With a book&#39;s <code>goalMode</code> set to <code>launch</code>, <code>expensive_target</code> and <code>wasteful_search_term</code> are suppressed entirely — exploration spend is expected during launch.</li><li><strong>Missing economics.</strong> Rules that need profitability (<code>expensive_target</code>, <code>profitable_target</code>, <code>budget_constrained_winner</code>, <code>placement_opportunity</code>) do not fire without royalty and target ACoS. <code>search_term_harvest</code> still fires but leaves the bid to the human.</li><li><strong>Staleness and expiry.</strong> A recommendation expires <code>stalenessDays</code> (3 days) after it is created; the API rejects expired recommendations with <code>409 RECOMMENDATION_EXPIRED</code>. At apply time, any action whose evidence ends more than 3 days ago is refused with <code>STALE_EVIDENCE</code> — re-sync before writing.</li></ul><h2 id="related-reading" tabindex="-1">Related reading <a class="header-anchor" href="#related-reading" aria-label="Permalink to &quot;Related reading&quot;">​</a></h2><ul><li><a href="/amazon-king/guide/recommendations">Recommendations guide</a> — reading and acting on findings</li><li><a href="/amazon-king/guide/applying-changes">Applying changes</a> — the guarded write flow</li><li><a href="/amazon-king/architecture/data-pipeline">Architecture: data pipeline</a> — when rules run</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/optimization-rules.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const optimizationRules = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  optimizationRules as default
};
