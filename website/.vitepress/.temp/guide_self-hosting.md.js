import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Self-hosting in Production","description":"Deploy amazon-king on your own Docker host — requirements, secrets, HTTPS proxying, startup, read-only validation, and safe updates.","frontmatter":{"title":"Self-hosting in Production","description":"Deploy amazon-king on your own Docker host — requirements, secrets, HTTPS proxying, startup, read-only validation, and safe updates."},"headers":[],"relativePath":"guide/self-hosting.md","filePath":"guide/self-hosting.md","lastUpdated":1787088281000}');
const _sfc_main = { name: "guide/self-hosting.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="self-hosting-in-production" tabindex="-1">Self-hosting in Production <a class="header-anchor" href="#self-hosting-in-production" aria-label="Permalink to &quot;Self-hosting in Production&quot;">​</a></h1><p>This guide deploys the single-owner alpha on one Docker host using the included production Compose stack: PostgreSQL, a migration runner, the API, the worker, and a web service serving the built dashboard. It does not turn the application into a multi-tenant service, and it does not provide Amazon Ads API approval.</p><h2 id="safety-and-responsibility" tabindex="-1">Safety and responsibility <a class="header-anchor" href="#safety-and-responsibility" aria-label="Permalink to &quot;Safety and responsibility&quot;">​</a></h2><p>You — the operator — control the database, OAuth credentials, encryption keys, SMTP account, backups, retention, and ad spend. The current release has not completed the live validation gates in the product plan: <strong>keep <code>KILL_SWITCH=true</code></strong> until the full live-write checklist has been performed against an Amazon test environment or a dedicated low-risk campaign.</p><p>Each deployment needs its own approved Login with Amazon application and Amazon Ads API access. Never copy the maintainer&#39;s or another operator&#39;s credentials — see <a href="/amazon-king/guide/connecting-amazon">Connecting Amazon Ads</a>.</p><h2 id="requirements" tabindex="-1">Requirements <a class="header-anchor" href="#requirements" aria-label="Permalink to &quot;Requirements&quot;">​</a></h2><ul><li>A Linux host with current Docker Engine and Docker Compose</li><li>A domain name pointing to the host</li><li>An HTTPS reverse proxy (Caddy, nginx, or a managed load balancer)</li><li>An SMTP account or trusted SMTP relay</li><li>Amazon Ads API approval and a Login with Amazon application</li><li>Encrypted off-host backups</li></ul><p>The Docker host should not expose PostgreSQL or the API container directly. The provided web service binds to <code>127.0.0.1:8080</code> by default; override with <code>APP_BIND_ADDRESS</code> / <code>APP_PORT</code> only if you know why.</p><h2 id="configure" tabindex="-1">Configure <a class="header-anchor" href="#configure" aria-label="Permalink to &quot;Configure&quot;">​</a></h2><p>Create the gitignored production configuration:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prod-config</span></span></code></pre></div><p>Generate independent secrets — use a URL-safe password for PostgreSQL because it is embedded in a connection URL:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">openssl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> rand</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -hex</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 24</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # POSTGRES_PASSWORD</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">openssl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> rand</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -base64</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 48</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # SESSION_SECRET</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">openssl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> rand</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -hex</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 32</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # TOKEN_ENCRYPTION_KEY</span></span></code></pre></div><p>Set those three values in <code>.env.production</code>, replace every remaining <code>change-me</code> value, and set:</p><ul><li><code>OWNER_EMAIL</code> — the only address allowed to sign in</li><li><code>WEB_ORIGIN</code> and <code>API_PUBLIC_URL</code> — the public HTTPS origin</li><li><code>AMAZON_REDIRECT_URI</code> — <code>&lt;origin&gt;/api/integrations/amazon/callback</code>, registered <strong>exactly</strong> in the Login with Amazon app</li><li><code>LWA_CLIENT_ID</code> / <code>LWA_CLIENT_SECRET</code> — this deployment&#39;s own LWA app</li><li>working SMTP settings with a verified sender address — port 465 with <code>SMTP_SECURE=true</code> for implicit TLS, or the provider&#39;s STARTTLS port (commonly 587) with <code>SMTP_SECURE=false</code></li></ul><p>Boot-time validation refuses to start on a bad production config; the rules are in <a href="/amazon-king/guide/configuration#production-only-requirements">Configuration</a> and every variable is listed in the <a href="/amazon-king/reference/environment-variables">environment variable reference</a>.</p><h2 id="https-proxy" tabindex="-1">HTTPS proxy <a class="header-anchor" href="#https-proxy" aria-label="Permalink to &quot;HTTPS proxy&quot;">​</a></h2><p>Terminate HTTPS in a host reverse proxy and forward to <code>127.0.0.1:8080</code>. A minimal Caddy site:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ads.example.com {</span></span>
<span class="line"><span>  reverse_proxy 127.0.0.1:8080</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>The public origin must use HTTPS: production sessions use <code>Secure</code> cookies and will not work correctly over plain HTTP. Set <code>TRUST_PROXY=true</code> so the API sees real client IPs through your proxy.</p><h2 id="start-and-inspect" tabindex="-1">Start and inspect <a class="header-anchor" href="#start-and-inspect" aria-label="Permalink to &quot;Start and inspect&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prod-up</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prod-logs</span></span></code></pre></div><p>The migration container must complete before the API and worker start. Then:</p><ol><li>Check <code>https://&lt;your-origin&gt;/api/health</code>.</li><li>Request a sign-in link for <code>OWNER_EMAIL</code> — and verify that other addresses receive no link.</li></ol><p>Keep the host firewall closed except SSH and the HTTPS reverse proxy. Do not expose ports 3000 or 5432.</p><h2 id="validate-read-only-operation" tabindex="-1">Validate read-only operation <a class="header-anchor" href="#validate-read-only-operation" aria-label="Permalink to &quot;Validate read-only operation&quot;">​</a></h2><p>Before enabling any write capability, work through this checklist:</p><ol><li>Confirm <code>KILL_SWITCH=true</code> in both the API and worker containers.</li><li>Connect the Amazon account and enable only the intended profile.</li><li>Complete profile discovery, structure sync, and metrics sync.</li><li>Reconcile dashboard totals against the Amazon Ads console.</li><li>Confirm that missing KDP economics suppress profit recommendations.</li><li>Exercise your backup and restore procedures on an isolated host.</li><li>Review logs for accidentally leaked tokens, codes, or report URLs.</li></ol><p>Do not set <code>KILL_SWITCH=false</code> merely to explore the interface. When you are ready to test writes, follow the live validation sequence: a test account or dedicated low-risk campaign, one profile, one explicitly approved small change.</p><h2 id="install-on-a-phone" tabindex="-1">Install on a phone <a class="header-anchor" href="#install-on-a-phone" aria-label="Permalink to &quot;Install on a phone&quot;">​</a></h2><p>On phones the app is install-only: the production build is an installable PWA, and a phone browser tab shows install instructions instead of the dashboard. Sign in first in the phone browser, then install when the gate appears — on iOS an installed web app gets its own cookie storage, copied from Safari at the moment you add it to the home screen, so installing while signed in is what carries your session across.</p><p>Installing means the browser menu&#39;s <strong>Install app</strong> action on Android Chromium-based browsers, or <strong>Share → Add to Home Screen</strong> in iOS Safari. The installed app launches in standalone mode, without the browser&#39;s address and navigation bars. If you previously added the site as a plain home-screen shortcut, remove that shortcut and install again after deploying the PWA-enabled build. The service worker is network-only — no Amazon data is cached on the device.</p><p>Inside the installed app, sign-in links have to come back by hand. Tapping a link in your mail app opens the browser, and on iOS the installed app cannot see the session created there, so the sign-in screen and the &quot;Confirm it&#39;s you&quot; dialog both accept a pasted link: press and hold the link in the email, choose <strong>Copy Link</strong>, and paste it into the app. On Android the installed app shares the browser&#39;s cookies, so tapping the link and reopening the app is enough.</p><p>Tablets and desktops are unaffected and keep normal browser access.</p><h2 id="updating" tabindex="-1">Updating <a class="header-anchor" href="#updating" aria-label="Permalink to &quot;Updating&quot;">​</a></h2><p>Back up PostgreSQL and the report volume, review the release notes and migrations, then fetch and rebuild:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pull</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --ff-only</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prod-up</span></span></code></pre></div><p>Do not track a moving branch automatically on a production advertiser account. Pin and review releases once versioned releases are available.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><p>Day-two work — backups, monitoring, incidents, and data deletion — is in the <a href="/amazon-king/guide/operations">Operations runbook</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/self-hosting.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const selfHosting = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  selfHosting as default
};
