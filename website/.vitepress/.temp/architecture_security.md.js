import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Security model","description":"How amazon-king secures a self-hosted ad optimizer — two independent logins, browser isolation from Amazon credentials, stateless CSRF, layered rate limits, recent-auth gates, guarded writes, and secret handling.","frontmatter":{"title":"Security model","description":"How amazon-king secures a self-hosted ad optimizer — two independent logins, browser isolation from Amazon credentials, stateless CSRF, layered rate limits, recent-auth gates, guarded writes, and secret handling."},"headers":[],"relativePath":"architecture/security.md","filePath":"architecture/security.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "architecture/security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="security-model" tabindex="-1">Security model <a class="header-anchor" href="#security-model" aria-label="Permalink to &quot;Security model&quot;">​</a></h1><p>The security posture follows <code>docs/plan.md</code> §13: the app holds powerful Amazon credentials, so the design assumes the browser is hostile territory and keeps every secret server-side. This page describes what the code actually enforces, with file references.</p><h2 id="two-separate-logins" tabindex="-1">Two separate logins <a class="header-anchor" href="#two-separate-logins" aria-label="Permalink to &quot;Two separate logins&quot;">​</a></h2><p>Login A (app sign-in) and Login B (the Amazon OAuth connection) are independent systems that never share state. The app session never contains an Amazon token; the Amazon flow requires an app session but adds nothing to it.</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-12",
        class: "mermaid",
        graph: "sequenceDiagram%0A%20%20%20%20participant%20B%20as%20Browser%0A%20%20%20%20participant%20API%20as%20apps%2Fapi%0A%20%20%20%20participant%20DB%20as%20PostgreSQL%0A%20%20%20%20participant%20LWA%20as%20Login%20with%20Amazon%0A%0A%20%20%20%20rect%20rgb(60%2C%2070%2C%2090)%0A%20%20%20%20%20%20%20%20note%20over%20B%2CDB%3A%20Login%20A%20%E2%80%94%20passwordless%20email%20(app%20session)%0A%20%20%20%20%20%20%20%20B-%3E%3EAPI%3A%20POST%20%2Fapi%2Fsession%2Flogin%20%7Bemail%2C%20next%3F%7D%0A%20%20%20%20%20%20%20%20API-%3E%3EDB%3A%20login_tokens%20row%20(HMAC-SHA256%20hash%2C%2015-min%20TTL%2C%20origin)%0A%20%20%20%20%20%20%20%20API--%3E%3EB%3A%20200%20(email%20sent%2C%20dev%20logs%20the%20link)%0A%20%20%20%20%20%20%20%20B-%3E%3EAPI%3A%20GET%20%2Fapi%2Fsession%2Fverify%3Ftoken%3D%E2%80%A6%0A%20%20%20%20%20%20%20%20API-%3E%3EDB%3A%20consume%20token%20(single-use)%2C%20create%20session%20(hash%20only)%0A%20%20%20%20%20%20%20%20API--%3E%3EB%3A%20Set-Cookie%20ak_session%2C%20redirect%20to%20stored%20origin%20%2B%20next%0A%20%20%20%20end%0A%0A%20%20%20%20rect%20rgb(70%2C%2080%2C%2060)%0A%20%20%20%20%20%20%20%20note%20over%20B%2CLWA%3A%20Login%20B%20%E2%80%94%20Amazon%20OAuth%20(per%20workspace)%0A%20%20%20%20%20%20%20%20B-%3E%3EAPI%3A%20POST%20%2Fapi%2Fintegrations%2Famazon%2Fstart%0A%20%20%20%20%20%20%20%20API-%3E%3EDB%3A%20oauth_states%20row%20(state%20hash%2C%20user-bound%2C%2010-min%20TTL)%0A%20%20%20%20%20%20%20%20API--%3E%3EB%3A%20LWA%20consent%20URL%20(scope%20advertising%3A%3Acampaign_management)%0A%20%20%20%20%20%20%20%20B-%3E%3ELWA%3A%20owner%20consents%0A%20%20%20%20%20%20%20%20LWA-%3E%3EAPI%3A%20GET%20callback%3Fstate%3D%E2%80%A6%26code%3D%E2%80%A6%0A%20%20%20%20%20%20%20%20API-%3E%3EDB%3A%20mark%20state%20used%20BEFORE%20exchange%0A%20%20%20%20%20%20%20%20API-%3E%3ELWA%3A%20server-side%20code%20exchange%20(client%20secret)%0A%20%20%20%20%20%20%20%20LWA--%3E%3EAPI%3A%20refresh%20token%0A%20%20%20%20%20%20%20%20API-%3E%3EDB%3A%20AES-256-GCM%20encrypt%20%E2%86%92%20amazon_connections%0A%20%20%20%20end%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<h3 id="login-a-—-passwordless-email" tabindex="-1">Login A — passwordless email <a class="header-anchor" href="#login-a-—-passwordless-email" aria-label="Permalink to &quot;Login A — passwordless email&quot;">​</a></h3><p>Implemented in <code>apps/api/src/services/session.ts</code>.</p><ul><li><code>POST /api/session/login</code> generates a 32-byte random token and stores only its keyed HMAC-SHA256 hash in <code>login_tokens</code>, with a <strong>15-minute TTL</strong> (<code>LOGIN_TOKEN_TTL_MS</code>). The endpoint always returns 200 — it never reveals whether an address is allowed.</li><li>When <code>OWNER_EMAIL</code> is set (required in production), any other address is a silent no-op: no token is issued and the response is identical.</li><li>The token remembers the <strong>origin</strong> the login started from, checked against an exact allowlist: the configured <code>WEB_ORIGIN</code> always; in development only, localhost/127.0.0.1 on any port and <code>https://*.trycloudflare.com</code> quick tunnels. An optional <code>next</code> path returns the user to the page that required re-auth; only same-origin relative paths matching <code>^/[^/\\\\]</code> (max 500 chars) are accepted, and the stored value is re-validated at verify time.</li><li><code>GET /api/session/verify</code> <strong>consumes</strong> the token atomically (single-use), provisions the owner user/workspace on first login, and creates a session.</li><li>Sessions live 7 days, <strong>rolling</strong> — every authenticated request extends expiry (<code>SESSION_TTL_MS</code>). Only the token hash is stored server-side, plus IP and user agent for audit.</li><li>The <code>ak_session</code> cookie is <code>HttpOnly</code>, <code>SameSite=Lax</code>, <code>Path=/</code>, and <code>Secure</code> outside development.</li></ul><h3 id="login-b-—-amazon-ads-oauth" tabindex="-1">Login B — Amazon Ads OAuth <a class="header-anchor" href="#login-b-—-amazon-ads-oauth" aria-label="Permalink to &quot;Login B — Amazon Ads OAuth&quot;">​</a></h3><p>Implemented in <code>apps/api/src/services/amazon.ts</code> and <code>packages/amazon-ads/src/oauth.ts</code>.</p><ul><li>The consent URL requests exactly one scope: <code>advertising::campaign_management</code>.</li><li><code>state</code> is a 32-byte random value; only its hash is stored in <code>oauth_states</code>, bound to the authenticated user, with a <strong>10-minute TTL</strong> (<code>OAUTH_STATE_TTL_MS</code>).</li><li>On callback the state is <strong>marked used before the code exchange</strong>, so a replayed callback can never exchange twice. A state issued to a different user is refused (<code>foreign_state</code>). Missing/unknown/expired state redirects with <code>invalid_state</code> — no error detail leaks.</li><li>The authorization code is exchanged <strong>server-side</strong> with the LWA client secret. The returned refresh token is envelope-encrypted (below) before it is written to <code>amazon_connections.encrypted_refresh_token</code> (bytea) with its <code>encryption_key_version</code>.</li><li>Token refresh (<code>packages/amazon-ads/src/token-manager.ts</code>) is <strong>serialized per connection</strong> with an in-process mutex, refreshes 5 minutes before expiry, and runs a <strong>circuit breaker</strong>: an unrecoverable refresh error marks the connection <code>reconnect_required</code>, fires <code>onReconnectRequired</code> once, and refuses further attempts until the owner reconnects. The ciphertext is decrypted only in the API/worker, immediately before a refresh (<code>apps/worker/src/tokens.ts</code>).</li><li>Disconnect wipes the ciphertext first, then fails every pending queue job for the connection&#39;s profiles (<code>failPendingJobsForProfiles</code>) so no sync runs against a dead grant.</li></ul><h2 id="browser-isolation" tabindex="-1">Browser isolation <a class="header-anchor" href="#browser-isolation" aria-label="Permalink to &quot;Browser isolation&quot;">​</a></h2><ul><li>The LWA <strong>client secret exists only in the deployment environment</strong> — never in code, never per-user, never logged. <code>apps/api/src/config.ts</code> refuses to boot without it, and production config additionally requires <code>OWNER_EMAIL</code>, <code>API_PUBLIC_URL</code>, SMTP settings, and a non-default 32+ char <code>SESSION_SECRET</code>.</li><li>The browser receives only the consent URL and redirects from the OAuth flow — never codes or tokens — and never calls the Amazon Ads API directly; all Amazon traffic goes through the backend gateway.</li><li><strong>Strict CSP</strong> via helmet on the API: <code>default-src &#39;none&#39;</code>, <code>frame-ancestors &#39;none&#39;</code> (the API serves JSON only, so nothing else is needed).</li><li><strong>CORS</strong> is limited to the exact <code>WEB_ORIGIN</code> with credentials enabled.</li><li>The installable web app&#39;s service worker (<code>apps/web/public/sw.js</code>) is network-only: it bypasses every <code>/api</code> request and non-GET request entirely, so no Amazon data is ever cached by the browser.</li></ul><h2 id="csrf" tabindex="-1">CSRF <a class="header-anchor" href="#csrf" aria-label="Permalink to &quot;CSRF&quot;">​</a></h2><p>Mutations use a <strong>stateless, session-derived CSRF token</strong>: <code>HMAC-SHA256(SESSION_SECRET, &quot;csrf:&quot; + sessionTokenHash)</code> (<code>csrfTokenFor</code> in <code>session.ts</code>). The token is exposed on <code>GET /api/session</code> and must be sent back as the <code>x-csrf-token</code> header on every POST/PATCH/DELETE under <code>/api</code> — compared with <code>timingSafeEqual</code>. The only exemption is <code>POST /api/session/login</code>, which has no session yet. Because the token derives from the session rather than being stored, there is nothing to rotate or leak server-side, and logout invalidates it implicitly.</p><h2 id="rate-limits" tabindex="-1">Rate limits <a class="header-anchor" href="#rate-limits" aria-label="Permalink to &quot;Rate limits&quot;">​</a></h2><p><code>@fastify/rate-limit</code> with a global ceiling plus per-route buckets (<code>apps/api/src/server.ts</code>):</p><table tabindex="0"><thead><tr><th>Bucket</th><th>Limit</th><th>Routes</th></tr></thead><tbody><tr><td>Global</td><td>200 req/min</td><td>everything</td></tr><tr><td>STRICT</td><td>10 req/min</td><td>login request/verify, OAuth start/callback</td></tr><tr><td>WRITE</td><td>20 req/min</td><td>syncs, book mappings/covers, change-set apply/rollback, max-cpc, campaign creation</td></tr><tr><td>PREVIEW</td><td>120 req/min</td><td>change-set preview (a read, but expensive; the Change center fetches one per visible set)</td></tr></tbody></table><p>Over-limit requests get <code>429</code> with error code <code>RATE_LIMITED</code>.</p><h2 id="recent-authentication" tabindex="-1">Recent authentication <a class="header-anchor" href="#recent-authentication" aria-label="Permalink to &quot;Recent authentication&quot;">​</a></h2><p>Spend-changing actions require a session created within the last <strong>15 minutes</strong> (<code>RECENT_AUTH_MS</code>, <code>isRecentAuth</code>): applying a change set, rolling back an action, setting a campaign max CPC, and creating campaign-creation change sets. A gated call from an older session fails with <code>401 REAUTH_REQUIRED</code>; the web app answers with a one-click re-auth dialog whose magic link carries the current page as <code>next</code>. One deliberate exception: retrying a <strong>failed</strong> change set replays an already-approved payload through the same guarded path (Amazon state is re-read and compared before anything is sent), so it skips the recent-auth gate.</p><h2 id="guarded-writes" tabindex="-1">Guarded writes <a class="header-anchor" href="#guarded-writes" aria-label="Permalink to &quot;Guarded writes&quot;">​</a></h2><p>Writes default to <strong>off at two independent levels</strong>, and both fail closed:</p><ul><li>The global <strong>kill switch</strong> (<code>KILL_SWITCH</code>) defaults to enabled — writes stay disabled unless the operator explicitly sets <code>KILL_SWITCH=false</code>.</li><li>Every profile starts <code>write_enabled = false</code>; applying to a read-only profile fails with <code>WRITES_DISABLED</code>.</li></ul><p>The apply path in <code>apps/api/src/services/changes.ts</code> then enforces, in order: the change set is immutable and fingerprint-idempotent; recommendations in it must not be expired; <strong>live Amazon state is re-read</strong> and compared against the approved before snapshot (<code>STALE_BEFORE_STATE</code> blocks the set); guardrails re-run (<code>GUARDRAIL_VIOLATION</code> blocks); each action is applied with per-item result mapping; and every applied action is <strong>verified by a fresh re-read</strong> (<code>verified_at</code> set, else <code>verification_failed</code>). Rollback is a compensating API action recorded with <code>rollback_of_id</code>, never a database undo. Every step writes <code>audit_events</code>. See <a href="/amazon-king/guide/applying-changes">Applying changes</a> for the operator workflow.</p><h2 id="secret-handling" tabindex="-1">Secret handling <a class="header-anchor" href="#secret-handling" aria-label="Permalink to &quot;Secret handling&quot;">​</a></h2><ul><li><strong>Redaction is built into the loggers</strong> (<code>packages/observability</code>): authorization headers, access/refresh tokens, client secrets, OAuth codes, and pre-signed report download URLs are censored as <code>[REDACTED]</code> at the top level and one level deep. <code>redactSecrets</code> deep-clones unknown payloads (Amazon error bodies, request payloads) before they reach logs or error tracking. The report downloader logs &quot;URL redacted&quot; and never the pre-signed URL itself.</li><li><strong>Token encryption</strong> (<code>packages/crypto</code>): AES-256-GCM with a master key from <code>TOKEN_ENCRYPTION_KEY</code> (64 hex chars = 32 bytes). Ciphertext format is <code>keyVersion (uint16 BE) | iv (12 B) | authTag (16 B) | ciphertext</code>, so the version travels with the data. Rotation means adding <code>TOKEN_ENCRYPTION_KEY_V2</code> (and so on) — old rows keep decrypting under their recorded version.</li><li><strong>Audit events</strong> store only safe, non-secret details by schema convention; OAuth callback failures log only the sanitized error code, never the code or token material.</li></ul><h2 id="database-isolation-and-roles" tabindex="-1">Database isolation and roles <a class="header-anchor" href="#database-isolation-and-roles" aria-label="Permalink to &quot;Database isolation and roles&quot;">​</a></h2><p>All queries are scoped by <code>workspace_id</code> (the product is single-owner, but isolation is enforced in the data layer, not assumed). The plan calls for row-level security when deployed on Supabase and for separate low-privilege web, worker, and migration roles; the bundled compose deployment uses a single database user, so treat network isolation of the database as the boundary there. See <a href="/amazon-king/guide/self-hosting">Self-hosting</a> for hardening notes.</p><h2 id="operator-guidance" tabindex="-1">Operator guidance <a class="header-anchor" href="#operator-guidance" aria-label="Permalink to &quot;Operator guidance&quot;">​</a></h2><ul><li>Keep encrypted PostgreSQL backups with point-in-time recovery; the report artifact volume is re-downloadable and does not need backup.</li><li>The incident procedure — disable writes via the kill switch, rotate <code>SESSION_SECRET</code>/<code>TOKEN_ENCRYPTION_KEY*</code>, invalidate sessions, disconnect Amazon — is documented in <a href="/amazon-king/guide/operations">Operations</a>.</li><li>Never test write operations against important live campaigns first: use a dedicated low-risk campaign, one profile, one manually approved action.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture/security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const security = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  security as default
};
