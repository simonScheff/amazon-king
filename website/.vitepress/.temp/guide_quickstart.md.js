import { ssrRenderAttrs, ssrRenderAttr } from "vue/server-renderer";
import { _ as _imports_0 } from "./overview.DYAcUitT.js";
import { _ as _imports_1 } from "./settings.DppkyGHe.js";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Quickstart: First Session","description":"A guided first session with Amazon King — sign in, tour the dashboard, connect Amazon Ads, trigger a sync, enter book economics, and approve your first recommendation.","frontmatter":{"title":"Quickstart: First Session","description":"A guided first session with Amazon King — sign in, tour the dashboard, connect Amazon Ads, trigger a sync, enter book economics, and approve your first recommendation."},"headers":[],"relativePath":"guide/quickstart.md","filePath":"guide/quickstart.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "guide/quickstart.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="quickstart-first-session" tabindex="-1">Quickstart: first session <a class="header-anchor" href="#quickstart-first-session" aria-label="Permalink to &quot;Quickstart: first session&quot;">​</a></h1><p>This walkthrough takes you from a fresh <code>make run</code> to reviewing your first recommendation. It assumes the <a href="/amazon-king/guide/installation">installation</a> is done and all three processes are running.</p><div class="info custom-block"><p class="custom-block-title">No Amazon credentials yet?</p><p>Everything up to <a href="#connect-your-amazon-account">Connect your Amazon account</a> works offline. Without LWA credentials the pipeline can&#39;t import real data — follow <a href="/amazon-king/guide/connecting-amazon">Connecting Amazon Ads</a> when you&#39;re ready, and come back here for the sync walkthrough.</p></div><h2 id="_1-sign-in" tabindex="-1">1. Sign in <a class="header-anchor" href="#_1-sign-in" aria-label="Permalink to &quot;1. Sign in&quot;">​</a></h2><p>Open <code>http://localhost:5173</code>. Enter any email address on <code>/login</code> and submit. In development there is no mail server, so the API returns the magic link in its response and the page shows a <strong>&quot;Continue sign-in&quot;</strong> link — click it. (The same link is printed in the API logs if you miss it.)</p><p>You&#39;re now on the overview page. Login links are single-use and expire in 15 minutes; the session itself lasts 7 days, rolling.</p><h2 id="_2-tour-the-dashboard" tabindex="-1">2. Tour the dashboard <a class="header-anchor" href="#_2-tour-the-dashboard" aria-label="Permalink to &quot;2. Tour the dashboard&quot;">​</a></h2><p>The overview (<code>/</code>) is the control room. Before any data exists it will be mostly empty — here&#39;s what each part is for.</p><p><img${ssrRenderAttr("src", _imports_0)} alt="Dashboard overview with KPI cards, daily performance charts, and top problems"></p><ul><li><strong>Product filter</strong> — the checkbox dropdown at the bottom of the left sidebar scopes the whole app to one or more of your books (&quot;All products&quot; by default). The overview, campaigns, search terms, and recommendations screens all respect it, and the selection persists as you navigate.</li><li><strong>Country and date-range selectors</strong> — every number on the page respects these. Currency is never mixed across marketplaces; pick one country at a time.</li><li><strong>Six KPI cards</strong> — Spend, Sales, Orders, ACoS, Est. royalty, and Est. ad profit. Clicking a card toggles its series on the trend charts.</li><li><strong>Daily performance chart</strong> — spend, sales, and orders over the selected range. A <strong>daily profitability</strong> chart appears once book economics exist.</li><li><strong>Amber banner</strong> — shown when advertised products have no economics yet. Profit-based numbers are withheld, not guessed, until you fill them in.</li><li><strong>Top problems &amp; opportunities</strong> — the five highest-priority pending recommendations, one click away from the full list.</li><li><strong>Sync &amp; connection health</strong> — whether the Amazon connection is live and when each profile last synced.</li></ul><p>The left navigation covers the rest: <code>/recommendations</code> (filterable table of every finding), <code>/campaigns</code>, <code>/search-terms</code> (cross-campaign search-term analysis), <code>/changes</code> (the change center: every change set and its status), <code>/connect</code>, and <code>/settings</code>.</p><h2 id="_3-connect-your-amazon-account" tabindex="-1">3. Connect your Amazon account <a class="header-anchor" href="#_3-connect-your-amazon-account" aria-label="Permalink to &quot;3. Connect your Amazon account&quot;">​</a></h2><p>Go to <code>/connect</code>. Starting the connection sends you through Login With Amazon (scope <code>advertising::campaign_management</code>) — this is Login B, entirely separate from your app session. After you approve, the API exchanges the code server-side, envelope-encrypts the refresh token, and discovers your profiles.</p><p>Back on <code>/connect</code> you&#39;ll see the connection status and the discovered profiles (one per marketplace), each with a checkbox to enable it for syncing. Enable the profile(s) you care about — nothing syncs for disabled profiles.</p><h2 id="_4-trigger-the-first-sync" tabindex="-1">4. Trigger the first sync <a class="header-anchor" href="#_4-trigger-the-first-sync" aria-label="Permalink to &quot;4. Trigger the first sync&quot;">​</a></h2><p>The scheduler will pick up newly enabled profiles on its own, but you don&#39;t have to wait: press <strong>Sync now</strong> in Settings (or call <code>POST /api/profiles/:id/syncs</code> directly).</p><p>From here, the worker takes over automatically:</p><ol><li><strong>Structure sync</strong> imports campaigns, ad groups, targets, and ads for the enabled profile. The scheduler repeats it every 45 minutes.</li><li><strong>Metrics sync</strong> runs once daily after 05:00 UTC, importing yesterday&#39;s Sponsored Products reports (campaigns, targeting, search terms, advertised products), followed by a trailing 14-day re-sync to absorb Amazon&#39;s attribution lag. Each import is requested, polled, downloaded, validated, and reconciled before it&#39;s marked complete.</li><li><strong>Recommendation run</strong> is chained automatically after a successful metrics sync (and skipped if the last complete metrics sync is older than 48 hours). The optimizer evaluates all nine rules over 7/14/30/60-day windows.</li></ol><p>A <code>schedule_tick</code> job fires every 15 minutes to enqueue whatever is due — structure syncs, the daily metrics jobs, hourly-adjacent health checks — so a fresh profile gets its first structure sync within minutes even if you never press anything.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>The first metrics sync only imports yesterday. Give the daily cycle a few days to build history before judging recommendations — rules require minimum evidence and will stay quiet until the data supports them.</p></div><h2 id="_5-enter-book-economics" tabindex="-1">5. Enter book economics <a class="header-anchor" href="#_5-enter-book-economics" aria-label="Permalink to &quot;5. Enter book economics&quot;">​</a></h2><p>Go to <code>/settings</code>. The book economics section lists advertised ASINs the sync discovered; identify each one (title, format) and enter, per marketplace:</p><ul><li><strong>List price</strong> and <strong>estimated royalty per sale</strong> — what you actually earn per order (check your KDP royalty reports),</li><li><strong>target ACoS</strong> as a 0–1 fraction (e.g. <code>0.30</code>, not <code>30</code>),</li><li><strong>goal mode</strong> — <code>profit</code>, <code>balanced</code>, <code>launch</code>, or <code>visibility</code>,</li><li>optional ceilings: max spend without a sale, max bid, max daily budget.</li></ul><p><img${ssrRenderAttr("src", _imports_1)} alt="Settings page with book economics forms for entering royalty per sale and target ACoS"></p><p>The moment economics exist, the dashboard&#39;s estimated royalty and estimated ad profit KPIs become real, and the profit-based rules start participating in recommendation runs. See the <a href="/amazon-king/guide/book-economics">book economics guide</a> for how to compute royalty per sale for each format.</p><h2 id="_6-review-your-first-recommendation" tabindex="-1">6. Review your first recommendation <a class="header-anchor" href="#_6-review-your-first-recommendation" aria-label="Permalink to &quot;6. Review your first recommendation&quot;">​</a></h2><p>Open <code>/recommendations</code>. Filter by profile, priority (P1–P5), or rule type. Each row shows the finding, its evidence window, and its confidence; expanding it shows the exact inputs the rule stored — spend, orders, clicks, smoothed conversion rate, and the rule version that produced it.</p><p>Recommendations expire: if the underlying data goes stale, the finding moves to <code>expired</code> instead of silently applying to old numbers. Advisory-only types (harvesting, budget, placement, cannibalization) can&#39;t be applied directly — they point you at manual actions or the <a href="/amazon-king/guide/campaign-tools">campaign tools</a>.</p><h2 id="_7-approve-it-into-a-draft-change-set" tabindex="-1">7. Approve it into a draft change set <a class="header-anchor" href="#_7-approve-it-into-a-draft-change-set" aria-label="Permalink to &quot;7. Approve it into a draft change set&quot;">​</a></h2><p>For an applicable recommendation (a bid change or a negative exact keyword), approving it creates a <strong>draft change set</strong> in <code>/changes</code>. Nothing has touched Amazon at this point — the draft is an immutable, inspectable bundle of proposed writes.</p><p>Applying is a deliberate, gated step covered in <a href="/amazon-king/guide/applying-changes">applying &amp; rolling back changes</a>: preview against a fresh Amazon re-read, recent re-authentication, guardrail re-checks, per-item apply, and post-write verification. Note that with <code>KILL_SWITCH=true</code> (the default) and a profile that isn&#39;t <code>writeEnabled</code>, applies are blocked — that is the intended alpha posture until you&#39;ve validated the pipeline on a low-risk campaign.</p><h2 id="where-to-go-next" tabindex="-1">Where to go next <a class="header-anchor" href="#where-to-go-next" aria-label="Permalink to &quot;Where to go next&quot;">​</a></h2><ul><li><a href="/amazon-king/guide/recommendations">Reviewing recommendations</a> — filters, evidence, and expiry in depth</li><li><a href="/amazon-king/guide/applying-changes">Applying &amp; rolling back changes</a> — the guarded write lifecycle</li><li><a href="/amazon-king/guide/operations">Operations runbook</a> — sync health, logs, and the kill switch</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/quickstart.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const quickstart = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  quickstart as default
};
