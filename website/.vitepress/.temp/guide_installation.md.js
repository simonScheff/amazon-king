import { ssrRenderAttrs, ssrRenderStyle, ssrRenderAttr } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/amazon-king/screenshots/login.png";
const __pageData = JSON.parse('{"title":"Installation","description":"Install Amazon King for local development with Node.js, pnpm, Docker, and Make — one command starts PostgreSQL, the API, the worker, and the dashboard.","frontmatter":{"title":"Installation","description":"Install Amazon King for local development with Node.js, pnpm, Docker, and Make — one command starts PostgreSQL, the API, the worker, and the dashboard."},"headers":[],"relativePath":"guide/installation.md","filePath":"guide/installation.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "guide/installation.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="installation" tabindex="-1">Installation <a class="header-anchor" href="#installation" aria-label="Permalink to &quot;Installation&quot;">​</a></h1><p>This gets the whole stack running locally: PostgreSQL in Docker, the API on port 3000, the background worker, and the dashboard on port 5173.</p><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><ul><li><strong>Node.js ≥ 20</strong> — the API and worker are Node + TypeScript.</li><li><strong>pnpm 10.12.4</strong> — the repo pins the package manager via the <code>packageManager</code> field in <code>package.json</code>; with Corepack enabled (<code>corepack enable</code>), the correct version is used automatically.</li><li><strong>Docker</strong> (with the Compose plugin) — local PostgreSQL runs as a container; nothing else is installed on your host.</li><li><strong>GNU Make</strong> — the Makefile is the single entry point for setup, running, testing, and the production stack.</li></ul><h2 id="clone-and-set-up" tabindex="-1">Clone and set up <a class="header-anchor" href="#clone-and-set-up" aria-label="Permalink to &quot;Clone and set up&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> clone</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://github.com/simonScheff/amazon-king.git</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> amazon-king</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> setup</span></span></code></pre></div><p><code>make setup</code> installs workspace dependencies and creates <code>.env</code> from <code>.env.example</code> if one doesn&#39;t exist.</p><h2 id="configure-env" tabindex="-1">Configure <code>.env</code> <a class="header-anchor" href="#configure-env" aria-label="Permalink to &quot;Configure \`.env\`&quot;">​</a></h2><p>Open <code>.env</code> and review the values. What each one is for in local dev:</p><table tabindex="0"><thead><tr><th>Variable</th><th>Required for</th><th>Notes</th></tr></thead><tbody><tr><td><code>DATABASE_URL</code></td><td>API, worker, migrations</td><td>Default points at the Docker database: <code>postgres://postgres:postgres@localhost:5432/amazon_king</code>.</td></tr><tr><td><code>SESSION_SECRET</code></td><td>API</td><td>Signs app session cookies. Minimum 16 characters; the example value is fine for local dev only.</td></tr><tr><td><code>WEB_ORIGIN</code></td><td>API</td><td><code>http://localhost:5173</code> — used for CORS and post-login redirects.</td></tr><tr><td><code>AMAZON_REDIRECT_URI</code></td><td>API</td><td><code>http://localhost:3000/api/integrations/amazon/callback</code> — must end in <code>/api/integrations/amazon/callback</code> and match your LWA app registration.</td></tr><tr><td><code>LWA_CLIENT_ID</code> / <code>LWA_CLIENT_SECRET</code></td><td>API (boot), worker (Amazon calls)</td><td>The API refuses to boot without them — validation requires a non-empty value even in dev, so <strong>dummy values are fine</strong> for pure local UI work. The worker boots without them, but any Amazon call fails with <code>missing_lwa_credentials</code> until real values are set.</td></tr><tr><td><code>TOKEN_ENCRYPTION_KEY</code></td><td>API, worker</td><td>64 hex chars; encrypts Amazon refresh tokens at rest. The all-zeros example is dev-only.</td></tr><tr><td><code>REPORT_STORAGE_DIR</code></td><td>Worker</td><td>Local stand-in for S3 report storage; defaults to <code>./.data/reports</code>.</td></tr><tr><td><code>KILL_SWITCH</code></td><td>API, worker</td><td>Defaults to <code>true</code> (fail closed). Leave it <code>true</code> until live validation is done.</td></tr><tr><td><code>OWNER_EMAIL</code></td><td>Production only</td><td>Optional in dev — any email can sign in locally.</td></tr><tr><td><code>SMTP_*</code></td><td>Production only</td><td>Optional in dev. Without SMTP, login links are returned in the API response and logged instead of emailed.</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>The API validates its environment at boot and exits on any missing required value. If startup fails, the error names the variable — check <code>.env</code> first. <code>make run</code> also runs a <code>preflight</code> check for <code>DATABASE_URL</code>, <code>SESSION_SECRET</code>, and the two LWA variables before starting anything.</p></div><p>For the full list including production-only variables, see the <a href="/amazon-king/reference/environment-variables">environment variables reference</a>.</p><h2 id="run" tabindex="-1">Run <a class="header-anchor" href="#run" aria-label="Permalink to &quot;Run&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span></span></code></pre></div><p>This one target chains everything: <code>setup</code> → <code>preflight</code> → <code>db-up</code> (starts PostgreSQL via <code>docker compose</code> and waits for readiness) → <code>migrate</code> (applies SQL migrations) → starts all three processes. Ctrl-C stops them together.</p><p>Three processes run side by side:</p><ul><li><strong>API</strong> (<code>http://localhost:3000</code>) — the Fastify backend: auth, the guarded write service, and the OAuth callback.</li><li><strong>Worker</strong> — the background job loop: syncs, report imports, recommendation runs. Read-only against Amazon in the MVP.</li><li><strong>Web</strong> (<code>http://localhost:5173</code>) — the Vite dev server for the dashboard; it proxies <code>/api</code> to the API on port 3000, so the browser only ever talks to one origin.</li></ul><p>Open <code>http://localhost:5173</code> and you&#39;re at the sign-in screen.</p><h2 id="signing-in-during-development" tabindex="-1">Signing in during development <a class="header-anchor" href="#signing-in-during-development" aria-label="Permalink to &quot;Signing in during development&quot;">​</a></h2><p>Enter any email address (there is no <code>OWNER_EMAIL</code> lock in dev). With no SMTP configured, the magic link can&#39;t be emailed, so the API does two things:</p><ul><li>returns the link in the login response, which the dashboard surfaces as a <strong>&quot;Continue sign-in&quot;</strong> link, and</li><li>logs the full link to the API console.</li></ul><p><img${ssrRenderAttr("src", _imports_0)} alt="Sign-in screen showing the dev &quot;Continue sign-in&quot; link after submitting an email"></p><p>Click the link (or paste it from the logs) and you&#39;re signed in. Login links are single-use and expire after 15 minutes; the session lasts 7 days, rolling.</p><h2 id="stopping-and-cleaning-up" tabindex="-1">Stopping and cleaning up <a class="header-anchor" href="#stopping-and-cleaning-up" aria-label="Permalink to &quot;Stopping and cleaning up&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> stop</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # stop local PostgreSQL</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> clean</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # also delete the database volume and ./.data (destroys local data)</span></span></code></pre></div><h2 id="all-make-targets" tabindex="-1">All Make targets <a class="header-anchor" href="#all-make-targets" aria-label="Permalink to &quot;All Make targets&quot;">​</a></h2><p><code>make help</code> prints the current list:</p><table tabindex="0"><thead><tr><th>Target</th><th>What it does</th></tr></thead><tbody><tr><td><code>help</code></td><td>Show available targets</td></tr><tr><td><code>install</code></td><td>Install workspace dependencies</td></tr><tr><td><code>setup</code></td><td>Install deps and create <code>.env</code> from <code>.env.example</code> if missing</td></tr><tr><td><code>preflight</code></td><td>Validate required local configuration before starting services</td></tr><tr><td><code>db-up</code></td><td>Start local PostgreSQL (docker compose)</td></tr><tr><td><code>migrate</code></td><td>Apply database migrations</td></tr><tr><td><code>run</code></td><td>Run the entire application (db + api + worker + web)</td></tr><tr><td><code>dev</code></td><td>Alias for <code>run</code></td></tr><tr><td><code>test</code></td><td>Run all tests</td></tr><tr><td><code>typecheck</code></td><td>Typecheck all packages</td></tr><tr><td><code>lint</code></td><td>Check formatting</td></tr><tr><td><code>build</code></td><td>Build the web app</td></tr><tr><td><code>check</code></td><td>Formatting, typechecks, tests, and the production web build</td></tr><tr><td><code>prod-config</code></td><td>Create the ignored production environment file if missing</td></tr><tr><td><code>prod-preflight</code></td><td>Reject missing or placeholder production configuration</td></tr><tr><td><code>prod-up</code></td><td>Build and start the self-hosted production stack</td></tr><tr><td><code>prod-logs</code></td><td>Follow production stack logs</td></tr><tr><td><code>prod-stop</code></td><td>Stop the production stack without deleting persistent data</td></tr><tr><td><code>stop</code></td><td>Stop local PostgreSQL</td></tr><tr><td><code>clean</code></td><td>Stop db and delete its data volume (destroys local data)</td></tr></tbody></table><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><a href="/amazon-king/guide/quickstart">Quickstart</a> — a guided first session through the dashboard</li><li><a href="/amazon-king/guide/connecting-amazon">Connecting Amazon Ads</a> — when you&#39;re ready for real data</li><li><a href="/amazon-king/guide/self-hosting">Self-hosting in production</a> — the <code>prod-*</code> targets in detail</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/installation.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const installation = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  installation as default
};
