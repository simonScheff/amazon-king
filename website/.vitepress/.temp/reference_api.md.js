import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"HTTP API Reference","description":"Complete reference for the amazon-king REST API — authentication, CSRF, rate limits, and every endpoint with request and response schemas.","frontmatter":{"title":"HTTP API Reference","description":"Complete reference for the amazon-king REST API — authentication, CSRF, rate limits, and every endpoint with request and response schemas."},"headers":[],"relativePath":"reference/api.md","filePath":"reference/api.md","lastUpdated":1786911396000}');
const _sfc_main = { name: "reference/api.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="http-api-reference" tabindex="-1">HTTP API Reference <a class="header-anchor" href="#http-api-reference" aria-label="Permalink to &quot;HTTP API Reference&quot;">​</a></h1><p>The API is served by the <code>api</code> process (Fastify). In the production compose stack it sits behind the bundled web proxy, so all endpoints are <strong>same-origin with the dashboard</strong>. In local development the API listens on <code>http://localhost:3000</code> and the Vite dev server proxies <code>/api</code> to it.</p><h2 id="conventions" tabindex="-1">Conventions <a class="header-anchor" href="#conventions" aria-label="Permalink to &quot;Conventions&quot;">​</a></h2><h3 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h3><p>Sign-in is passwordless email (Login A). A successful verify sets an <code>ak_session</code> cookie (<code>HttpOnly</code>, <code>SameSite=Lax</code>, <code>Secure</code> outside development, ~7 day rolling expiry). Every endpoint except <code>GET /api/health</code> and the login start/verify pair requires this cookie; a missing or expired session returns <code>401 UNAUTHENTICATED</code>.</p><p>The Amazon OAuth connection (Login B) is a separate flow — the session never contains an Amazon token and the browser never talks to the Amazon Ads API directly.</p><h3 id="csrf" tabindex="-1">CSRF <a class="header-anchor" href="#csrf" aria-label="Permalink to &quot;CSRF&quot;">​</a></h3><p>Every <code>POST</code>, <code>PATCH</code>, and <code>DELETE</code> under <code>/api</code> requires the <code>x-csrf-token</code> header carrying the per-session token returned by <code>GET /api/session</code> (<code>csrfToken</code>). The only exemption is <code>POST /api/session/login</code> (no session exists yet). A missing or mismatched token returns <code>403 CSRF_MISMATCH</code>.</p><h3 id="recent-authentication" tabindex="-1">Recent authentication <a class="header-anchor" href="#recent-authentication" aria-label="Permalink to &quot;Recent authentication&quot;">​</a></h3><p>Spend-changing actions require a sign-in no older than 15 minutes. Affected endpoints return <code>401 REAUTH_REQUIRED</code> when the session is too old:</p><ul><li><code>POST /api/campaigns/:campaignId/max-cpc</code></li><li><code>POST /api/campaigns/:campaignId/state</code></li><li><code>POST /api/campaigns/:campaignId/name</code></li><li><code>POST /api/campaign-creation-change-sets</code></li><li><code>POST /api/change-sets/:id/apply</code> — except when retrying a <code>failed</code> change set, which replays an already-approved payload through the same guarded path</li><li><code>POST /api/change-actions/:actionId/rollback</code></li></ul><h3 id="error-envelope" tabindex="-1">Error envelope <a class="header-anchor" href="#error-envelope" aria-label="Permalink to &quot;Error envelope&quot;">​</a></h3><p>Every non-2xx response has the same shape:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;error&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;code&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;VALIDATION_ERROR&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;message&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;Request failed validation&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;details&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;issues&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [] }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><code>details</code> is optional. See <a href="/amazon-king/reference/errors">Errors</a> for the full code list.</p><h3 id="rate-limits" tabindex="-1">Rate limits <a class="header-anchor" href="#rate-limits" aria-label="Permalink to &quot;Rate limits&quot;">​</a></h3><p>Limits are per client, per minute. Exceeding any tier returns <code>429 RATE_LIMITED</code>.</p><table tabindex="0"><thead><tr><th>Tier</th><th>Limit</th><th>Applies to</th></tr></thead><tbody><tr><td>Global</td><td>200/min</td><td>Every route without an explicit tier</td></tr><tr><td>STRICT</td><td>10/min</td><td>Login start/verify, Amazon OAuth start/callback</td></tr><tr><td>WRITE</td><td>20/min</td><td>Sync requests, mappings, cover, cannibalization/max-CPC/creation sets, apply, rollback</td></tr><tr><td>PREVIEW</td><td>120/min</td><td><code>GET /api/change-sets/:id/preview</code></td></tr></tbody></table><h3 id="request-ids" tabindex="-1">Request IDs <a class="header-anchor" href="#request-ids" aria-label="Permalink to &quot;Request IDs&quot;">​</a></h3><p>An <code>x-request-id</code> request header is propagated when present, otherwise one is generated. The value is echoed on every response as <code>x-request-id</code> and appears in server logs.</p><h3 id="field-formats" tabindex="-1">Field formats <a class="header-anchor" href="#field-formats" aria-label="Permalink to &quot;Field formats&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Type</th><th>Format</th><th>Example</th></tr></thead><tbody><tr><td>Money / bid</td><td>String decimal, signed: <code>/^-?\\d+(\\.\\d{1,4})?$/</code></td><td><code>&quot;12.3400&quot;</code></td></tr><tr><td>Money (non-negative)</td><td><code>/^\\d+(\\.\\d{1,4})?$/</code></td><td><code>&quot;0.45&quot;</code></td></tr><tr><td>Date</td><td><code>YYYY-MM-DD</code></td><td><code>&quot;2026-08-01&quot;</code></td></tr><tr><td>Timestamp</td><td>ISO 8601 with timezone offset</td><td><code>&quot;2026-08-16T15:49:56.726Z&quot;</code></td></tr><tr><td>Currency</td><td>ISO 4217, <code>/^[A-Z]{3}$/</code></td><td><code>&quot;USD&quot;</code></td></tr></tbody></table><p>Monetary values are always string-encoded decimals; floating point never appears in payloads. Values are never aggregated across currencies — mixing currencies in one view returns <code>409 MIXED_CURRENCY</code>.</p><hr><h2 id="health" tabindex="-1">Health <a class="header-anchor" href="#health" aria-label="Permalink to &quot;Health&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Method</th><th>Path</th><th>Auth</th><th>Response</th></tr></thead><tbody><tr><td>GET</td><td><code>/api/health</code></td><td>none</td><td><code>{ &quot;status&quot;: &quot;ok&quot; }</code></td></tr></tbody></table><hr><h2 id="session" tabindex="-1">Session <a class="header-anchor" href="#session" aria-label="Permalink to &quot;Session&quot;">​</a></h2><h3 id="post-api-session-login" tabindex="-1"><code>POST /api/session/login</code> <a class="header-anchor" href="#post-api-session-login" aria-label="Permalink to &quot;\`POST /api/session/login\`&quot;">​</a></h3><p>Starts the passwordless email flow. Always returns 200, even for unknown emails, so the endpoint never reveals whether an address is allowed.</p><ul><li><strong>Auth:</strong> none (CSRF-exempt). <strong>Rate:</strong> STRICT.</li></ul><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Constraints</th></tr></thead><tbody><tr><td>email</td><td>string</td><td>Valid email address</td></tr><tr><td>next</td><td>string</td><td>Optional. Same-origin path <code>/^\\/[^/\\\\]/</code>, max 500 chars; the post-verify redirect lands here</td></tr></tbody></table><p>Response <code>200</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;ok&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre></div><p>In development, when SMTP is not configured, the magic link is also returned as <code>devLoginUrl</code> and logged; with SMTP configured the link is only emailed.</p><h3 id="get-api-session-verify-token" tabindex="-1"><code>GET /api/session/verify?token=</code> <a class="header-anchor" href="#get-api-session-verify-token" aria-label="Permalink to &quot;\`GET /api/session/verify?token=\`&quot;">​</a></h3><p>Completes login. Not a JSON endpoint.</p><ul><li><strong>Auth:</strong> none. <strong>Rate:</strong> STRICT.</li><li>Success: <code>302</code> to the web origin (plus the <code>next</code> path when one was stored at login start), setting the <code>ak_session</code> cookie.</li><li>Failure: <code>302</code> to <code>{WEB_ORIGIN}/login?error=invalid_token</code>.</li></ul><h3 id="post-api-session-logout" tabindex="-1"><code>POST /api/session/logout</code> <a class="header-anchor" href="#post-api-session-logout" aria-label="Permalink to &quot;\`POST /api/session/logout\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF.</li><li>Response <code>204</code>; clears the cookie and invalidates the session server-side.</li></ul><h3 id="get-api-session" tabindex="-1"><code>GET /api/session</code> <a class="header-anchor" href="#get-api-session" aria-label="Permalink to &quot;\`GET /api/session\`&quot;">​</a></h3><p>Returns the current session and a fresh CSRF token.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;userId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;1&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;workspaceId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;1&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;email&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;owner@example.com&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;expiresAt&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;2026-08-23T15:49:56.726Z&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;csrfToken&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;...&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><hr><h2 id="amazon-connection-oauth" tabindex="-1">Amazon connection (OAuth) <a class="header-anchor" href="#amazon-connection-oauth" aria-label="Permalink to &quot;Amazon connection (OAuth)&quot;">​</a></h2><h3 id="post-api-integrations-amazon-start" tabindex="-1"><code>POST /api/integrations/amazon/start</code> <a class="header-anchor" href="#post-api-integrations-amazon-start" aria-label="Permalink to &quot;\`POST /api/integrations/amazon/start\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF. <strong>Rate:</strong> STRICT.</li><li>Response <code>200</code>: <code>{ &quot;url&quot;: &quot;https://www.amazon.com/ap/oa?...&quot; }</code> — navigate the browser to <code>url</code> to authorize the <code>advertising::campaign_management</code> scope. State is single-use and expires after 10 minutes.</li></ul><h3 id="get-api-integrations-amazon-callback-state-code" tabindex="-1"><code>GET /api/integrations/amazon/callback?state&amp;code</code> <a class="header-anchor" href="#get-api-integrations-amazon-callback-state-code" aria-label="Permalink to &quot;\`GET /api/integrations/amazon/callback?state&amp;code\`&quot;">​</a></h3><p>OAuth redirect target (<code>AMAZON_REDIRECT_URI</code> must point here). Not a JSON endpoint.</p><ul><li><strong>Auth:</strong> session cookie if present (its absence becomes a redirect error). <strong>Rate:</strong> STRICT.</li><li>Success: <code>302</code> to <code>{WEB_ORIGIN}/connect?connected=1</code>.</li><li>Failure: <code>302</code> to <code>{WEB_ORIGIN}/connect?error=&lt;code&gt;</code> with one of <code>invalid_callback</code>, <code>invalid_state</code>, <code>session_required</code>, <code>foreign_state</code>, <code>exchange_failed</code>, <code>profile_discovery_failed</code> (see <a href="/amazon-king/reference/errors#oauth-redirect-errors">Errors</a>).</li></ul><h3 id="get-api-integrations-amazon-status" tabindex="-1"><code>GET /api/integrations/amazon/status</code> <a class="header-anchor" href="#get-api-integrations-amazon-status" aria-label="Permalink to &quot;\`GET /api/integrations/amazon/status\`&quot;">​</a></h3><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;status&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;connected&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;grantedAt&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;2026-08-01T10:00:00.000Z&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;lastErrorCode&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">null</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p><code>status</code> is <code>connected</code>, <code>reconnect_required</code>, or <code>disconnected</code>.</p><h3 id="post-api-integrations-amazon-disconnect" tabindex="-1"><code>POST /api/integrations/amazon/disconnect</code> <a class="header-anchor" href="#post-api-integrations-amazon-disconnect" aria-label="Permalink to &quot;\`POST /api/integrations/amazon/disconnect\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF.</li><li>Response <code>204</code>. Removes the stored (encrypted) refresh token and marks the connection disconnected.</li></ul><hr><h2 id="profiles-syncs" tabindex="-1">Profiles &amp; syncs <a class="header-anchor" href="#profiles-syncs" aria-label="Permalink to &quot;Profiles &amp; syncs&quot;">​</a></h2><h3 id="get-api-profiles" tabindex="-1"><code>GET /api/profiles</code> <a class="header-anchor" href="#get-api-profiles" aria-label="Permalink to &quot;\`GET /api/profiles\`&quot;">​</a></h3><p>Returns every Amazon Ads profile mirrored into the workspace:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>profileId</td><td>string</td><td>Amazon profile id, used in paths</td></tr><tr><td>accountId</td><td>string | null</td><td>Amazon account entity id</td></tr><tr><td>region</td><td><code>&quot;NA&quot; | &quot;EU&quot; | &quot;FE&quot;</code></td><td></td></tr><tr><td>countryCode</td><td>string</td><td>e.g. <code>&quot;US&quot;</code></td></tr><tr><td>currencyCode</td><td>string</td><td>ISO 4217</td></tr><tr><td>timezone</td><td>string | null</td><td></td></tr><tr><td>accountType</td><td>string | null</td><td></td></tr><tr><td>enabled</td><td>boolean</td><td>Included in syncs and dashboards</td></tr><tr><td>writeEnabled</td><td>boolean</td><td>Owner opt-in for guarded writes</td></tr></tbody></table><h3 id="patch-api-profiles-profileid" tabindex="-1"><code>PATCH /api/profiles/:profileId</code> <a class="header-anchor" href="#patch-api-profiles-profileid" aria-label="Permalink to &quot;\`PATCH /api/profiles/:profileId\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF.</li></ul><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Required</th></tr></thead><tbody><tr><td>enabled</td><td>boolean</td><td>no</td></tr><tr><td>writeEnabled</td><td>boolean</td><td>no</td></tr></tbody></table><p>Response <code>200</code>: the updated profile (same shape as <code>GET /api/profiles</code> rows). <code>404 NOT_FOUND</code> for an unknown profile.</p><h3 id="post-api-profiles-profileid-syncs" tabindex="-1"><code>POST /api/profiles/:profileId/syncs</code> <a class="header-anchor" href="#post-api-profiles-profileid-syncs" aria-label="Permalink to &quot;\`POST /api/profiles/:profileId/syncs\`&quot;">​</a></h3><p>Requests a manual sync. The work runs in the worker, never in the request: a <code>structure_sync</code> job plus a <code>metrics_sync</code> job covering the trailing 60 complete UTC days are enqueued.</p><ul><li><strong>Auth:</strong> session + CSRF. <strong>Rate:</strong> WRITE.</li><li>Response <code>200</code>: a SyncRun (below).</li><li>Errors: <code>409 PROFILE_DISABLED</code> when the profile is not enabled, <code>404 NOT_FOUND</code> for an unknown profile.</li></ul><h3 id="get-api-syncs-syncid" tabindex="-1"><code>GET /api/syncs/:syncId</code> <a class="header-anchor" href="#get-api-syncs-syncid" aria-label="Permalink to &quot;\`GET /api/syncs/:syncId\`&quot;">​</a></h3><p>Response <code>200</code>: SyncRun; <code>404 NOT_FOUND</code> when unknown.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th></tr></thead><tbody><tr><td>id</td><td>string</td></tr><tr><td>profileId</td><td>string (Amazon profile id)</td></tr><tr><td>kind</td><td><code>&quot;structure&quot; | &quot;metrics&quot; | &quot;backfill&quot;</code></td></tr><tr><td>status</td><td>string (<code>running</code>, <code>complete</code>, <code>failed</code>, …)</td></tr><tr><td>startedAt</td><td>timestamp</td></tr><tr><td>finishedAt</td><td>timestamp | null</td></tr><tr><td>error</td><td>string | null</td></tr></tbody></table><hr><h2 id="dashboard-metrics" tabindex="-1">Dashboard &amp; metrics <a class="header-anchor" href="#dashboard-metrics" aria-label="Permalink to &quot;Dashboard &amp; metrics&quot;">​</a></h2><p>All endpoints here take a shared <code>days</code> query parameter: integer 1–90, default <code>30</code>. They also accept a shared <code>books</code> query parameter: a comma-separated list of book ids (e.g. <code>?books=3,7</code>) restricting the view to ad groups that advertise any of the selected books (union; a multi-book ad group contributes its whole numbers). Absent or empty means all products. An unknown book id fails the whole request with <code>404 NOT_FOUND</code>.</p><h3 id="get-api-dashboard-summary-days-country-books" tabindex="-1"><code>GET /api/dashboard/summary?days&amp;country&amp;books</code> <a class="header-anchor" href="#get-api-dashboard-summary-days-country-books" aria-label="Permalink to &quot;\`GET /api/dashboard/summary?days&amp;country&amp;books\`&quot;">​</a></h3><p><code>country</code> is a two-letter country code (<code>/^[A-Za-z]{2}$/</code>, upper-cased), default <code>US</code>.</p><p>Response <code>200</code> (DashboardSummary):</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>dateRange</td><td><code>{start, end}</code></td><td>ISO dates</td></tr><tr><td>currency</td><td>string</td><td></td></tr><tr><td>totals</td><td>object</td><td><code>impressions</code>, <code>clicks</code>, <code>orders</code> (ints); <code>cost</code>, <code>sales</code> (money); <code>acos</code> (number | null); <code>estimatedRoyalty</code>, <code>estimatedAdProfit</code> (money | null)</td></tr><tr><td>economicsMissing</td><td>boolean</td><td>True when no KDP economics exist for the view</td></tr><tr><td>dataCurrentThrough</td><td>timestamp</td><td></td></tr><tr><td>writesDisabled</td><td>boolean, optional</td><td>Kill switch on, or every profile read-only</td></tr><tr><td>daily</td><td>array, optional</td><td>Per-day <code>{date, cost, sales, orders, estimatedRoyalty}</code> series</td></tr></tbody></table><p>Errors: <code>409 MIXED_CURRENCY</code> when the selected window spans currencies.</p><h3 id="get-api-campaigns-days-books" tabindex="-1"><code>GET /api/campaigns?days&amp;books</code> <a class="header-anchor" href="#get-api-campaigns-days-books" aria-label="Permalink to &quot;\`GET /api/campaigns?days&amp;books\`&quot;">​</a></h3><p>Response <code>200</code>: array of campaign rows.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>profileId</td><td>string</td><td></td></tr><tr><td>campaignId</td><td>string</td><td>Amazon campaign id — the key for all campaign routes</td></tr><tr><td>name, state</td><td>string</td><td></td></tr><tr><td>totals</td><td>object</td><td><code>impressions</code>, <code>clicks</code>, <code>cost</code>, <code>sales</code>, <code>orders</code></td></tr><tr><td>amazonConsoleUrl</td><td>string | null</td><td>Campaign Manager link; null when no account id on file</td></tr><tr><td>profitability</td><td>object</td><td><code>{dateRange, currency, estimatedRoyalty, estimatedAdProfit, economicsMissing, dataCurrentThrough}</code>; money fields null when economics are missing</td></tr></tbody></table><h3 id="get-api-campaigns-id-days-books" tabindex="-1"><code>GET /api/campaigns/:id?days&amp;books</code> <a class="header-anchor" href="#get-api-campaigns-id-days-books" aria-label="Permalink to &quot;\`GET /api/campaigns/:id?days&amp;books\`&quot;">​</a></h3><p>Response <code>200</code> (CampaignDetail); <code>404 NOT_FOUND</code> when unknown.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>dateRange</td><td>object</td><td><code>{start, end}</code></td></tr><tr><td>currency</td><td>string</td><td></td></tr><tr><td>campaign</td><td>object</td><td>List row plus <code>acos</code>, <code>estimatedRoyalty</code>, <code>estimatedAdProfit</code> in <code>totals</code></td></tr><tr><td>economicsMissing</td><td>boolean</td><td></td></tr><tr><td>dataCurrentThrough</td><td>timestamp</td><td></td></tr><tr><td>daily</td><td>array</td><td>Per-day <code>{date, cost, sales, estimatedRoyalty, estimatedAdProfit}</code></td></tr><tr><td>adGroups</td><td>array</td><td><code>{id, name, state, totals}</code></td></tr><tr><td>targets</td><td>array</td><td>Same shape</td></tr><tr><td>searchTerms</td><td>array</td><td>Same shape plus <code>estimatedRoyalty</code>, <code>estimatedAdProfit</code>, <code>economicsMissing</code> per term</td></tr><tr><td>negativeKeywords</td><td>array</td><td><code>{id, keywordText, matchType, level: &quot;campaign&quot; | &quot;ad_group&quot;, adGroupId, adGroupName, state}</code></td></tr></tbody></table><hr><h2 id="search-terms" tabindex="-1">Search terms <a class="header-anchor" href="#search-terms" aria-label="Permalink to &quot;Search terms&quot;">​</a></h2><p>Cross-campaign views over shopper search terms. Royalty and profit fields are null — never guessed — when book economics are missing.</p><h3 id="get-api-search-terms-days-books" tabindex="-1"><code>GET /api/search-terms?days&amp;books</code> <a class="header-anchor" href="#get-api-search-terms-days-books" aria-label="Permalink to &quot;\`GET /api/search-terms?days&amp;books\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Param</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>days</td><td>int</td><td>1–90, default 30</td></tr><tr><td>books</td><td>string</td><td>Optional comma-separated book ids; restricts the aggregate to ad groups advertising any of them (union semantics)</td></tr></tbody></table><p>Response <code>200</code>: array of rows — <code>{searchTerm, campaignCount, countryCodes[], currency, totals (with acos), estimatedRoyalty, estimatedAdProfit, economicsMissing, dataCurrentThrough}</code>.</p><h3 id="get-api-search-terms-term-days-books-country" tabindex="-1"><code>GET /api/search-terms/:term?days&amp;books&amp;country</code> <a class="header-anchor" href="#get-api-search-terms-term-days-books-country" aria-label="Permalink to &quot;\`GET /api/search-terms/:term?days&amp;books&amp;country\`&quot;">​</a></h3><p>Per-term drill-down. <code>country</code> (two-letter code) selects the marketplace view.</p><p>Response <code>200</code> (SearchTermDetail); <code>404 NOT_FOUND</code> when the term has no data.</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>searchTerm</td><td>string</td><td></td></tr><tr><td>countryCode</td><td>string</td><td>Marketplace selected for this view</td></tr><tr><td>availableCountryCodes</td><td>string[]</td><td>Markets with data in the window (min 1)</td></tr><tr><td>dateRange, currency, totals, economicsMissing, dataCurrentThrough</td><td>—</td><td>As dashboard</td></tr><tr><td>daily</td><td>array</td><td>Per-day <code>{date, cost, sales, estimatedRoyalty, estimatedAdProfit}</code> for the trend chart, in the selected market</td></tr><tr><td>campaigns</td><td>array</td><td>Per-campaign <code>{profileId, campaignId, name, state, totals, estimatedRoyalty, estimatedAdProfit, economicsMissing}</code></td></tr></tbody></table><p>Errors: <code>409 MIXED_CURRENCY</code> when aggregating across currencies.</p><hr><h2 id="books-economics" tabindex="-1">Books &amp; economics <a class="header-anchor" href="#books-economics" aria-label="Permalink to &quot;Books &amp; economics&quot;">​</a></h2><h3 id="get-api-books" tabindex="-1"><code>GET /api/books</code> <a class="header-anchor" href="#get-api-books" aria-label="Permalink to &quot;\`GET /api/books\`&quot;">​</a></h3><p>Response <code>200</code>: array of books — <code>{id, asin, title, format, status, coverImageUrl | null, profileIds[], marketplaceAsins: [{profileId, asin}], economics: [...]}</code>. Each economics entry has <code>profileId</code>, <code>effectiveFrom</code> (date), <code>currency</code>, <code>listPrice</code>, <code>estimatedRoyaltyPerSale</code>, <code>targetAcos</code> (0–1 fraction or null), <code>goalMode</code>, <code>maxSpendWithoutSale</code>, <code>maxBid</code>, <code>maxDailyBudget</code>, <code>notes</code>.</p><h3 id="get-api-books-unmapped-products" tabindex="-1"><code>GET /api/books/unmapped-products</code> <a class="header-anchor" href="#get-api-books-unmapped-products" aria-label="Permalink to &quot;\`GET /api/books/unmapped-products\`&quot;">​</a></h3><p>Response <code>200</code>: array of advertised ASINs not yet linked to the catalog — <code>{profileId, asin, countryCode, currencyCode, adCount}</code>.</p><h3 id="post-api-books-mappings" tabindex="-1"><code>POST /api/books/mappings</code> <a class="header-anchor" href="#post-api-books-mappings" aria-label="Permalink to &quot;\`POST /api/books/mappings\`&quot;">​</a></h3><p>Confirms catalog metadata and links an advertised ASIN to one or more profiles.</p><ul><li><strong>Auth:</strong> session + CSRF. <strong>Rate:</strong> WRITE.</li><li>Response <code>201</code>: the created/updated book.</li></ul><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Constraints</th></tr></thead><tbody><tr><td>profileIds</td><td>string[]</td><td>1–100 entries, deduplicated</td></tr><tr><td>asin</td><td>string</td><td>1–64 chars (trimmed)</td></tr><tr><td>title</td><td>string</td><td>1–500 chars (trimmed)</td></tr><tr><td>format</td><td>enum</td><td><code>paperback</code>, <code>hardcover</code>, <code>kindle</code>, <code>other</code></td></tr><tr><td>coverImageUrl</td><td>string</td><td>Optional URL, max 2048 chars</td></tr></tbody></table><h3 id="post-api-books-bookid-economics" tabindex="-1"><code>POST /api/books/:bookId/economics</code> <a class="header-anchor" href="#post-api-books-bookid-economics" aria-label="Permalink to &quot;\`POST /api/books/:bookId/economics\`&quot;">​</a></h3><p>Sets user-entered KDP royalty economics (effective-dated).</p><ul><li><strong>Auth:</strong> session + CSRF.</li><li>Response <code>204</code>.</li></ul><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Constraints</th></tr></thead><tbody><tr><td>profileId</td><td>string</td><td>Profile this economics row applies to</td></tr><tr><td>effectiveFrom</td><td>date</td><td><code>YYYY-MM-DD</code></td></tr><tr><td>currency</td><td>string</td><td>ISO 4217</td></tr><tr><td>listPrice</td><td>money (≥0)</td><td></td></tr><tr><td>estimatedRoyaltyPerSale</td><td>money (≥0)</td><td>Royalty per attributed sale</td></tr><tr><td>targetAcos</td><td>number | null</td><td>0–1 fraction</td></tr><tr><td>goalMode</td><td>enum</td><td><code>profit</code>, <code>balanced</code>, <code>launch</code>, <code>visibility</code></td></tr><tr><td>maxSpendWithoutSale</td><td>money (≥0)</td><td>Optional</td></tr><tr><td>maxBid</td><td>money (≥0)</td><td>Optional</td></tr><tr><td>maxDailyBudget</td><td>money (≥0)</td><td>Optional</td></tr><tr><td>notes</td><td>string</td><td>Optional</td></tr></tbody></table><p>Errors: <code>409 BOOK_PROFILE_NOT_LINKED</code> when the book is not linked to the given profile.</p><h3 id="put-api-books-bookid-cover" tabindex="-1"><code>PUT /api/books/:bookId/cover</code> <a class="header-anchor" href="#put-api-books-bookid-cover" aria-label="Permalink to &quot;\`PUT /api/books/:bookId/cover\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF. <strong>Rate:</strong> WRITE.</li><li>Body: <code>{ &quot;coverImageUrl&quot;: &quot;https://…&quot; }</code> or <code>null</code> to clear (URL, max 2048 chars).</li><li>Response <code>204</code>.</li></ul><hr><h2 id="recommendations" tabindex="-1">Recommendations <a class="header-anchor" href="#recommendations" aria-label="Permalink to &quot;Recommendations&quot;">​</a></h2><h3 id="get-api-recommendations-type-state-books" tabindex="-1"><code>GET /api/recommendations?type&amp;state&amp;books</code> <a class="header-anchor" href="#get-api-recommendations-type-state-books" aria-label="Permalink to &quot;\`GET /api/recommendations?type&amp;state&amp;books\`&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Param</th><th>Type</th><th>Values</th></tr></thead><tbody><tr><td>type</td><td>enum</td><td><code>wasteful_search_term</code>, <code>expensive_target</code>, <code>profitable_target</code>, <code>search_term_harvest</code>, <code>budget_constrained_winner</code>, <code>high_ctr_poor_conversion</code>, <code>low_impressions</code>, <code>placement_opportunity</code>, <code>cannibalization_conflict</code></td></tr><tr><td>state</td><td>enum</td><td><code>pending</code>, <code>approved</code>, <code>rejected</code>, <code>expired</code>, <code>applied</code>, <code>protected</code></td></tr><tr><td>books</td><td>string</td><td>Optional comma-separated book ids; keeps findings whose campaign/ad group advertises any of them</td></tr></tbody></table><p>Response <code>200</code>: array of Recommendation:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>id, type, state</td><td>string / enums</td><td></td></tr><tr><td>priority</td><td>int 1–5</td><td>1 = highest impact</td></tr><tr><td>profileId, campaignId, adGroupId, targetId</td><td>string | null</td><td>Entity grain depends on the rule</td></tr><tr><td>searchTerm</td><td>string | null</td><td></td></tr><tr><td>currentValue, proposedValue</td><td>money | null</td><td>Null for diagnostics and negative keywords</td></tr><tr><td>rationale</td><td>string</td><td>Human-readable explanation</td></tr><tr><td>confidence</td><td>number 0–1</td><td></td></tr><tr><td>evidenceWindow</td><td><code>{start, end}</code></td><td>ISO dates</td></tr><tr><td>dataFreshness, expiresAt, createdAt</td><td>timestamp</td><td></td></tr><tr><td>ruleVersion</td><td>string</td><td>e.g. <code>expensive_target@1</code></td></tr></tbody></table><h3 id="get-api-recommendations-id" tabindex="-1"><code>GET /api/recommendations/:id</code> <a class="header-anchor" href="#get-api-recommendations-id" aria-label="Permalink to &quot;\`GET /api/recommendations/:id\`&quot;">​</a></h3><p>Response <code>200</code>: one Recommendation. <code>404 NOT_FOUND</code> when unknown.</p><h3 id="get-api-recommendations-id-cannibalization-context" tabindex="-1"><code>GET /api/recommendations/:id/cannibalization-context</code> <a class="header-anchor" href="#get-api-recommendations-id-cannibalization-context" aria-label="Permalink to &quot;\`GET /api/recommendations/:id/cannibalization-context\`&quot;">​</a></h3><p>Evidence for resolving one <code>cannibalization_conflict</code> finding.</p><p>Response <code>200</code>: <code>{recommendationId, profileId, searchTerm, currency, confidence, evidenceWindow, dataFreshness, expiresAt, totalSpend, campaigns}</code> where <code>campaigns</code> (min 2) holds <code>{campaignId, name, state, targetingType, spend, orders}</code> — Amazon campaign ids, never internal keys. <code>404 NOT_FOUND</code> when unknown.</p><h3 id="post-api-recommendations-id-reject" tabindex="-1"><code>POST /api/recommendations/:id/reject</code> <a class="header-anchor" href="#post-api-recommendations-id-reject" aria-label="Permalink to &quot;\`POST /api/recommendations/:id/reject\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF.</li><li>Response <code>200</code>: the Recommendation in state <code>rejected</code>. <code>404 NOT_FOUND</code> when unknown.</li></ul><h3 id="post-api-recommendations-change-sets" tabindex="-1"><code>POST /api/recommendations/change-sets</code> <a class="header-anchor" href="#post-api-recommendations-change-sets" aria-label="Permalink to &quot;\`POST /api/recommendations/change-sets\`&quot;">​</a></h3><p>Drafts one immutable change set from selected recommendations.</p><ul><li><strong>Auth:</strong> session + CSRF.</li><li>Body: <code>{ &quot;recommendationIds&quot;: [&quot;…&quot;] }</code> (array, min 1, deduplicated).</li><li>Response <code>200</code>: the created ChangeSet <code>{id, profileId, status, createdAt, kind, dependsOnChangeSetId}</code>.</li><li>Errors: <code>400 BAD_REQUEST</code> (empty list), <code>409 INVALID_STATE</code> (recommendation not pending), <code>409 RECOMMENDATION_EXPIRED</code>, <code>409 RECOMMENDATION_NOT_WRITABLE</code> (advisory-only type, or a profile whose writes are off), <code>409 TOO_MANY_ACTIONS</code> (more than 20 actions).</li></ul><p>Only three recommendation types are writable in the MVP: <code>wasteful_search_term</code> → <code>add_negative_exact</code>, <code>expensive_target</code> → <code>update_bid</code>, <code>profitable_target</code> → <code>update_bid</code>. All others are advisory and cannot enter a change set.</p><h3 id="post-api-recommendations-id-cannibalization-change-set" tabindex="-1"><code>POST /api/recommendations/:id/cannibalization-change-set</code> <a class="header-anchor" href="#post-api-recommendations-id-cannibalization-change-set" aria-label="Permalink to &quot;\`POST /api/recommendations/:id/cannibalization-change-set\`&quot;">​</a></h3><p>Resolves a cannibalization finding by drafting negative-exact keywords (or a negative ASIN target when the term is an ASIN) in the conflicting campaigns, keeping <code>destinationCampaignId</code> as the surviving target.</p><ul><li><strong>Auth:</strong> session + CSRF. <strong>Rate:</strong> WRITE.</li><li>Body: <code>{ &quot;destinationCampaignId&quot;: &quot;…&quot; }</code>.</li><li>Response <code>200</code>: the created ChangeSet.</li><li>Errors: <code>409 INVALID_RECOMMENDATION_TYPE</code> (not a cannibalization finding), plus the recommendation state errors above.</li></ul><hr><h2 id="campaign-max-cpc" tabindex="-1">Campaign Max CPC <a class="header-anchor" href="#campaign-max-cpc" aria-label="Permalink to &quot;Campaign Max CPC&quot;">​</a></h2><h3 id="get-api-campaigns-campaignid-max-cpc" tabindex="-1"><code>GET /api/campaigns/:campaignId/max-cpc</code> <a class="header-anchor" href="#get-api-campaigns-campaignid-max-cpc" aria-label="Permalink to &quot;\`GET /api/campaigns/:campaignId/max-cpc\`&quot;">​</a></h3><p>Response <code>200</code> (CampaignMaxCpc):</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>campaignId, profileId, currency</td><td>string</td><td></td></tr><tr><td>maxCpc</td><td>money (≥0) | null</td><td>Configured ceiling</td></tr><tr><td>status</td><td>enum</td><td><code>not_configured</code>, <code>pending</code>, <code>covered</code>, <code>drifted</code>, <code>unsupported</code></td></tr><tr><td>strategy</td><td>enum | null</td><td><code>LEGACY_FOR_SALES</code>, <code>AUTO_FOR_SALES</code>, <code>MANUAL</code>, <code>RULE_BASED</code></td></tr><tr><td>adjustments</td><td>array</td><td><code>{type: &quot;placement&quot; | &quot;audience&quot;, name, percentage}</code></td></tr><tr><td>activeBidRules</td><td>array</td><td><code>{id, name, category, subcategory, status}</code></td></tr><tr><td>coverageIssues</td><td>string[]</td><td>Why the campaign is not <code>covered</code></td></tr><tr><td>currentMaxBaseBid, currentMaxAdjustedBid</td><td>money | null</td><td></td></tr><tr><td>counts</td><td>object</td><td><code>{adGroups, explicitTargetBids, bidsAboveCeiling}</code></td></tr><tr><td>writeEnabled</td><td>boolean</td><td></td></tr><tr><td>sourceReadAt, enforcedAt</td><td>timestamp | null</td><td></td></tr></tbody></table><h3 id="post-api-campaigns-campaignid-max-cpc" tabindex="-1"><code>POST /api/campaigns/:campaignId/max-cpc</code> <a class="header-anchor" href="#post-api-campaigns-campaignid-max-cpc" aria-label="Permalink to &quot;\`POST /api/campaigns/:campaignId/max-cpc\`&quot;">​</a></h3><p>Drafts a change set that enforces the ceiling (clamps base bids, switches the bidding strategy to <code>LEGACY_FOR_SALES</code>, neutralizes placement/audience adjustments and bid rules).</p><ul><li><strong>Auth:</strong> session + CSRF + <strong>recent-auth</strong>. <strong>Rate:</strong> WRITE.</li><li>Body: <code>{ &quot;maxCpc&quot;: &quot;0.85&quot; }</code> — non-negative decimal, must be greater than 0.</li><li>Response <code>200</code>: <code>{changeSet, controls, actionsCreated}</code> (<code>controls</code> is the CampaignMaxCpc shape above).</li><li>Errors: <code>403 WRITES_DISABLED</code> (kill switch or read-only profile), <code>409 TOO_MANY_ACTIONS</code> (campaign has more than 5,000 bid controls), <code>401 REAUTH_REQUIRED</code>.</li></ul><hr><h2 id="campaign-state-rename" tabindex="-1">Campaign state &amp; rename <a class="header-anchor" href="#campaign-state-rename" aria-label="Permalink to &quot;Campaign state &amp; rename&quot;">​</a></h2><p>One-click guarded updates from the campaign detail page. Each call drafts a single-action <code>campaign_update</code> change set (<code>update_campaign_state</code> / <code>update_campaign_name</code>) and immediately runs the guarded apply (fresh Amazon re-read, before-state compare, guardrails, post-write verification); the verified change is written through to the local mirror. Both action types are rollbackable (the before-state is restored). There is no campaign delete: Amazon only offers terminal <code>ARCHIVED</code>, which the app deliberately does not expose.</p><h3 id="post-api-campaigns-campaignid-state" tabindex="-1"><code>POST /api/campaigns/:campaignId/state</code> <a class="header-anchor" href="#post-api-campaigns-campaignid-state" aria-label="Permalink to &quot;\`POST /api/campaigns/:campaignId/state\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF + <strong>recent-auth</strong>. <strong>Rate:</strong> WRITE.</li><li>Body: <code>{ &quot;state&quot;: &quot;enabled&quot; | &quot;paused&quot; }</code>.</li><li>Response <code>200</code>: <code>{changeSet, actions}</code> (the applied set).</li><li>Errors: <code>403 WRITES_DISABLED</code>, <code>404 NOT_FOUND</code> (unknown campaign), <code>409 STALE_BEFORE_STATE</code> (campaign state changed on Amazon since the last sync; the set is <code>blocked</code>), <code>401 REAUTH_REQUIRED</code>.</li></ul><h3 id="post-api-campaigns-campaignid-name" tabindex="-1"><code>POST /api/campaigns/:campaignId/name</code> <a class="header-anchor" href="#post-api-campaigns-campaignid-name" aria-label="Permalink to &quot;\`POST /api/campaigns/:campaignId/name\`&quot;">​</a></h3><ul><li><strong>Auth:</strong> session + CSRF + <strong>recent-auth</strong>. <strong>Rate:</strong> WRITE.</li><li>Body: <code>{ &quot;name&quot;: &quot;…&quot; }</code> — trimmed, 1–128 chars.</li><li>Response <code>200</code>: <code>{changeSet, actions}</code> (the applied set).</li><li>Errors: same as above.</li></ul><hr><h2 id="campaign-creation" tabindex="-1">Campaign creation <a class="header-anchor" href="#campaign-creation" aria-label="Permalink to &quot;Campaign creation&quot;">​</a></h2><h3 id="post-api-campaign-creation-change-sets" tabindex="-1"><code>POST /api/campaign-creation-change-sets</code> <a class="header-anchor" href="#post-api-campaign-creation-change-sets" aria-label="Permalink to &quot;\`POST /api/campaign-creation-change-sets\`&quot;">​</a></h3><p>Drafts one <code>campaign_creation</code> change set per profile, each holding an ordered <code>create_campaign</code> → <code>create_ad_group</code> → <code>create_product_ad</code> / <code>create_keyword</code> / <code>create_target</code> action chain. Nothing is created on Amazon until the sets are applied.</p><ul><li><strong>Auth:</strong> session + CSRF + <strong>recent-auth</strong>. <strong>Rate:</strong> WRITE.</li></ul><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Constraints</th></tr></thead><tbody><tr><td>profileIds</td><td>string[]</td><td>Min 1; one change set per profile</td></tr><tr><td>campaign.name</td><td>string</td><td>Min 1 char</td></tr><tr><td>campaign.dailyBudget</td><td>money (≥0)</td><td></td></tr><tr><td>campaign.targetingType</td><td>enum</td><td><code>AUTO</code>, <code>MANUAL</code></td></tr><tr><td>campaign.startDate</td><td>date</td><td><code>YYYY-MM-DD</code></td></tr><tr><td>campaign.state</td><td>enum</td><td><code>enabled</code> or <code>paused</code> (default <code>paused</code>)</td></tr><tr><td>adGroup.name</td><td>string</td><td>Min 1 char</td></tr><tr><td>adGroup.defaultBid</td><td>money (≥0)</td><td></td></tr><tr><td>bookId</td><td>string</td><td>Provides the per-marketplace ASINs for the product ads</td></tr><tr><td>keywords</td><td>array</td><td><code>{text, matchType: EXACT|PHRASE|BROAD, bid}</code>; default <code>[]</code></td></tr><tr><td>targets</td><td>array</td><td>Optional <code>{asin, bid?}</code>; ASIN matches <code>/^B0[A-Z0-9]{8}$/i</code>, applied as <code>ASIN_SAME_AS</code> product targets</td></tr><tr><td>cannibalization</td><td>object</td><td>Optional <code>{recommendationId}</code> — see below</td></tr></tbody></table><p>At least one keyword or product target is required for <code>MANUAL</code> campaigns (<code>400 VALIDATION_ERROR</code> otherwise). <code>AUTO</code> campaigns must not carry keywords or product targets at all — Amazon creates the default auto targets itself and rejects manual targeting clauses in auto campaigns (<code>400 VALIDATION_ERROR</code> when the combination is submitted).</p><p>Response <code>200</code>: <code>{ &quot;changeSets&quot;: [ChangeSet, …] }</code>.</p><p>When <code>cannibalization.recommendationId</code> is set, the API validates the finding and additionally drafts a <code>recommendation</code>-kind change set that adds the term as a campaign-level negative exact keyword (or negative ASIN target) in every conflicting campaign, with <code>dependsOnChangeSetId</code> pointing at the creation set. Applying that set before the creation set is <code>applied</code> fails with <code>409 DEPENDENCY_NOT_APPLIED</code>, so the term is never blocked everywhere at once.</p><p>Errors: <code>404 NOT_FOUND</code> (unknown book), <code>409 BOOK_PROFILE_NOT_LINKED</code> (book not linked to a requested profile), <code>403 WRITES_DISABLED</code>, <code>401 REAUTH_REQUIRED</code>.</p><hr><h2 id="change-sets-guarded-writes" tabindex="-1">Change sets &amp; guarded writes <a class="header-anchor" href="#change-sets-guarded-writes" aria-label="Permalink to &quot;Change sets &amp; guarded writes&quot;">​</a></h2><h3 id="get-api-change-sets" tabindex="-1"><code>GET /api/change-sets</code> <a class="header-anchor" href="#get-api-change-sets" aria-label="Permalink to &quot;\`GET /api/change-sets\`&quot;">​</a></h3><p>Response <code>200</code>: array of ChangeSet <code>{id, profileId, status, createdAt, kind?, dependsOnChangeSetId?}</code>. <code>status</code> is one of <code>draft</code>, <code>previewed</code>, <code>applying</code>, <code>applied</code>, <code>partially_applied</code>, <code>failed</code>, <code>blocked</code>; <code>kind</code> is one of <code>recommendation</code>, <code>max_cpc</code>, <code>rollback</code>, <code>campaign_creation</code>.</p><h3 id="get-api-change-sets-id-preview" tabindex="-1"><code>GET /api/change-sets/:id/preview</code> <a class="header-anchor" href="#get-api-change-sets-id-preview" aria-label="Permalink to &quot;\`GET /api/change-sets/:id/preview\`&quot;">​</a></h3><p>Re-evaluates guardrails without writing anything; moves <code>draft</code> sets to <code>previewed</code>.</p><ul><li><strong>Auth:</strong> session. <strong>Rate:</strong> PREVIEW.</li><li>Response <code>200</code>: <code>{changeSet, actions, guardrails}</code> where <code>guardrails</code> is an array of <code>&quot;CODE: message&quot;</code> strings (empty when clean) and each action is a ChangeAction (below).</li></ul><h3 id="post-api-change-sets-id-apply" tabindex="-1"><code>POST /api/change-sets/:id/apply</code> <a class="header-anchor" href="#post-api-change-sets-id-apply" aria-label="Permalink to &quot;\`POST /api/change-sets/:id/apply\`&quot;">​</a></h3><p>Executes the guarded write: re-reads Amazon state, compares it against the recorded before-state, re-runs guardrails, applies each action, and verifies with a post-write read.</p><ul><li><strong>Auth:</strong> session + CSRF + <strong>recent-auth</strong> (not required when retrying a <code>failed</code> set). <strong>Rate:</strong> WRITE.</li><li>Response <code>200</code>: <code>{changeSet, actions}</code> with per-action results.</li><li>Errors: <code>403 WRITES_DISABLED</code> (kill switch or read-only profile), <code>409 APPLY_IN_PROGRESS</code>, <code>409 CHANGE_SET_BLOCKED</code>, <code>409 DEPENDENCY_NOT_APPLIED</code>, <code>409 STALE_BEFORE_STATE</code>, <code>409 GUARDRAIL_VIOLATION</code>, <code>409 MAX_CPC_EXCEEDED</code>, <code>409 RECOMMENDATION_EXPIRED</code>, <code>502 AMAZON_APPLY_FAILED</code>. Individual failed actions carry <code>AMAZON_HTTP_&lt;status&gt;</code>, <code>AMAZON_RESPONSE_INVALID</code>, <code>AMAZON_NETWORK_ERROR</code>, <code>PARENT_FAILED</code>, or <code>MISSING_RESULT</code> in their result payload (see <a href="/amazon-king/reference/errors#per-action-failure-codes">Errors</a>).</li></ul><h3 id="post-api-change-actions-actionid-rollback" tabindex="-1"><code>POST /api/change-actions/:actionId/rollback</code> <a class="header-anchor" href="#post-api-change-actions-actionid-rollback" aria-label="Permalink to &quot;\`POST /api/change-actions/:actionId/rollback\`&quot;">​</a></h3><p>Creates and applies a compensating change set (<code>update_bid</code> restores the before bid; <code>add_negative_exact</code> removes the verified negative). Rollback is a compensating Amazon action, never a database undo.</p><ul><li><strong>Auth:</strong> session + CSRF + <strong>recent-auth</strong>. <strong>Rate:</strong> WRITE.</li><li>Response <code>200</code>: <code>{changeSet, actions}</code> for the rollback set; on success the original action moves to <code>rolled_back</code>.</li><li>Errors: <code>404 NOT_FOUND</code>, <code>409 NOT_ROLLBACKABLE</code> (action not <code>applied</code>, or no verified compensating operation exists — campaign-creation chains and negative ASIN targets are not rollbackable), <code>403 WRITES_DISABLED</code>.</li></ul><p>ChangeAction shape (preview/apply/rollback responses):</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td>id, changeSetId</td><td>string</td><td></td></tr><tr><td>actionType</td><td>enum</td><td><code>update_bid</code>, <code>update_ad_group_default_bid</code>, <code>update_campaign_bidding</code>, <code>update_optimization_rule</code>, <code>add_negative_exact</code>, <code>remove_negative_exact</code>, <code>create_campaign</code>, <code>create_ad_group</code>, <code>create_product_ad</code>, <code>create_keyword</code>, <code>create_target</code>, <code>add_negative_target</code></td></tr><tr><td>beforeValue, afterValue</td><td>money | null</td><td></td></tr><tr><td>entityName, searchTerm, campaignName, amazonCampaignId, beforeDetail, afterDetail</td><td>string | null</td><td>Optional context</td></tr><tr><td>rollbackAvailable</td><td>boolean</td><td>Optional</td></tr><tr><td>status</td><td>enum</td><td><code>pending</code>, <code>applied</code>, <code>partially_applied</code>, <code>failed</code>, <code>verification_failed</code>, <code>rolled_back</code></td></tr><tr><td>amazonRequestId</td><td>string | null</td><td>For Amazon support traces</td></tr><tr><td>errorMessage</td><td>string | null</td><td></td></tr></tbody></table><hr><h2 id="audit-system" tabindex="-1">Audit &amp; system <a class="header-anchor" href="#audit-system" aria-label="Permalink to &quot;Audit &amp; system&quot;">​</a></h2><h3 id="get-api-audit-events" tabindex="-1"><code>GET /api/audit-events</code> <a class="header-anchor" href="#get-api-audit-events" aria-label="Permalink to &quot;\`GET /api/audit-events\`&quot;">​</a></h3><p>Response <code>200</code>: the 100 most recent audit events — <code>{id, actor, event, entityType, entityId | null, createdAt, details}</code>.</p><h3 id="get-api-system-data-freshness" tabindex="-1"><code>GET /api/system/data-freshness</code> <a class="header-anchor" href="#get-api-system-data-freshness" aria-label="Permalink to &quot;\`GET /api/system/data-freshness\`&quot;">​</a></h3><p>Response <code>200</code>: per-profile, per-dataset freshness — <code>{profileId, dataset, lastSuccessAt | null, completeThrough | null (date)}</code>.</p><hr><h2 id="related-reading" tabindex="-1">Related reading <a class="header-anchor" href="#related-reading" aria-label="Permalink to &quot;Related reading&quot;">​</a></h2><ul><li><a href="/amazon-king/reference/errors">Errors</a> — every error code and redirect error</li><li><a href="/amazon-king/examples/api-workflows">Examples: API workflows</a> — end-to-end call sequences</li><li><a href="/amazon-king/guide/applying-changes">Applying changes</a> — the guarded write flow in the UI</li><li><a href="/amazon-king/architecture/security">Security architecture</a> — why the browser never sees Amazon tokens</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/api.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const api = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  api as default
};
