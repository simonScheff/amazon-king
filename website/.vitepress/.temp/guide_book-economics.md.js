import { ssrRenderAttrs, ssrRenderAttr } from "vue/server-renderer";
import { _ as _imports_1 } from "./settings.DppkyGHe.js";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Book Economics","description":"Why ACoS is not author profit, and how to enter per-book KDP royalty economics so amazon-king can make profit-aware recommendations.","frontmatter":{"title":"Book Economics","description":"Why ACoS is not author profit, and how to enter per-book KDP royalty economics so amazon-king can make profit-aware recommendations."},"headers":[],"relativePath":"guide/book-economics.md","filePath":"guide/book-economics.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "guide/book-economics.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="book-economics" tabindex="-1">Book Economics <a class="header-anchor" href="#book-economics" aria-label="Permalink to &quot;Book Economics&quot;">​</a></h1><p>Amazon reports ACoS — <strong>ad spend divided by ad-attributed retail revenue</strong>. That number measures the ad account&#39;s efficiency against the retail price the <em>customer</em> paid. It is not your profit. As a KDP author you earn a royalty per sale, not the list price: printing costs, Amazon&#39;s cut, and delivery fees sit between the two. A campaign at 30% ACoS can be profitable for one book and a loss-maker for another, depending on royalty.</p><p>amazon-king therefore keeps two layers strictly separate:</p><ul><li><strong>ACoS-based recommendations</strong> work from imported ad data alone.</li><li><strong>Profit-aware recommendations</strong> require economics you enter yourself. The optimizer never guesses royalties — when economics are missing, profit recommendations are suppressed entirely.</li></ul><p><img${ssrRenderAttr("src", _imports_1)} alt="Settings page with book economics and advertised ASIN identification"></p><h2 id="identifying-your-books" tabindex="-1">Identifying your books <a class="header-anchor" href="#identifying-your-books" aria-label="Permalink to &quot;Identifying your books&quot;">​</a></h2><p>The ad account knows ASINs, not titles. After the first sync, open <strong>Settings → New advertised ASINs to identify</strong>. Each advertised ASIN that is not yet linked to your catalog appears there; identify it by confirming:</p><ul><li>the <strong>title</strong>,</li><li>the <strong>format</strong> (<code>paperback</code>, <code>hardcover</code>, <code>kindle</code>, or <code>other</code>),</li><li>the <strong>profiles</strong> (markets) where this ASIN is yours, and</li><li>optionally a <strong>cover image URL</strong>.</li></ul><p>Submitting posts to <code>POST /api/books/mappings</code> and links the ASIN to a book in your workspace catalog, so dashboards and rules can attribute ad sales to a real book.</p><h2 id="entering-economics" tabindex="-1">Entering economics <a class="header-anchor" href="#entering-economics" aria-label="Permalink to &quot;Entering economics&quot;">​</a></h2><p>Economics are entered per <strong>book per market</strong> (profile) on the Settings page. Each entry carries:</p><table tabindex="0"><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody><tr><td><code>listPrice</code></td><td>Retail list price in the market&#39;s currency.</td></tr><tr><td><code>estimatedRoyaltyPerSale</code></td><td>Your net royalty per sale — the amount that actually reaches you. This is the number profit math runs on.</td></tr><tr><td><code>effectiveFrom</code></td><td>The date these economics apply from (see below).</td></tr><tr><td><code>targetAcos</code></td><td>Optional target ACoS as a 0–1 fraction (e.g. <code>0.30</code>). Drives bid proposals.</td></tr><tr><td><code>goalMode</code></td><td><code>profit</code>, <code>balanced</code>, <code>launch</code>, or <code>visibility</code> (see below).</td></tr><tr><td><code>maxSpendWithoutSale</code></td><td>Optional cap on spend allowed before a sale.</td></tr><tr><td><code>maxBid</code> / <code>maxDailyBudget</code></td><td>Optional hard ceilings used as guardrail inputs.</td></tr><tr><td><code>notes</code></td><td>Free text, kept with the entry.</td></tr></tbody></table><p>Money fields are decimal strings in the profile&#39;s currency; amazon-king never aggregates across currencies.</p><h3 id="effective-dated-history" tabindex="-1">Effective-dated history <a class="header-anchor" href="#effective-dated-history" aria-label="Permalink to &quot;Effective-dated history&quot;">​</a></h3><p>Economics are <strong>effective-dated</strong>, not overwritten. Rows are unique per (book, profile, <code>effective_from</code>), and the optimizer uses the latest row whose <code>effective_from</code> is on or before today. When a launch price ends or a royalty changes, add a new row with a new effective date instead of editing the old one — historical recommendations stay reproducible against the economics that were in force at the time.</p><h2 id="what-changes-when-economics-are-present" tabindex="-1">What changes when economics are present <a class="header-anchor" href="#what-changes-when-economics-are-present" aria-label="Permalink to &quot;What changes when economics are present&quot;">​</a></h2><p>With at least one effective economics row for a market:</p><ul><li>The dashboard shows the <strong>Est. royalty</strong> and <strong>Est. ad profit</strong> KPIs and the <strong>Daily profitability</strong> chart. Without economics these stay hidden and the dashboard says so explicitly.</li><li>The profit-aware rules activate: <code>expensive_target</code> (cut bids on unprofitable targets), <code>profitable_target</code> (raise bids on profitable ones), <code>budget_constrained_winner</code> (flag winners starved by budget), and <code>placement_opportunity</code> (placement-level profit signals).</li><li><code>targetAcos</code> and royalty feed the bid proposal formula, so suggested bids aim at <em>your</em> break-even point rather than retail ACoS.</li></ul><p>Without economics, every profit rule stays silent and the system degrades to ACoS-only advice. Per-rule thresholds are listed in <a href="/amazon-king/reference/optimization-rules">Optimization rules</a>.</p><h2 id="goal-modes" tabindex="-1">Goal modes <a class="header-anchor" href="#goal-modes" aria-label="Permalink to &quot;Goal modes&quot;">​</a></h2><p>The goal mode tunes how aggressive the rules may be for a book:</p><ul><li><code>profit</code> — optimize strictly for royalty profit.</li><li><code>balanced</code> — the default; profit-aware but tolerant of moderate spend.</li><li><code>launch</code> — discovery mode for new releases. In launch mode the <code>expensive_target</code> and <code>wasteful_search_term</code> rules are suppressed: the system will not down-bid or negative-keyword a book you are deliberately spending to explore.</li><li><code>visibility</code> — maximize exposure while staying inside your caps.</li></ul><p>Goal mode is per book per market, so a mature backlist title can run on <code>profit</code> while a new release runs on <code>launch</code> in the same account.</p><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><p>With economics in place, recommendations become profit-aware — see <a href="/amazon-king/guide/recommendations">Reviewing recommendations</a>, and <a href="/amazon-king/guide/applying-changes">Applying &amp; rolling back changes</a> for what happens when you approve one.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/book-economics.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const bookEconomics = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  bookEconomics as default
};
