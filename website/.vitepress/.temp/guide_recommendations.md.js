import { ssrRenderAttrs, ssrRenderAttr } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/amazon-king/screenshots/recommendations.png";
const _imports_1 = "/amazon-king/screenshots/recommendation-detail.png";
const __pageData = JSON.parse(`{"title":"Reviewing Recommendations","description":"How amazon-king's deterministic rules produce recommendations, how to read the evidence and confidence, and which findings are actionable.","frontmatter":{"title":"Reviewing Recommendations","description":"How amazon-king's deterministic rules produce recommendations, how to read the evidence and confidence, and which findings are actionable."},"headers":[],"relativePath":"guide/recommendations.md","filePath":"guide/recommendations.md","lastUpdated":1787237726000}`);
const _sfc_main = { name: "guide/recommendations.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="reviewing-recommendations" tabindex="-1">Reviewing Recommendations <a class="header-anchor" href="#reviewing-recommendations" aria-label="Permalink to &quot;Reviewing Recommendations&quot;">​</a></h1><p>Recommendations are the product&#39;s core output: deterministic, evidence-backed findings that a human reviews before anything touches Amazon. Nothing here is generated by an LLM, and nothing applies itself.</p><p><img${ssrRenderAttr("src", _imports_0)} alt="Recommendations list with filters and evidence columns"></p><h2 id="how-recommendations-are-produced" tabindex="-1">How recommendations are produced <a class="header-anchor" href="#how-recommendations-are-produced" aria-label="Permalink to &quot;How recommendations are produced&quot;">​</a></h2><p>The worker runs a <code>recommendation_run</code> job after each successful metrics sync. A run only proceeds when the profile has a <strong>complete metrics sync fresher than 48 hours</strong> — otherwise it skips, and stale data never produces new advice.</p><p>Each run:</p><ol><li>Loads the profile&#39;s structure, its synced negative keywords and negative ASIN targets, daily metrics, your <a href="/amazon-king/guide/book-economics">book economics</a>, and recent applied changes (cooldowns).</li><li>Evaluates all <strong>nine versioned rules</strong> over four evidence windows — 7, 14, 30, and 60 days.</li><li><strong>Deduplicates across windows</strong>: the same rule firing on the same entity keeps only the highest-impact draft.</li><li>Skips any identity that already has a <strong>pending</strong> recommendation, so the queue never piles up duplicates of a finding you have not reviewed yet.</li><li>Skips any identity you <strong>rejected</strong>, for 60 days after the rejection.</li><li>Expires stale pending recommendations first: each recommendation is created with <code>expires_at</code> three days out and transitions <code>pending → expired</code> once stale. A pending or approved finding is also expired as soon as a synced negative resolves it, rather than waiting out those three days: <code>cannibalization_conflict</code> when the term is blocked in enough campaigns, <code>wasteful_search_term</code> when this campaign can no longer serve the term, and <code>high_ctr_poor_conversion</code> when the remaining unblocked traffic would not trigger the rule.</li></ol><p>Every stored recommendation records its rule version, exact evidence inputs, evidence window, and data-freshness timestamp, so any finding can be traced back to the numbers that produced it.</p><h3 id="confidence" tabindex="-1">Confidence <a class="header-anchor" href="#confidence" aria-label="Permalink to &quot;Confidence&quot;">​</a></h3><p>Confidence is a simple evidence-volume score:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>confidence = min(1, observed / (2 × required evidence))</span></span></code></pre></div><p>A finding exactly at its evidence threshold scores 0.5; double the required evidence saturates at 1.0. It is not a probability of being right — it tells you how much data the rule had. Thresholds and required evidence per rule are in <a href="/amazon-king/reference/optimization-rules">Optimization rules</a>.</p><h2 id="the-recommendations-ui" tabindex="-1">The recommendations UI <a class="header-anchor" href="#the-recommendations-ui" aria-label="Permalink to &quot;The recommendations UI&quot;">​</a></h2><p><code>/recommendations</code> lists findings with filters by <strong>type</strong> and <strong>state</strong>, and a table with six columns: <strong>Priority</strong> (1–5), <strong>Finding</strong>, <strong>State</strong>, <strong>Evidence</strong> (window), <strong>Created</strong> (date and time the finding was stored), and <strong>Confidence</strong>. The overview&#39;s pending list shows the same created timestamp on each card.</p><p>States are <code>pending</code>, <code>approved</code>, <code>rejected</code>, <code>expired</code>, <code>applied</code>, and <code>protected</code>.</p><h2 id="the-detail-page" tabindex="-1">The detail page <a class="header-anchor" href="#the-detail-page" aria-label="Permalink to &quot;The detail page&quot;">​</a></h2><p><img${ssrRenderAttr("src", _imports_1)} alt="Recommendation detail with current-vs-proposed comparison and evidence"></p><p>Opening a finding shows everything needed to decide:</p><ul><li><strong>What was found</strong> — the finding text plus the entity it targets (campaign, ad group, target, search term).</li><li><strong>Action summary</strong> — for actionable types, the current value next to the proposed value with a comparison bar chart.</li><li><strong>What happens now</strong> / <strong>Next step</strong> — exactly what approving will do.</li><li><strong>This will not</strong> — an explicit exclusion list (for example, a bid change will not pause the target or touch other keywords).</li><li><strong>Evidence &amp; guardrails</strong> — the evidence window, data freshness, expiry, and creation timestamps, plus the guardrail posture.</li></ul><h2 id="actionable-vs-review-only-types" tabindex="-1">Actionable vs. review-only types <a class="header-anchor" href="#actionable-vs-review-only-types" aria-label="Permalink to &quot;Actionable vs. review-only types&quot;">​</a></h2><p>Only three of the nine types can enter a change set. The mapping is fixed in the shared contracts package so the UI and the guarded-write service cannot drift apart:</p><table tabindex="0"><thead><tr><th>Type</th><th>Change action</th></tr></thead><tbody><tr><td><code>wasteful_search_term</code></td><td><code>add_negative_exact</code></td></tr><tr><td><code>expensive_target</code></td><td><code>update_bid</code></td></tr><tr><td><code>profitable_target</code></td><td><code>update_bid</code></td></tr><tr><td><code>search_term_harvest</code></td><td>review-only</td></tr><tr><td><code>budget_constrained_winner</code></td><td>review-only</td></tr><tr><td><code>high_ctr_poor_conversion</code></td><td>review-only (resolved via a dedicated flow)</td></tr><tr><td><code>low_impressions</code></td><td>review-only</td></tr><tr><td><code>placement_opportunity</code></td><td>review-only</td></tr><tr><td><code>cannibalization_conflict</code></td><td>review-only (resolved via a dedicated flow)</td></tr></tbody></table><p>Review-only findings are diagnostics. They carry the same evidence but no Amazon write; the detail page says &quot;Review-only finding — no change set can be created&quot; and offers <strong>Dismiss finding</strong>. Two types have no single write but several sensible responses, so they open their own resolution screen instead: <code>cannibalization_conflict</code> and <code>high_ctr_poor_conversion</code>.</p><p>Every finding names its campaign and links to it. The <code>campaign</code> object on the payload carries the Amazon campaign id, name, and state; the numeric <code>campaignId</code> beside it is an internal database row id and addresses nothing in Amazon.</p><h2 id="approving-rejecting-dismissing" tabindex="-1">Approving, rejecting, dismissing <a class="header-anchor" href="#approving-rejecting-dismissing" aria-label="Permalink to &quot;Approving, rejecting, dismissing&quot;">​</a></h2><ul><li><strong>Approve (create change set)</strong> — approval <em>is</em> creating a draft change set; there is no intermediate &quot;approved but not drafted&quot; limbo. The finding moves to <code>approved</code> and the new draft waits in the <a href="/amazon-king/guide/applying-changes">Change center</a>. Nothing has been sent to Amazon at this point.</li><li><strong>Reject</strong> — the finding moves to <code>rejected</code>; no change set is created. The finding is also recorded as dismissed, so later runs stay quiet about that exact identity for 60 days (the longest evidence window) instead of raising it again from the same metrics.</li><li><strong>Dismiss finding</strong> — the review-only equivalent of reject.</li><li><strong>Snooze</strong> — the same dismissal with a shorter window. The conversion resolution screen uses 30 days, so a finding you are actively fixing comes back to confirm whether the fix worked.</li></ul><p>All transitions are audited.</p><h2 id="protected-entities" tabindex="-1">Protected entities <a class="header-anchor" href="#protected-entities" aria-label="Permalink to &quot;Protected entities&quot;">​</a></h2><p>The data model reserves a <code>protected</code> recommendation state and the detail page shows a <strong>Protect entity</strong> button, but the protection API does not exist yet — the button is disabled, and no <code>protected_entities</code> table is implemented. Treat protection as a planned feature, not a safety mechanism: today, nothing is implicitly protected, and guardrails are what stand between a recommendation and a write.</p><h2 id="clicked-but-not-bought" tabindex="-1">Clicked, but not bought <a class="header-anchor" href="#clicked-but-not-bought" aria-label="Permalink to &quot;Clicked, but not bought&quot;">​</a></h2><p>A <code>high_ctr_poor_conversion</code> finding means shoppers click the ad and then do not buy: the cover, title, and price in the search results are working, and the product page is not. No bid change fixes that, and the Ads API cannot touch a KDP listing, so the detail page opens a resolution screen instead of a dead end.</p><p>It names the campaign, links to it in the app and in the Amazon Ads console, links to the book&#39;s retail listing, and offers four responses:</p><ul><li><strong>Fix the listing and keep running</strong> — a checklist (cover at thumbnail size, price against comparable titles, title and blurb, Look Inside, reviews, targeting fit) with &quot;remind me in 30 days&quot;.</li><li><strong>Block the terms that never order</strong> — the campaign&#39;s zero-order shopper terms that are not already a negative, with their clicks and spend; the chosen ones become a draft change set of campaign-level negatives. Exact negatives only hide the identical query; a phrase negative hides any query that contains those words in order. Historical clicks stay in the evidence window after you apply a negative, but those terms are not offered again.</li><li><strong>Cap what one click may cost</strong> — the same <a href="/amazon-king/guide/campaign-tools#max-cpc-ceiling">Max CPC ceiling</a> as the campaign page, prefilled below the observed average CPC. The suggestion is a cut below what clicks cost today, not a break-even bid: break-even needs a conversion rate, which is exactly what this finding says is missing.</li><li><strong>Pause the campaign</strong> — stops the spend after a confirmation, and is the only one of the four that writes to Amazon immediately.</li></ul><h2 id="cannibalization-conflicts" tabindex="-1">Cannibalization conflicts <a class="header-anchor" href="#cannibalization-conflicts" aria-label="Permalink to &quot;Cannibalization conflicts&quot;">​</a></h2><p>A <code>cannibalization_conflict</code> finding means the same search term is spending in multiple campaigns at once. It is not resolved with a simple bid or negative — the detail page embeds a dedicated resolution screen where you pick the campaign that should keep the term (or create a new one) and draft negatives everywhere else under a dependency lock. See <a href="/amazon-king/guide/campaign-tools#resolving-cannibalization">Campaign tools</a>.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/recommendations.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const recommendations = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  recommendations as default
};
