import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Architecture overview","description":"How amazon-king is structured as a modular monolith — web dashboard, Fastify API, PostgreSQL job worker, and shared packages — plus its runtime lifecycle and deployment shape.","frontmatter":{"title":"Architecture overview","description":"How amazon-king is structured as a modular monolith — web dashboard, Fastify API, PostgreSQL job worker, and shared packages — plus its runtime lifecycle and deployment shape."},"headers":[],"relativePath":"architecture/overview.md","filePath":"architecture/overview.md","lastUpdated":1787088281000}');
const _sfc_main = { name: "architecture/overview.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="architecture-overview" tabindex="-1">Architecture overview <a class="header-anchor" href="#architecture-overview" aria-label="Permalink to &quot;Architecture overview&quot;">​</a></h1><p>amazon-king is a <strong>modular monolith</strong> in a pnpm monorepo: three deployable processes (web, api, worker) and six shared packages, all TypeScript, all talking to one PostgreSQL database. The web server and worker run as separate processes but ship as one product. The authoritative specification is <code>docs/plan.md</code> in the repository root.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>apps/</span></span>
<span class="line"><span>  web/       dashboard (React + Vite)</span></span>
<span class="line"><span>  api/       browser-facing backend + OAuth callback (Fastify)</span></span>
<span class="line"><span>  worker/    imports, reports, analysis, scheduled jobs</span></span>
<span class="line"><span>packages/</span></span>
<span class="line"><span>  contracts/       shared zod request/response schemas</span></span>
<span class="line"><span>  amazon-ads/      OAuth client, token manager, regional gateway, API adapters</span></span>
<span class="line"><span>  database/        SQL migrations, repositories, PostgreSQL job queue</span></span>
<span class="line"><span>  optimizer/       pure, deterministic recommendation rules</span></span>
<span class="line"><span>  crypto/          AES-256-GCM envelope encryption for refresh tokens</span></span>
<span class="line"><span>  observability/   structured logging with secret redaction</span></span></code></pre></div><h2 id="component-diagram" tabindex="-1">Component diagram <a class="header-anchor" href="#component-diagram" aria-label="Permalink to &quot;Component diagram&quot;">​</a></h2>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-10",
        class: "mermaid",
        graph: "flowchart%20LR%0A%20%20%20%20subgraph%20browser%5B%22Browser%22%5D%0A%20%20%20%20%20%20%20%20UI%5B%22React%20dashboard%22%5D%0A%20%20%20%20end%0A%0A%20%20%20%20subgraph%20host%5B%22Deployment%22%5D%0A%20%20%20%20%20%20%20%20WEB%5B%22apps%2Fweb%3Cbr%2F%3Estatic%20SPA%20%2B%20%2Fapi%20proxy%22%5D%0A%20%20%20%20%20%20%20%20API%5B%22apps%2Fapi%3Cbr%2F%3EFastify%205%22%5D%0A%20%20%20%20%20%20%20%20WORKER%5B%22apps%2Fworker%3Cbr%2F%3Ejob%20loop%22%5D%0A%20%20%20%20%20%20%20%20DB%5B(%22PostgreSQL%3Cbr%2F%3E%2B%20job_queue%22)%5D%0A%20%20%20%20%20%20%20%20REPORTS%5B%2F%22Report%20artifacts%3Cbr%2F%3E(.json.gz%20%2B%20sha256)%22%2F%5D%0A%20%20%20%20end%0A%0A%20%20%20%20AMAZON%5B%22Amazon%20Ads%20API%3Cbr%2F%3E(3%20regional%20hosts)%22%5D%0A%0A%20%20%20%20UI%20--%3E%7C%22HTTPS%2C%20%2Fapi%20only%22%7C%20WEB%0A%20%20%20%20WEB%20--%3E%20API%0A%20%20%20%20API%20--%3E%20DB%0A%20%20%20%20WORKER%20--%3E%20DB%0A%20%20%20%20WORKER%20--%3E%20REPORTS%0A%20%20%20%20WORKER%20--%3E%20GW%5B%22packages%2Famazon-ads%3Cbr%2F%3EAmazonAdsGateway%22%5D%0A%20%20%20%20API%20--%3E%20GW%0A%20%20%20%20GW%20--%3E%7C%22OAuth%20%2B%20Reporting%20v3%20%2B%20SP%20v3%22%7C%20AMAZON%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>The browser talks only to <code>apps/web</code> (which proxies <code>/api</code> to <code>apps/api</code>). It never holds an Amazon token and never calls the Amazon Ads API; all Amazon traffic goes through the gateway behind the API or the worker.</p><h2 id="the-three-apps" tabindex="-1">The three apps <a class="header-anchor" href="#the-three-apps" aria-label="Permalink to &quot;The three apps&quot;">​</a></h2><h3 id="apps-web-—-dashboard" tabindex="-1"><code>apps/web</code> — dashboard <a class="header-anchor" href="#apps-web-—-dashboard" aria-label="Permalink to &quot;\`apps/web\` — dashboard&quot;">​</a></h3><p>React 19 + Vite + TypeScript, TanStack Router (code-based routes) and TanStack Query, Tailwind CSS v4, Recharts. It implements every screen in the plan §12 (dashboard, campaigns, search terms, books, recommendations, change center, settings). All API responses are validated at the fetch boundary with the zod schemas from <code>@amazon-king/contracts</code>, so a shape mismatch fails loudly in the UI instead of corrupting state. A network-only service worker (<code>apps/web/public/sw.js</code>) lets an HTTPS deployment be installed as a standalone app without ever caching <code>/api</code> responses. Phones must use that installed app: an install gate replaces the app routes with install instructions in a phone browser tab, leaving only the sign-in screen reachable there.</p><h3 id="apps-api-—-browser-facing-backend" tabindex="-1"><code>apps/api</code> — browser-facing backend <a class="header-anchor" href="#apps-api-—-browser-facing-backend" aria-label="Permalink to &quot;\`apps/api\` — browser-facing backend&quot;">​</a></h3><p>Fastify 5 (<code>apps/api/src/server.ts</code>) with cookie, CORS, helmet, and rate-limit plugins. Responsibilities:</p><ul><li><strong>Login A sessions</strong> — passwordless email sign-in, <code>ak_session</code> cookie, stateless HMAC CSRF tokens (see <a href="/amazon-king/architecture/security">Security model</a>).</li><li><strong>Login B OAuth</strong> — the Amazon consent redirect and callback; the refresh token is envelope-encrypted before it touches the database.</li><li><strong>Read API</strong> — dashboard summary, campaigns, search terms, books, recommendations, audit events, all scoped to the caller&#39;s workspace.</li><li><strong>Guarded writes</strong> — change-set create/preview/apply/rollback and campaign creation in <code>apps/api/src/services/changes.ts</code>: fingerprint-idempotent creation, live before-state re-read, guardrail re-check, per-item apply, post-write verification.</li><li><strong>Rate limits and recent-auth gates</strong> — see <a href="/amazon-king/architecture/security#rate-limits">Security model</a>.</li></ul><p>Route handlers are thin wrappers over injectable services; tests run against an in-memory fake database, no network.</p><h3 id="apps-worker-—-background-pipeline" tabindex="-1"><code>apps/worker</code> — background pipeline <a class="header-anchor" href="#apps-worker-—-background-pipeline" aria-label="Permalink to &quot;\`apps/worker\` — background pipeline&quot;">​</a></h3><p>A poll-claim-execute loop (<code>apps/worker/src/loop.ts</code>) over the PostgreSQL job queue. One job at a time per worker process: a job is claimed with <code>FOR UPDATE SKIP LOCKED</code>, given a 120-second lease (default <code>WORKER_LEASE_SECONDS</code>) that is heartbeated every 30 seconds (<code>WORKER_HEARTBEAT_MS</code>), and returned to <code>pending</code> by lease reaping every 60 seconds (<code>WORKER_REAP_INTERVAL_MS</code>) if the worker dies mid-job. SIGTERM/SIGINT stop the loop after the in-flight job finishes — a graceful shutdown never strands a claimed job.</p><p>Job handlers live in <code>apps/worker/src/jobs/</code>: <code>profile_discovery</code>, <code>structure_sync</code>, <code>metrics_sync</code>, <code>recent_window_resync</code>, <code>recommendation_run</code>, <code>connection_health</code>, and the self-rescheduling <code>schedule_tick</code>. Handlers are read-only against Amazon in the MVP and depend on injected store/gateway/storage/clock interfaces — they never read the wall clock directly. See <a href="/amazon-king/architecture/data-pipeline">Data pipeline</a> for the full flows.</p><h2 id="the-shared-packages" tabindex="-1">The shared packages <a class="header-anchor" href="#the-shared-packages" aria-label="Permalink to &quot;The shared packages&quot;">​</a></h2><ul><li><strong><code>packages/contracts</code></strong> — zod schemas for every API request and response; the single source of truth shared by the web app&#39;s fetch boundary and the API&#39;s validators.</li><li><strong><code>packages/amazon-ads</code></strong> — the only code that talks to Amazon: LWA OAuth client, <code>TokenManager</code> (serialized per-connection refresh, 5-minute early skew, circuit breaker), a regional transport that honors <code>Retry-After</code> with full-jitter backoff, the <code>AmazonAdsGateway</code> interface, and the Reporting v3 plus Sponsored Products v3 adapters. Every Amazon payload is strictly validated with zod at the boundary and translated to internal domain models.</li><li><strong><code>packages/database</code></strong> — plain SQL migrations under <code>migrations/</code>, a thin <code>pg</code> pool wrapper, explicit repository modules (parameterized SQL only), and the <code>FOR UPDATE SKIP LOCKED</code> job queue (<code>packages/database/src/queue.ts</code>).</li><li><strong><code>packages/optimizer</code></strong> — pure and deterministic: no I/O, no wall clock (time is injected). The nine §9 rules, guardrails, smoothed conversion rates, ±15% bid clamp, and ranking. Money is integer micro-units internally with decimal strings at the boundaries.</li><li><strong><code>packages/crypto</code></strong> — AES-256-GCM envelope encryption for Amazon refresh tokens with versioned keys; ciphertext embeds the key version.</li><li><strong><code>packages/observability</code></strong> — pino loggers with a built-in redaction list (tokens, codes, secrets, pre-signed URLs) and <code>redactSecrets</code> for unknown error payloads.</li></ul><p>Dependency direction is one-way: apps depend on packages, packages never on apps, and <code>optimizer</code> depends on nothing with side effects.</p><h2 id="runtime-lifecycle" tabindex="-1">Runtime lifecycle <a class="header-anchor" href="#runtime-lifecycle" aria-label="Permalink to &quot;Runtime lifecycle&quot;">​</a></h2><p>Recurring work is driven entirely by the job queue — no cron, no external scheduler. On boot the worker enqueues a <code>schedule_tick</code> job if none is pending (<code>apps/worker/src/index.ts</code>). Each tick enqueues whatever is due, records the timestamps in its own payload (so they survive restarts), and re-enqueues itself 15 minutes out (<code>SCHEDULE_TICK_MS</code>), guarded by <code>enqueueIfNotQueued</code> so a retried tick never duplicates work.</p><p>Cadence, from <code>apps/worker/src/jobs/schedule-tick.ts</code>:</p><table tabindex="0"><thead><tr><th>Job</th><th>Cadence</th></tr></thead><tbody><tr><td><code>profile_discovery</code></td><td>Every 24 h</td></tr><tr><td><code>connection_health</code></td><td>Every 4 h</td></tr><tr><td><code>structure_sync</code></td><td>Every 45 min per enabled profile</td></tr><tr><td><code>metrics_sync</code></td><td>Once daily per enabled profile, after 05:00 UTC (yesterday)</td></tr><tr><td><code>recent_window_resync</code></td><td>Once daily per enabled profile, trailing 14 days</td></tr><tr><td><code>recommendation_run</code></td><td>Chained by <code>metrics_sync</code> after a complete import</td></tr></tbody></table><p>Two deliberate design points:</p><ul><li><code>recommendation_run</code> is <strong>never scheduled directly</strong>. It runs only after a metrics sync in which every report family completed, so recommendations are never generated from a partial dataset. A 48-hour freshness gate (<code>RECOMMENDATION_FRESHNESS_HOURS</code>) skips the run when data is stale.</li><li><code>recent_window_resync</code> exists because Amazon attribution lags: conversions keep landing on recent days after the first import, so the trailing 14-day window (<code>RECENT_WINDOW_DAYS</code>) is re-imported daily and idempotent upserts correct the fact rows.</li></ul><h2 id="design-rules" tabindex="-1">Design rules <a class="header-anchor" href="#design-rules" aria-label="Permalink to &quot;Design rules&quot;">​</a></h2><p>These are binding constraints from <code>docs/plan.md</code>; the codebase enforces them mechanically, not by convention.</p><ul><li><strong>Two separate logins.</strong> App sign-in (Login A, passwordless email) and the Amazon OAuth connection (Login B) are fully independent. The app session never contains an Amazon token.</li><li><strong>Browser isolation.</strong> The browser never receives the LWA client secret, an access token, or a refresh token, and never calls the Amazon Ads API directly. All Amazon traffic goes through the backend gateway.</li><li><strong>Gateway boundary.</strong> Every Amazon payload is strictly validated and translated to internal models inside <code>packages/amazon-ads</code>. The optimizer never sees raw Amazon field names.</li><li><strong>Idempotent pipeline.</strong> Reports are asynchronous (request → poll → download → validate → reconcile → batch upsert with <code>INSERT ... ON CONFLICT DO UPDATE</code>), deduped by deterministic spec fingerprints, so re-running any step converges instead of duplicating.</li><li><strong>Deterministic optimizer.</strong> Rules only — no LLM decisions. Every rule is versioned, stores its exact inputs as immutable evidence, requires minimum evidence, uses smoothed conversion rates, clamps bid changes to ±15% per cooldown period, and expires when data goes stale. Profit rules are suppressed (never guessed) when the owner has not entered KDP economics.</li><li><strong>Guarded writes.</strong> Read-only is the default per profile. Applying a change requires an immutable change set, a fresh re-read of Amazon state matching the approved before snapshot, guardrail re-checks, an idempotency fingerprint, per-item result handling, post-write re-read verification, and an audit log entry. A global kill switch disables all writes immediately and fails closed.</li></ul><h2 id="production-deployment-shape" tabindex="-1">Production deployment shape <a class="header-anchor" href="#production-deployment-shape" aria-label="Permalink to &quot;Production deployment shape&quot;">​</a></h2><p><code>compose.production.yml</code> runs five services on one host:</p><ul><li><strong>db</strong> — <code>postgres:16-alpine</code> with a persistent volume and healthcheck.</li><li><strong>migrate</strong> — one-shot container that applies the SQL migrations (<code>scripts/migrate.ts</code>) before anything else starts; api and worker wait on its successful completion.</li><li><strong>api</strong> — Fastify on port 3000 (container-internal), <code>TRUST_PROXY=true</code> behind the reverse proxy.</li><li><strong>worker</strong> — the job loop; report artifacts stream to the <code>report-data</code> volume mounted at <code>/app/.data/reports</code> (<code>REPORT_STORAGE_DIR</code>), stored gzipped with a sha256 checksum per artifact.</li><li><strong>web</strong> — serves the built SPA and proxies <code>/api</code>; the only published port, bound to <code>\${APP_BIND_ADDRESS:-127.0.0.1}:\${APP_PORT:-8080}</code>. It is loopback by default because a TLS-terminating reverse proxy in front (see <code>deploy/nginx.conf</code>) is the expected public entry point.</li></ul><p>Secrets come from the environment file only — no secrets in images, none in the repository. See <a href="/amazon-king/guide/self-hosting">Self-hosting</a> for the full setup and <a href="/amazon-king/guide/operations">Operations</a> for day-2 runbooks.</p><h2 id="where-to-go-next" tabindex="-1">Where to go next <a class="header-anchor" href="#where-to-go-next" aria-label="Permalink to &quot;Where to go next&quot;">​</a></h2><ul><li><a href="/amazon-king/architecture/data-pipeline">Data pipeline</a> — sync flows, the job queue, and the recommendation run in detail.</li><li><a href="/amazon-king/architecture/security">Security model</a> — the two logins, CSRF, rate limits, and guarded writes.</li><li><a href="/amazon-king/architecture/data-model">Data model</a> — the PostgreSQL schema, table by table.</li><li><a href="/amazon-king/guide/key-concepts">Key concepts</a> — the product-level vocabulary these components implement.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture/overview.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const overview = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  overview as default
};
