import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Operations Runbook","description":"Backups, monitoring, incident response, and data-deletion duties for operating a self-hosted amazon-king instance.","frontmatter":{"title":"Operations Runbook","description":"Backups, monitoring, incident response, and data-deletion duties for operating a self-hosted amazon-king instance."},"headers":[],"relativePath":"guide/operations.md","filePath":"guide/operations.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "guide/operations.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="operations-runbook" tabindex="-1">Operations Runbook <a class="header-anchor" href="#operations-runbook" aria-label="Permalink to &quot;Operations Runbook&quot;">​</a></h1><p>amazon-king stores application state in PostgreSQL and compressed Amazon report artifacts in the <code>report-data</code> Docker volume. Both may contain sensitive advertiser data, and both are the operator&#39;s responsibility.</p><h2 id="backups" tabindex="-1">Backups <a class="header-anchor" href="#backups" aria-label="Permalink to &quot;Backups&quot;">​</a></h2><p>Create encrypted, off-host PostgreSQL backups on a schedule, and test restoration into an isolated, disposable environment. A logical backup:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --env-file</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .env.production</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose.production.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -T</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> db</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> pg_dump</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -U</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> amazon_king</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> amazon_king</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -Fc</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> &gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> amazon-king.dump</span></span></code></pre></div><p>Protect the resulting file immediately — it contains application and advertiser data.</p><p>Back up the <code>report-data</code> volume with a tool that preserves ownership and encrypts data before it leaves the host.</p><div class="danger custom-block"><p class="custom-block-title">DANGER</p><p>A database backup without the token encryption keys cannot recover Amazon connections. Back the keys up <strong>separately</strong> in a secret manager — never beside the database backups, or one theft defeats both layers.</p></div><p>The repository does not yet automate point-in-time recovery or raw-report retention. Configure these controls in your own infrastructure and document your chosen retention periods before production use.</p><h2 id="monitoring" tabindex="-1">Monitoring <a class="header-anchor" href="#monitoring" aria-label="Permalink to &quot;Monitoring&quot;">​</a></h2><p>At minimum, alert on:</p><ul><li>API or worker restarts</li><li>Amazon connection refresh failures (<code>reconnect_required</code> transitions)</li><li>dead queue jobs</li><li>stale syncs (a profile whose last complete metrics sync is aging out)</li><li>report reconciliation failures</li><li>write verification failures</li><li>PostgreSQL capacity</li><li>filesystem capacity</li><li>backup failures</li></ul><p>Logs must remain access-controlled and must never contain OAuth credentials or presigned report URLs — see <a href="#log-redaction">Log redaction</a> below.</p><h2 id="suspected-credential-or-data-exposure" tabindex="-1">Suspected credential or data exposure <a class="header-anchor" href="#suspected-credential-or-data-exposure" aria-label="Permalink to &quot;Suspected credential or data exposure&quot;">​</a></h2><p>If you suspect the LWA client secret, a token, the session secret, an encryption key, or advertiser data has leaked:</p><ol><li>Set <code>KILL_SWITCH=true</code> and restart the API and worker.</li><li>Revoke or rotate the affected LWA client credentials and the Amazon connection.</li><li>Rotate <code>SESSION_SECRET</code>; invalidate active rows in the <code>sessions</code> table.</li><li>Rotate the token encryption keys, retaining old versions until all stored ciphertext has been re-encrypted or the Amazon connections are revoked (see <a href="/amazon-king/guide/configuration#token-encryption-key">key rotation</a>).</li><li>Restrict network access and preserve the relevant audit and infrastructure logs.</li><li>Determine which advertiser data was affected and follow applicable Amazon and legal notification requirements.</li><li>Patch and verify in an isolated environment before restoring access.</li></ol><p>Report software vulnerabilities privately according to <code>SECURITY.md</code> in the repository. Never include a live secret or advertiser dataset in the report.</p><h2 id="data-export-and-deletion" tabindex="-1">Data export and deletion <a class="header-anchor" href="#data-export-and-deletion" aria-label="Permalink to &quot;Data export and deletion&quot;">​</a></h2><p>The alpha does not provide an in-product workspace export or a full deletion workflow. Disconnecting Amazon revokes the application&#39;s access and destroys the stored refresh token, but <strong>imported metrics and audit records remain in PostgreSQL</strong>. Until a tested application workflow exists, fulfill deletion requests at the infrastructure/database layer.</p><p>This limitation must be disclosed in your privacy notice if you operate the instance for anyone&#39;s data but your own.</p><h2 id="log-redaction" tabindex="-1">Log redaction <a class="header-anchor" href="#log-redaction" aria-label="Permalink to &quot;Log redaction&quot;">​</a></h2><p>Logs are structured and redacted by the platform, but the rule bears repeating for anything you add yourself: never log</p><ul><li>authorization headers or Amazon access/refresh tokens,</li><li>OAuth authorization codes or state values,</li><li>client secrets or session secrets,</li><li>presigned report download URLs.</li></ul><p>Amazon auth errors are sanitized before logging; keep it that way in any custom tooling, and scrub logs before attaching them to issues or reports.</p><h2 id="updating-cadence" tabindex="-1">Updating cadence <a class="header-anchor" href="#updating-cadence" aria-label="Permalink to &quot;Updating cadence&quot;">​</a></h2><p>Follow the update procedure in <a href="/amazon-king/guide/self-hosting#updating">Self-hosting</a>: back up first, review release notes and migrations, <code>git pull --ff-only</code>, <code>make prod-up</code>. Pin and review versioned releases once they are available rather than tracking a moving branch on a production advertiser account.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/operations.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const operations = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  operations as default
};
