import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Commands","description":"Every Makefile target, pnpm script, and per-package command for developing, testing, and operating amazon-king.","frontmatter":{"title":"Commands","description":"Every Makefile target, pnpm script, and per-package command for developing, testing, and operating amazon-king."},"headers":[],"relativePath":"reference/commands.md","filePath":"reference/commands.md","lastUpdated":1787252819000}');
const _sfc_main = { name: "reference/commands.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="commands" tabindex="-1">Commands <a class="header-anchor" href="#commands" aria-label="Permalink to &quot;Commands&quot;">​</a></h1><p>All commands run from the repository root unless noted. The Makefile loads <code>.env</code> into every recipe when the file exists, so <code>DATABASE_URL</code> and friends are available to each target.</p><h2 id="makefile-targets" tabindex="-1">Makefile targets <a class="header-anchor" href="#makefile-targets" aria-label="Permalink to &quot;Makefile targets&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Target</th><th>Behavior</th></tr></thead><tbody><tr><td><code>make help</code></td><td>Lists the available targets (the default goal).</td></tr><tr><td><code>make install</code></td><td><code>pnpm install</code> — install workspace dependencies.</td></tr><tr><td><code>make setup</code></td><td><code>install</code>, then create <code>.env</code> from <code>.env.example</code> if missing (set <code>LWA_CLIENT_ID</code>/<code>LWA_CLIENT_SECRET</code> afterwards).</td></tr><tr><td><code>make preflight</code></td><td>Fail fast unless <code>DATABASE_URL</code>, <code>SESSION_SECRET</code>, <code>LWA_CLIENT_ID</code>, and <code>LWA_CLIENT_SECRET</code> are set in <code>.env</code>.</td></tr><tr><td><code>make db-up</code></td><td><code>docker compose up -d db</code>, then wait up to 30 s for <code>pg_isready</code> on the <code>amazon-king-db</code> container.</td></tr><tr><td><code>make migrate</code></td><td>Apply database migrations: sources <code>.env</code>, then <code>pnpm exec tsx scripts/migrate.ts</code>.</td></tr><tr><td><code>make run</code></td><td><code>setup</code> → <code>preflight</code> → <code>db-up</code> → <code>migrate</code>, then start api (<code>http://localhost:3000</code>), worker, and web (<code>http://localhost:5173</code>) together. Ctrl-C stops all three.</td></tr><tr><td><code>make dev</code></td><td>Alias for <code>run</code>.</td></tr><tr><td><code>make test</code></td><td><code>pnpm -r test</code> — every package&#39;s tests.</td></tr><tr><td><code>make typecheck</code></td><td><code>pnpm -r typecheck</code>.</td></tr><tr><td><code>make lint</code></td><td><code>pnpm lint</code> — <code>prettier --check .</code>. Fix with <code>pnpm exec prettier --write .</code>.</td></tr><tr><td><code>make build</code></td><td><code>pnpm --filter @amazon-king/web build</code> — production web build.</td></tr><tr><td><code>make check</code></td><td><code>pnpm check</code> — formatting, typechecks, tests, and the production web build.</td></tr><tr><td><code>make prod-config</code></td><td>Create <code>.env.production</code> from <code>.env.production.example</code> if missing (git-ignored; replace every <code>change-me</code> value).</td></tr><tr><td><code>make prod-preflight</code></td><td>Fail unless <code>.env.production</code> exists and contains no <code>change-me</code> placeholder.</td></tr><tr><td><code>make prod-up</code></td><td><code>prod-preflight</code>, then <code>docker compose --env-file .env.production -f compose.production.yml up -d --build</code> — build and start the self-hosted stack (db, migrate, api, worker, web).</td></tr><tr><td><code>make prod-logs</code></td><td>Follow the production stack logs (<code>logs -f</code>).</td></tr><tr><td><code>make prod-stop</code></td><td>Stop the production stack (<code>down</code>) without deleting persistent data.</td></tr><tr><td><code>make stop</code></td><td>Stop local PostgreSQL (<code>docker compose down</code>).</td></tr><tr><td><code>make clean</code></td><td><code>stop</code>, then <code>docker compose down -v</code> and <code>rm -rf .data</code> — <strong>destroys all local data</strong> (database volume and downloaded reports).</td></tr></tbody></table><h2 id="root-pnpm-scripts" tabindex="-1">Root pnpm scripts <a class="header-anchor" href="#root-pnpm-scripts" aria-label="Permalink to &quot;Root pnpm scripts&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Script</th><th>Runs</th><th>Notes</th></tr></thead><tbody><tr><td><code>pnpm dev</code></td><td><code>pnpm -r --parallel --if-present dev</code></td><td>Starts every workspace dev server/watch process: api, worker, web, and the VitePress docs site (<code>website/</code>).</td></tr><tr><td><code>pnpm build</code></td><td><code>pnpm -r --if-present build</code></td><td>Every package with a <code>build</code> script: web and the docs site (<code>website/</code>).</td></tr><tr><td><code>pnpm test</code></td><td><code>pnpm -r test</code></td><td></td></tr><tr><td><code>pnpm typecheck</code></td><td><code>pnpm -r typecheck</code></td><td></td></tr><tr><td><code>pnpm lint</code></td><td><code>prettier --check .</code></td><td></td></tr><tr><td><code>pnpm check</code></td><td><code>pnpm lint &amp;&amp; pnpm typecheck &amp;&amp; pnpm test &amp;&amp; pnpm build</code></td><td>The full pre-submit gate.</td></tr></tbody></table><h2 id="per-package-scripts" tabindex="-1">Per-package scripts <a class="header-anchor" href="#per-package-scripts" aria-label="Permalink to &quot;Per-package scripts&quot;">​</a></h2><p>Every package supports <code>pnpm --filter &lt;name&gt; &lt;script&gt;</code>:</p><table tabindex="0"><thead><tr><th>Package</th><th>dev</th><th>start</th><th>build</th><th>test</th><th>typecheck</th></tr></thead><tbody><tr><td><code>@amazon-king/api</code></td><td><code>tsx watch src/index.ts</code></td><td><code>tsx src/index.ts</code></td><td>—</td><td>vitest</td><td>tsc</td></tr><tr><td><code>@amazon-king/worker</code></td><td><code>tsx watch src/index.ts</code></td><td><code>tsx src/index.ts</code></td><td>—</td><td>vitest</td><td>tsc</td></tr><tr><td><code>@amazon-king/web</code></td><td><code>vite</code></td><td>—</td><td><code>vite build</code></td><td>vitest (jsdom + Testing Library)</td><td>tsc</td></tr><tr><td><code>@amazon-king/database</code></td><td>—</td><td>—</td><td>—</td><td>vitest</td><td>tsc</td></tr><tr><td><code>@amazon-king/optimizer</code></td><td>—</td><td>—</td><td>—</td><td>vitest</td><td>tsc</td></tr><tr><td><code>@amazon-king/amazon-ads</code></td><td>—</td><td>—</td><td>—</td><td>vitest</td><td>tsc</td></tr><tr><td><code>@amazon-king/contracts</code></td><td>—</td><td>—</td><td>—</td><td>vitest</td><td>tsc</td></tr><tr><td><code>@amazon-king/crypto</code></td><td>—</td><td>—</td><td>—</td><td>vitest</td><td>tsc</td></tr><tr><td><code>@amazon-king/observability</code></td><td>—</td><td>—</td><td>—</td><td>vitest</td><td>tsc</td></tr></tbody></table><p>Test scripts use <code>vitest run --passWithNoTests</code> (contracts and observability: plain <code>vitest run</code>), so a package without tests never fails the suite. Api, worker, and package tests need neither network nor a real database — except the database integration suite below.</p><h2 id="database-migrations" tabindex="-1">Database migrations <a class="header-anchor" href="#database-migrations" aria-label="Permalink to &quot;Database migrations&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> migrate</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># or directly:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> tsx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scripts/migrate.ts</span></span></code></pre></div><p>Requires <code>DATABASE_URL</code>. Applies all pending SQL migrations from <code>packages/database/migrations</code> (numbered <code>NNNN_name.sql</code>), each in its own transaction, recording applied files in <code>schema_migrations</code>. Prints <code>Applied migrations: …</code> or <code>Database is up to date — no pending migrations.</code></p><h2 id="database-integration-tests" tabindex="-1">Database integration tests <a class="header-anchor" href="#database-integration-tests" aria-label="Permalink to &quot;Database integration tests&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">TEST_DATABASE_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">postgres://postgres:postgres@localhost:5432/amazon_king_test</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> \\</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">  pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --filter</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> @amazon-king/database</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> test</span></span></code></pre></div><p>The suite <strong>drops and recreates the <code>public</code> schema</strong> — point it only at a disposable scratch database, never at real data. Without <code>TEST_DATABASE_URL</code> the integration tests are skipped and the rest of the suite still passes.</p><h2 id="docs-site" tabindex="-1">Docs site <a class="header-anchor" href="#docs-site" aria-label="Permalink to &quot;Docs site&quot;">​</a></h2><p>The documentation site (VitePress) lives in <code>website/</code> and has its own dependencies:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> website</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      # local dev server with hot reload</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # static build to website/.vitepress/dist</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # serve the production build locally</span></span></code></pre></div><h2 id="related-reading" tabindex="-1">Related reading <a class="header-anchor" href="#related-reading" aria-label="Permalink to &quot;Related reading&quot;">​</a></h2><ul><li><a href="/amazon-king/guide/installation">Installation</a> — first-time setup walkthrough</li><li><a href="/amazon-king/guide/self-hosting">Self-hosting</a> — the production compose workflow</li><li><a href="/amazon-king/guide/operations">Operations</a> — day-two runbook</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/commands.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const commands = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  commands as default
};
