import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Data pipeline","description":"How amazon-king imports Amazon Ads structure and metrics — the metrics_sync report orchestration, structure_sync snapshot upserts, the PostgreSQL job queue, and the deterministic recommendation run.","frontmatter":{"title":"Data pipeline","description":"How amazon-king imports Amazon Ads structure and metrics — the metrics_sync report orchestration, structure_sync snapshot upserts, the PostgreSQL job queue, and the deterministic recommendation run."},"headers":[],"relativePath":"architecture/data-pipeline.md","filePath":"architecture/data-pipeline.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "architecture/data-pipeline.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="data-pipeline" tabindex="-1">Data pipeline <a class="header-anchor" href="#data-pipeline" aria-label="Permalink to &quot;Data pipeline&quot;">​</a></h1><p>The worker (<code>apps/worker</code>) owns all data movement: discovering profiles, pulling campaign structure, importing daily metrics through Amazon&#39;s asynchronous Reporting v3, and generating recommendations. Everything is idempotent — any step can be retried or replayed and the database converges to the same state.</p><h2 id="job-queue-mechanics" tabindex="-1">Job queue mechanics <a class="header-anchor" href="#job-queue-mechanics" aria-label="Permalink to &quot;Job queue mechanics&quot;">​</a></h2><p>All work flows through the <code>job_queue</code> table (<code>packages/database/src/queue.ts</code>), claimed by the loop in <code>apps/worker/src/loop.ts</code>:</p><ul><li><strong>Claim</strong> — one <code>pending</code> job with <code>run_at &lt;= now()</code>, oldest first, selected <code>FOR UPDATE SKIP LOCKED</code> so concurrent workers never take the same job. The claim sets <code>status = &#39;running&#39;</code>, increments <code>attempts</code>, and starts a lease (default 120 s, <code>WORKER_LEASE_SECONDS</code>).</li><li><strong>Heartbeat</strong> — every 30 s (<code>WORKER_HEARTBEAT_MS</code>) the running job&#39;s lease is extended. If the heartbeat finds the job no longer owned by this worker, the worker knows it was reaped.</li><li><strong>Complete</strong> — sets <code>status = &#39;done&#39;</code> and clears the lock.</li><li><strong>Fail</strong> — if attempts remain, the job returns to <code>pending</code> with an exponential backoff <code>run_at</code>: full jitter, base 1 s, capped at 5 minutes (<code>packages/database/src/backoff.ts</code>). At <code>max_attempts</code> (default 5) the job becomes <code>dead</code>. Handlers can throw <code>TerminalJobError</code> for failures retrying cannot fix (validation errors, reconciliation failures, dead OAuth grants) — those dead-letter immediately.</li><li><strong>Reap</strong> — every 60 s (<code>WORKER_REAP_INTERVAL_MS</code>), <code>reapExpiredLeases</code> returns <code>running</code> jobs whose lease expired (dead worker) to <code>pending</code>.</li><li><strong>Disconnect sweep</strong> — when the owner disconnects Amazon, <code>failPendingJobsForProfiles</code> fails every still-pending job addressed to that connection&#39;s profiles so queued syncs never run against a dead grant.</li></ul><p>Scheduling itself is a job: <code>schedule_tick</code> re-enqueues itself every 15 minutes and enqueues due recurring work deduped via <code>enqueueIfNotQueued</code> (a <code>payload @&gt; ?</code> match against pending/running jobs). See <a href="/amazon-king/architecture/overview#runtime-lifecycle">Architecture overview</a> for the cadence table.</p><h2 id="structure-sync" tabindex="-1">structure_sync <a class="header-anchor" href="#structure-sync" aria-label="Permalink to &quot;structure_sync&quot;">​</a></h2><p><code>apps/worker/src/jobs/structure-sync.ts</code> pulls the profile&#39;s Sponsored Products snapshot through the gateway (<code>syncCampaignStructure</code>: campaigns, ad groups, ads, keywords, product targets, negative keywords) and applies it in <strong>one transaction</strong> (<code>applyStructureSnapshot</code> in <code>apps/worker/src/store.ts</code>):</p><ol><li>Campaigns, ad groups, and ads are upserted by their Amazon ids, which are unique per profile.</li><li>Keywords and product targets share the <code>targets</code> table, distinguished by <code>target_kind</code> (<code>keyword</code> | <code>product</code>); keywords store <code>{type: &quot;keyword&quot;, value: keywordText}</code> in the <code>expression</code> jsonb column, product targets store the raw targeting expression.</li><li>Negative keywords are upserted at campaign and ad-group level, then <code>deleteMissingNegativeKeywords</code> removes rows Amazon no longer returns.</li><li>Every upsert diffs name/bid/budget/state and records changes into <code>entity_change_history</code> (<code>packages/database/src/repositories/structure.ts</code>), so the dashboard can show what changed on Amazon between syncs.</li></ol><p>Each run is bracketed by a <code>sync_runs</code> row (<code>kind = &#39;structure&#39;</code>) that ends <code>complete</code> or <code>failed</code>. An unrecoverable auth error marks the connection <code>reconnect_required</code> and dead-letters the job.</p><h2 id="metrics-sync-—-reporting-v3-orchestration" tabindex="-1">metrics_sync — Reporting v3 orchestration <a class="header-anchor" href="#metrics-sync-—-reporting-v3-orchestration" aria-label="Permalink to &quot;metrics_sync — Reporting v3 orchestration&quot;">​</a></h2><p>The core of the pipeline (<code>apps/worker/src/jobs/metrics-sync.ts</code>). Payload:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;profileId&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;startDate&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;YYYY-MM-DD&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;endDate&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;YYYY-MM-DD&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre></div><p>Date handling:</p><ul><li>The scheduled daily job syncs <strong>yesterday</strong> only; <code>recent_window_resync</code> separately re-imports the trailing 14 days for late attribution.</li><li>A manual/legacy payload without dates falls back to the trailing <strong>31 complete UTC days</strong>.</li><li>Ranges are split into chunks of at most <strong>31 inclusive days</strong> — the Reporting v3 maximum — inside the one queue job, so a 60-day manual import still runs as a single unit and recommendations chain only after every chunk lands.</li></ul><h3 id="spec-fingerprints-and-dedupe" tabindex="-1">Spec fingerprints and dedupe <a class="header-anchor" href="#spec-fingerprints-and-dedupe" aria-label="Permalink to &quot;Spec fingerprints and dedupe&quot;">​</a></h3><p>For each chunk, one spec is built per report family — <code>spCampaigns</code>, <code>spTargeting</code>, <code>spSearchTerm</code>, <code>spAdvertisedProduct</code> (<code>apps/worker/src/report-specs.ts</code>). Placement reporting has no stable daily import path yet, so no spec is built for it. Every family requests DAILY rows with explicit attribution columns: <code>impressions</code>, <code>clicks</code>, <code>cost</code>, <code>purchases7d</code>, <code>sales7d</code>, <code>purchases14d</code>, <code>sales14d</code>.</p><p>Each spec gets a deterministic fingerprint (<code>buildReportSpecFingerprint</code> in <code>packages/database/src/fingerprint.ts</code>): the SHA-256 of a stable JSON serialization of <code>{profileId, reportType, dateStart, dateEnd, columns}</code>, with columns sorted and <code>REPORT_CONFIGURATION_VERSION</code> (currently <code>reporting-v3-config-2</code>) prepended. Bumping that constant invalidates old fingerprints when adapter-owned dimensions change, so a queued sync can never adopt a stale artifact whose shape no longer matches the row schemas.</p><p>The fingerprint is a unique key on <code>report_jobs.spec_fingerprint</code>:</p><ul><li>A <strong>complete</strong> spec is never requested again — re-imports are free.</li><li>A <strong>failed</strong> or <strong>dead_letter</strong> spec is retried, up to 5 attempts (<code>MAX_REPORT_ATTEMPTS</code>); past that the job is dead-lettered and the queue job fails terminally.</li></ul><h3 id="per-family-flow" tabindex="-1">Per-family flow <a class="header-anchor" href="#per-family-flow" aria-label="Permalink to &quot;Per-family flow&quot;">​</a></h3>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-132",
        class: "mermaid",
        graph: "sequenceDiagram%0A%20%20%20%20participant%20W%20as%20worker%20(metrics_sync)%0A%20%20%20%20participant%20DB%20as%20PostgreSQL%0A%20%20%20%20participant%20G%20as%20AmazonAdsGateway%0A%20%20%20%20participant%20A%20as%20Amazon%20Reporting%20v3%0A%20%20%20%20participant%20S%20as%20Report%20storage%0A%0A%20%20%20%20W-%3E%3EDB%3A%20find%20report_jobs%20by%20spec_fingerprint%0A%20%20%20%20alt%20already%20complete%0A%20%20%20%20%20%20%20%20DB--%3E%3EW%3A%20skip%20(dedupe)%0A%20%20%20%20else%20queued%20%2F%20requested%20%2F%20retryable%0A%20%20%20%20%20%20%20%20W-%3E%3EG%3A%20requestReport(profile%2C%20spec)%0A%20%20%20%20%20%20%20%20G-%3E%3EA%3A%20POST%20%2Freporting%2Freports%20(GZIP_JSON%2C%20DAILY)%0A%20%20%20%20%20%20%20%20A--%3E%3EG%3A%20reportId%0A%20%20%20%20%20%20%20%20W-%3E%3EDB%3A%20status%3Dpolling%2C%20persist%20amazon_report_id%0A%20%20%20%20end%0A%20%20%20%20loop%20poll%3A%205s%2C%20doubling%20to%2060s%20cap%2C%2020min%20total%0A%20%20%20%20%20%20%20%20W-%3E%3EG%3A%20getReport(amazon_report_id)%0A%20%20%20%20%20%20%20%20G-%3E%3EA%3A%20GET%20%2Freporting%2Freports%2F%7Bid%7D%0A%20%20%20%20%20%20%20%20A--%3E%3EG%3A%20PROCESSING%20%E2%80%A6%20COMPLETED%20%2B%20download%20URL%0A%20%20%20%20end%0A%20%20%20%20W-%3E%3ES%3A%20stream%20download%20to%20workspace%2Fprofile%2FreportId.json.gz%20(sha256%20inline)%0A%20%20%20%20W-%3E%3ES%3A%20read%20back%2C%20verify%20checksum%2C%20gunzip%2C%20zod-validate%20rows%0A%20%20%20%20W-%3E%3EW%3A%20reconcileFacts%20(counts%2C%20ranges%2C%20currency%2C%20grain%20keys)%0A%20%20%20%20W-%3E%3EDB%3A%20single-transaction%20upsert%20(ON%20CONFLICT%20DO%20UPDATE)%0A%20%20%20%20W-%3E%3EDB%3A%20report_jobs.status%3Dcomplete%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<p>The <code>report_jobs</code> status machine: <code>queued → requested → polling → downloading → validating → importing → complete</code>, with <code>retryable</code>, <code>failed</code>, and <code>dead_letter</code> off to the side.</p><p>Key properties:</p><ul><li><strong>Restart-safe resume.</strong> The <code>amazon_report_id</code> is persisted as soon as Amazon issues it. After a worker restart, a job in <code>polling</code> or later resumes without re-requesting the report; the gateway&#39;s <code>reportOwner</code> callback (<code>findProfilePkForReport</code>) resolves which profile owns a persisted report id.</li><li><strong>Polling</strong> starts at 5 s, doubles each round, caps at 60 s, and gives up after 20 minutes total (<code>REPORT_POLL_*</code>), marking the job <code>retryable</code>.</li><li><strong>Download</strong> streams the pre-signed URL straight to disk — never buffered in memory (<code>downloadReport</code> in <code>packages/amazon-ads/src/adapters/reporting.ts</code>). The artifact keeps Amazon&#39;s gzip as-is at <code>&lt;workspaceId&gt;/&lt;profilePk&gt;/&lt;amazonReportId&gt;.json.gz</code> under <code>REPORT_STORAGE_DIR</code>, written to a temp file and atomically renamed, with a sha256 computed inline (<code>apps/worker/src/storage.ts</code>). The pre-signed URL is never logged.</li><li><strong>Validation</strong> reads the artifact back, verifies the checksum, gunzips, and runs each family&#39;s zod row schema (<code>parseReportRows</code>). Adapter validation errors are terminal.</li><li><strong>Reconciliation</strong> (<code>reconcileFacts</code> in <code>apps/worker/src/reconcile.ts</code>) rejects the report unless: the mapped row count matches the file; counts are non-negative integers and money is non-negative; every row&#39;s date lies inside the requested range; every row&#39;s currency matches the profile currency; and no duplicate grain keys exist (entity + date).</li><li><strong>Import</strong> is a single transaction: rows are passed as jsonb, expanded with <code>jsonb_to_recordset</code>, and upserted <code>ON CONFLICT DO UPDATE</code> into the family&#39;s <code>*_metrics_daily</code> table (<code>packages/database/src/repositories/metrics.ts</code>). Replays converge.</li></ul><h3 id="run-completion-and-chaining" tabindex="-1">Run completion and chaining <a class="header-anchor" href="#run-completion-and-chaining" aria-label="Permalink to &quot;Run completion and chaining&quot;">​</a></h3><p>The encompassing <code>sync_runs</code> row is marked <code>complete</code> <strong>only when every family fingerprint in every chunk reports <code>complete</code></strong> — adopted or resumed report jobs included. Only then does the handler enqueue a <code>recommendation_run</code> (deduped via <code>enqueueIfNotQueued</code>). Recommendations are never generated from partial data.</p><h2 id="recommendation-run" tabindex="-1">recommendation_run <a class="header-anchor" href="#recommendation-run" aria-label="Permalink to &quot;recommendation_run&quot;">​</a></h2><p><code>apps/worker/src/jobs/recommendation-run.ts</code> turns facts into ranked, reviewable recommendations:</p><ol><li><strong>Freshness gate.</strong> The run skips entirely unless the profile has a <code>complete</code> metrics sync run finished within the last 48 hours (<code>RECOMMENDATION_FRESHNESS_HOURS</code>) — never recommend from a silent partial dataset.</li><li><strong>Expire stale</strong>, then <strong>load inputs</strong>: the full structure snapshot, daily facts for the trailing 60 days (the longest evidence window), the latest effective book economics per marketplace ASIN, and applied change actions from the last 7 days (<code>cooldownDays</code>) so recently-touched entities stay quiet.</li><li><strong>Evaluate all nine rules</strong> — wasteful search term, expensive target, profitable target, search-term harvest, budget-constrained winner, high-CTR-poor-conversion, low impressions, placement opportunity, cannibalization conflict — over each of the 7/14/30/60-day windows ending yesterday. Rules are pure functions in <code>packages/optimizer/src/rules/</code> with injected time; profit rules suppress themselves when the campaign has no KDP economics.</li><li><strong>Dedupe across windows</strong>: the same rule firing on the same entity keeps only the highest-impact draft.</li><li><strong>Rank</strong> deterministically (<code>packages/optimizer/src/rank.ts</code>): impact (micros) descending, then confidence, then rule version, then entity key. Priority maps rank onto quintiles — the top fifth of a batch is P1, the bottom fifth P5.</li><li><strong>Insert</strong> each draft unless an identical pending recommendation already exists. Each row carries its evidence window, current/proposed values, confidence, <code>rule_version</code>, <code>data_freshness_at</code>, and <code>expires_at = now + 3 days</code> (<code>stalenessDays</code>); the exact rule inputs go to <code>recommendation_evidence.inputs</code> (jsonb, immutable) so every recommendation is reproducible.</li></ol><p>Rule thresholds and formulas are documented in <a href="/amazon-king/reference/optimization-rules">Optimization rules</a>; the review workflow in <a href="/amazon-king/guide/recommendations">Recommendations</a>.</p><h2 id="attribution-model" tabindex="-1">Attribution model <a class="header-anchor" href="#attribution-model" aria-label="Permalink to &quot;Attribution model&quot;">​</a></h2><p>Fact tables store both attribution windows explicitly and never merge them: <code>purchases7d</code>/<code>sales7d</code> and <code>purchases14d</code>/<code>sales14d</code> are separate columns on every <code>*_metrics_daily</code> table. At import time the convenience columns are set from the 7-day window — <code>orders = purchases7d</code>, <code>sales = sales7d</code> (<code>mapRowsToFacts</code> in <code>apps/worker/src/reconcile.ts</code>) — and rows missing optional attribution columns default to 0. This is why <code>recent_window_resync</code> exists: a conversion attributed 10 days late updates the stored 7d/14d columns through the same idempotent upsert.</p><h2 id="failure-semantics-at-a-glance" tabindex="-1">Failure semantics at a glance <a class="header-anchor" href="#failure-semantics-at-a-glance" aria-label="Permalink to &quot;Failure semantics at a glance&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Failure</th><th>Result</th></tr></thead><tbody><tr><td>Amazon report polling times out</td><td><code>retryable</code>, fresh report requested next attempt</td></tr><tr><td>Amazon report fails at source</td><td><code>retryable</code>, old report id superseded</td></tr><tr><td>Artifact checksum mismatch on read-back</td><td><code>failed</code>, retried</td></tr><tr><td>Zod validation / reconciliation failure</td><td>Terminal — dead-letters after the attempt budget</td></tr><tr><td>Unrecoverable auth error</td><td>Connection marked <code>reconnect_required</code>, job dead-lettered</td></tr><tr><td>Worker crash mid-job</td><td>Lease expires, <code>reapExpiredLeases</code> returns the job to <code>pending</code></td></tr><tr><td>5 report attempts exhausted</td><td><code>dead_letter</code>; the metrics sync run fails and chains nothing</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture/data-pipeline.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const dataPipeline = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  dataPipeline as default
};
