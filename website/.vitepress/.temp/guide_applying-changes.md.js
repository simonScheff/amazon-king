import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderAttr, ssrRenderSuspense, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/amazon-king/screenshots/change-center.png";
const __pageData = JSON.parse('{"title":"Applying & Rolling Back Changes","description":"The guarded write pipeline — change sets, previews, stale-state checks, guardrails, per-item results, verification, and compensating rollback.","frontmatter":{"title":"Applying & Rolling Back Changes","description":"The guarded write pipeline — change sets, previews, stale-state checks, guardrails, per-item results, verification, and compensating rollback."},"headers":[],"relativePath":"guide/applying-changes.md","filePath":"guide/applying-changes.md","lastUpdated":1787252819000}');
const _sfc_main = { name: "guide/applying-changes.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_Mermaid = resolveComponent("Mermaid");
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="applying-rolling-back-changes" tabindex="-1">Applying &amp; Rolling Back Changes <a class="header-anchor" href="#applying-rolling-back-changes" aria-label="Permalink to &quot;Applying &amp; Rolling Back Changes&quot;">​</a></h1><p>Every Amazon write goes through one guarded pipeline. Read-only is the default at two levels — the global <code>KILL_SWITCH</code> and per-profile <code>writeEnabled</code> — and even with both open, nothing is sent to Amazon without a fresh re-read proving the account still looks like what you approved.</p><p><img${ssrRenderAttr("src", _imports_0)} alt="Change center with expandable change sets and per-action rollback"></p><h2 id="change-sets" tabindex="-1">Change sets <a class="header-anchor" href="#change-sets" aria-label="Permalink to &quot;Change sets&quot;">​</a></h2><p>A change set is an <strong>immutable, human-approved unit of work</strong>: a set of actions (bid updates, negative keywords, campaign creations) plus the before-state they were approved against. Approving a recommendation creates one; so do the <a href="/amazon-king/guide/campaign-tools">campaign tools</a> (Max CPC, campaign creation, cannibalization negatives) and rollback.</p><h3 id="status-lifecycle" tabindex="-1">Status lifecycle <a class="header-anchor" href="#status-lifecycle" aria-label="Permalink to &quot;Status lifecycle&quot;">​</a></h3><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>draft → previewed → applying → applied</span></span>
<span class="line"><span>                         ↓     → partially_applied</span></span>
<span class="line"><span>                     failed ←──┘  (retryable)</span></span>
<span class="line"><span>                     blocked      (terminal: stale, guardrail, or Max CPC violation)</span></span></code></pre></div><ul><li><code>draft</code> / <code>previewed</code> — created, not yet applied. Previewing records the <code>previewed</code> status.</li><li><code>applying</code> — optimistic lock while the pipeline runs; a second apply attempt fails with <code>409 APPLY_IN_PROGRESS</code>.</li><li><code>applied</code> / <code>partially_applied</code> — terminal success states; re-applying is idempotent and returns the stored result without touching Amazon again.</li><li><code>failed</code> — the apply raised before or during the write. Retryable (see below).</li><li><code>blocked</code> — the pipeline refused the set: stale before-state, guardrail violation, or a Max CPC conflict. Terminal — create a fresh set from current data.</li></ul><h2 id="the-change-center" tabindex="-1">The change center <a class="header-anchor" href="#the-change-center" aria-label="Permalink to &quot;The change center&quot;">​</a></h2><p><code>/changes</code> lists every change set as an expandable row. The preview — actions and guardrail violations — is fetched lazily on first expand, so the page does not fan out one request per set. Inside an expanded set you get:</p><ul><li>each action with its before → after values and per-action status,</li><li>guardrail violations when present,</li><li>a <strong>confirm dialog</strong> before applying (danger-styled; the button reads <em>Retry apply</em> for failed sets),</li><li>per-action <strong>rollback buttons</strong> where the action supports it, and</li><li>a <strong>dependency lock notice</strong> when the set depends on another set that has not been applied yet (see <a href="/amazon-king/guide/campaign-tools#resolving-cannibalization">Cannibalization</a>) — the apply button is replaced by the notice until the dependency is <code>applied</code>.</li></ul><h2 id="the-apply-pipeline" tabindex="-1">The apply pipeline <a class="header-anchor" href="#the-apply-pipeline" aria-label="Permalink to &quot;The apply pipeline&quot;">​</a></h2><p>Applying runs these steps in order, inside the API:</p>`);
  ssrRenderSuspense(_push, {
    default: () => {
      _push(ssrRenderComponent(_component_Mermaid, {
        id: "mermaid-85",
        class: "mermaid",
        graph: "flowchart%20TD%0A%20%20%20%20A%5BApply%20requested%5D%20--%3E%20B%7BAlready%20applied%3F%7D%0A%20%20%20%20B%20--%20yes%20--%3E%20Z%5BReturn%20stored%20result%5D%0A%20%20%20%20B%20--%20no%20--%3E%20C%7BDependency%20applied%3F%7D%0A%20%20%20%20C%20--%20no%20--%3E%20E1%5B409%20DEPENDENCY_NOT_APPLIED%5D%0A%20%20%20%20C%20--%20yes%20--%3E%20D%7BProfile%20writeEnabled%3F%7D%0A%20%20%20%20D%20--%20no%20--%3E%20E2%5B403%20WRITES_DISABLED%5D%0A%20%20%20%20D%20--%20yes%20--%3E%20F%5BLock%20set%3A%20applying%5D%0A%20%20%20%20F%20--%3E%20G%7BRecommendations%20expired%3F%7D%0A%20%20%20%20G%20--%20yes%20--%3E%20E3%5B409%20RECOMMENDATION_EXPIRED%5D%0A%20%20%20%20G%20--%20no%20--%3E%20H%7BMax%20CPC%20policy%20OK%3F%7D%0A%20%20%20%20H%20--%20no%20--%3E%20E4%5B409%20MAX_CPC_EXCEEDED%20%E2%86%92%20blocked%5D%0A%20%20%20%20H%20--%20yes%20--%3E%20I%5BFresh%20Amazon%20re-read%20vs%20before-state%5D%0A%20%20%20%20I%20--%20mismatch%20--%3E%20E5%5B409%20STALE_BEFORE_STATE%20%E2%86%92%20blocked%5D%0A%20%20%20%20I%20--%20match%20--%3E%20J%5BGuardrail%20re-run%5D%0A%20%20%20%20J%20--%20violation%20--%3E%20E6%5B409%20GUARDRAIL_VIOLATION%20%E2%86%92%20blocked%5D%0A%20%20%20%20J%20--%20pass%20--%3E%20K%5BPer-item%20apply%20to%20Amazon%5D%0A%20%20%20%20K%20--%3E%20L%5BPost-write%20re-read%20verification%5D%0A%20%20%20%20L%20--%3E%20M%5Bapplied%20%2F%20partially_applied%20%2F%20failed%5D%0A"
      }, null, _parent));
    },
    fallback: () => {
      _push(` Loading... `);
    },
    _: 1
  });
  _push(`<ol><li><strong>Kill switch and profile gate.</strong> The global kill switch is evaluated on every guarded path; a read-only profile fails with <code>403 WRITES_DISABLED</code>.</li><li><strong>Dependency check.</strong> A set carrying <code>dependsOnChangeSetId</code> refuses with <code>409 DEPENDENCY_NOT_APPLIED</code> until the referenced set is <code>applied</code>.</li><li><strong>Optimistic lock.</strong> Only <code>draft</code>, <code>previewed</code>, or <code>failed</code> sets can enter <code>applying</code>; concurrent applies get <code>409 APPLY_IN_PROGRESS</code>.</li><li><strong>Expiry re-check.</strong> If any linked recommendation has expired, the set drops back to <code>previewed</code> with <code>409 RECOMMENDATION_EXPIRED</code> — re-create it from fresh data.</li><li><strong>Max CPC policy.</strong> Any proposed bid above an active campaign ceiling fails with <code>409 MAX_CPC_EXCEEDED</code> and blocks the set.</li><li><strong>Fresh re-read vs. before-state.</strong> The API re-reads the live Amazon structure and compares it to the stored before snapshot. Any drift fails with <code>409 STALE_BEFORE_STATE</code> and blocks the set. Actions Amazon already satisfies (e.g. the negative already exists) are marked applied without being resent — apply is idempotent at the action level.</li><li><strong>Guardrail re-run.</strong> Bid clamps, cooldowns, staleness, and change-set size limits are re-evaluated at apply time, not approval time. A violation blocks the set with <code>409 GUARDRAIL_VIOLATION</code> and per-violation details.</li><li><strong>Per-item apply.</strong> Amazon&#39;s per-item (207-style) results are mapped individually — a batch success never implies an item succeeded.</li><li><strong>Post-write verification.</strong> The intended state is re-read from Amazon; each action is stamped <code>verified_at</code>, or marked <code>verification_failed</code>.</li><li><strong>Final status.</strong> All items applied → <code>applied</code>; some failed → <code>partially_applied</code>; the write itself raised → <code>failed</code>.</li></ol><p>Every step is audit-logged with the actor, session, and IP.</p><h2 id="retrying-a-failed-set" tabindex="-1">Retrying a failed set <a class="header-anchor" href="#retrying-a-failed-set" aria-label="Permalink to &quot;Retrying a failed set&quot;">​</a></h2><p>A <code>failed</code> set can be retried in place. Because the payload was already approved once, the retry <strong>skips the recent-auth gate</strong> — but it goes through the same guarded pipeline, including the fresh Amazon re-read, so a retry can never push a stale change through.</p><h2 id="idempotency-and-fingerprints" tabindex="-1">Idempotency and fingerprints <a class="header-anchor" href="#idempotency-and-fingerprints" aria-label="Permalink to &quot;Idempotency and fingerprints&quot;">​</a></h2><p>Each change set and each action carries a fingerprint of its canonical content. Submitting an identical payload twice returns the existing set instead of creating a duplicate; the audit event records <code>replayed: true</code>. Combined with the status lock and the already-satisfied check, repeating any step of the flow is safe.</p><h2 id="recent-authentication" tabindex="-1">Recent authentication <a class="header-anchor" href="#recent-authentication" aria-label="Permalink to &quot;Recent authentication&quot;">​</a></h2><p>Spend-changing actions — first-time applies, rollbacks, Max CPC, campaign creation — require a sign-in from the <strong>last 15 minutes</strong>. If your session is older, the API answers <code>401 REAUTH_REQUIRED</code> and the UI opens the <strong>re-authentication dialog</strong>: one click emails a single-use magic link to your address (no typing), carrying the current page as its <code>next</code> path.</p><p>The link picks the interrupted action back up. An apply blocked on the change center returns as <code>/changes?apply=&lt;id&gt;</code>, which reopens that change set with its confirmation showing — you only confirm the write again, never hunt for the set. The write itself still needs that deliberate click. In the installed app, where the link is pasted back instead of followed, nothing navigates away and the apply simply finishes once the session is fresh.</p><h2 id="rollback" tabindex="-1">Rollback <a class="header-anchor" href="#rollback" aria-label="Permalink to &quot;Rollback&quot;">​</a></h2><p>Rollback is a <strong>compensating Amazon action</strong>, not a database undo. Requesting rollback on an applied action creates a new <code>rollback</code> change set that goes through the full pipeline and, on success, marks the original action <code>rolled_back</code>.</p><ul><li><strong>Rollbackable:</strong> applied <code>update_bid</code> actions (restores the stored before-bid) and applied, app-created <code>add_negative_exact</code> actions (via <code>remove_negative_exact</code>).</li><li><strong>Not rollbackable:</strong> campaign creations, and negative ASIN targets. The UI only shows rollback buttons where a verified compensating operation exists.</li><li>The rollback&#39;s guardrail cooldown check <strong>exempts the set being undone</strong>, so rolling back a change is never blocked by the cooldown that change itself created.</li></ul><h2 id="kill-switch-and-read-only-defaults" tabindex="-1">Kill switch and read-only defaults <a class="header-anchor" href="#kill-switch-and-read-only-defaults" aria-label="Permalink to &quot;Kill switch and read-only defaults&quot;">​</a></h2><p>Two independent switches must both be open for any write:</p><ol><li><code>KILL_SWITCH=false</code> globally — fail-closed; only the exact string <code>&quot;false&quot;</code> opens it (see <a href="/amazon-king/guide/configuration#kill-switch-semantics">Configuration</a>).</li><li><code>writeEnabled</code> on the profile (Settings → Profiles &amp; sync).</li></ol><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Amazon write operations have not completed end-to-end validation against a live Amazon account in this alpha. Keep the kill switch on until you have run the live validation sequence with a test account or a dedicated low-risk campaign and a small, explicitly approved change.</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/applying-changes.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const applyingChanges = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  applyingChanges as default
};
