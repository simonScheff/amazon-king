import { ssrRenderAttrs, ssrRenderAttr } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const _imports_0 = "/amazon-king/screenshots/campaign-detail.png";
const __pageData = JSON.parse('{"title":"Campaign Tools","description":"The new-campaign wizard, cannibalization resolution, and Max CPC enforcement — three campaign-level tools built on the guarded write pipeline.","frontmatter":{"title":"Campaign Tools","description":"The new-campaign wizard, cannibalization resolution, and Max CPC enforcement — three campaign-level tools built on the guarded write pipeline."},"headers":[],"relativePath":"guide/campaign-tools.md","filePath":"guide/campaign-tools.md","lastUpdated":1787252819000}');
const _sfc_main = { name: "guide/campaign-tools.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="campaign-tools" tabindex="-1">Campaign Tools <a class="header-anchor" href="#campaign-tools" aria-label="Permalink to &quot;Campaign Tools&quot;">​</a></h1><p>Beyond per-recommendation approvals, amazon-king has several campaign-level tools. All of them produce ordinary change sets that go through the same <a href="/amazon-king/guide/applying-changes">guarded apply pipeline</a> — preview, fresh re-read, guardrails, verification.</p><p><img${ssrRenderAttr("src", _imports_0)} alt="Campaign detail page with KPI cards, profit chart, and the ad groups tab"></p><p>The same page lists ad groups, targets, search terms, <strong>negative keywords</strong>, and <strong>negative products</strong> (ASIN exclusions) from the last structure sync. Those tables are read-only — adding or removing a negative still goes through a recommendation or Change center draft.</p><h2 id="pause-enable-rename" tabindex="-1">Pause / enable / rename <a class="header-anchor" href="#pause-enable-rename" aria-label="Permalink to &quot;Pause / enable / rename&quot;">​</a></h2><p>The campaign detail page header has one-click controls to <strong>pause</strong> (or re-enable) and <strong>rename</strong> a campaign. Each click drafts a single-action <code>campaign_update</code> change set and applies it immediately through the guarded pipeline (recent sign-in required — if your session is too old, the re-auth dialog emails you a magic link and returns you to the page). The verified change is reflected in the dashboard right away, and both actions are rollbackable from the Change center (the previous state or name is restored).</p><p>Use rename to annotate <em>why</em> you paused something — e.g. &quot;… — paused, accidental auto campaign&quot;. There is deliberately no delete: Amazon only offers a terminal <code>ARCHIVED</code> state that can never be undone, so the app does not expose it.</p><h2 id="new-campaign-wizard" tabindex="-1">New campaign wizard <a class="header-anchor" href="#new-campaign-wizard" aria-label="Permalink to &quot;New campaign wizard&quot;">​</a></h2><p>Entry points: <strong>+ New campaign</strong> on <code>/campaigns</code>, or <strong>Create a new campaign</strong> from a cannibalization resolution (which prefills market, campaign name, MANUAL targeting, and the conflicted term as an EXACT keyword).</p><p>The wizard at <code>/campaigns/new</code> has six steps:</p><ol><li><strong>Markets</strong> — pick one or more enabled profiles.</li><li><strong>Campaign</strong> — name, daily budget, targeting type (AUTO or MANUAL), start date, state. New campaigns default to <strong>paused</strong> so nothing spends before you deliberately enable it.</li><li><strong>Ad group</strong> — name and default bid.</li><li><strong>Book</strong> — pick a book from your catalog; the wizard uses its per-market ASINs for the product ads. Only books already linked to every selected market appear in the dropdown. Books that are missing a selected market are listed below it so you can confirm the marketplace ASIN and link them in place (the listing must already exist on that Amazon store). You can also add a market from <a href="/amazon-king/guide/book-economics#expanding-to-another-marketplace">Settings</a>.</li><li><strong>Keywords &amp; targets</strong> — keywords (EXACT, PHRASE, or BROAD, each with a bid) and/or ASIN product targets (optional bid). Entering a keyword or ASIN switches the campaign to <strong>MANUAL</strong> targeting automatically: Amazon rejects manual targeting clauses in AUTO campaigns (it creates the close match, loose match, substitutes, and complements targets itself), so an AUTO campaign is only valid with no keywords or targets entered, and a MANUAL campaign requires at least one.</li><li><strong>Review</strong> — the payload is validated against the shared contract schema before submission.</li></ol><p>Submitting posts to <code>POST /api/campaign-creation-change-sets</code> and creates <strong>one draft <code>campaign_creation</code> change set per market</strong>. The wizard never interrupts you for a sign-in, however long you spend in it: drafting touches only the app&#39;s own database, and the recent-sign-in requirement lands on the apply in the Change center, where the campaign actually reaches Amazon.</p><h3 id="how-creation-applies" tabindex="-1">How creation applies <a class="header-anchor" href="#how-creation-applies" aria-label="Permalink to &quot;How creation applies&quot;">​</a></h3><p>Each creation set is an ordered chain of actions: <code>create_campaign → create_ad_group → create_product_ad</code>, plus one <code>create_keyword</code> / <code>create_target</code> per keyword or ASIN target (MANUAL campaigns only — an AUTO campaign&#39;s set is just the three entity creates). Product targets are expressed as <code>ASIN_SAME_AS</code> expressions.</p><ul><li>Created Amazon ids from each phase are substituted into dependent actions; a child whose parent failed is not attempted and fails with <code>PARENT_FAILED</code>, so you never get orphaned ad groups or keywords.</li><li>There is no before-state to compare (nothing exists yet). Instead, apply treats a <strong>same-name campaign already present on Amazon</strong> as satisfied and resends nothing — retrying a partially applied creation is idempotent.</li><li>Created ids are verified against a fresh structure read, and a <code>structure_sync</code> job is enqueued after a successful apply so the dashboard mirrors the new entities immediately.</li></ul><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Campaign creation change sets are <strong>not rollbackable</strong> — there is no compensating &quot;delete campaign&quot; operation. Review the draft carefully in the change center before applying.</p></div><h2 id="resolving-cannibalization" tabindex="-1">Resolving cannibalization <a class="header-anchor" href="#resolving-cannibalization" aria-label="Permalink to &quot;Resolving cannibalization&quot;">​</a></h2><p>A <code>cannibalization_conflict</code> <a href="/amazon-king/guide/recommendations">recommendation</a> means one search term is accruing spend in two or more campaigns — you are bidding against yourself. The recommendation detail page embeds a resolution flow:</p><ol><li>Review the evidence: every conflicting campaign with its spend and orders for the term.</li><li>Choose a <strong>destination</strong>: one existing campaign that should keep serving the term, or <strong>Create a new campaign</strong> (which routes into the wizard with the term prefilled, and submits the finding&#39;s id with the payload).</li><li>Confirm. The API drafts a change set adding the term as a campaign-level <strong>negative exact keyword</strong> — or a <strong>negative ASIN target</strong> when the &quot;term&quot; is an ASIN — in every conflicting campaign except the destination (in <em>all</em> of them when the destination is a new campaign).</li></ol><h3 id="the-dependency-lock" tabindex="-1">The dependency lock <a class="header-anchor" href="#the-dependency-lock" aria-label="Permalink to &quot;The dependency lock&quot;">​</a></h3><p>When the destination is a new campaign, the negatives set carries <code>metadata.dependsOnChangeSetId</code> pointing at the creation set. Apply rejects it with <code>409 DEPENDENCY_NOT_APPLIED</code> until the creation set is <code>applied</code>, and the change center shows a lock notice instead of the apply button. This guarantees the term is never blocked in all campaigns at once — the new destination must exist on Amazon first.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>Negative ASIN targets are not rollbackable; negative exact keywords created this way are (they are verified app-created negatives).</p></div><h2 id="max-cpc" tabindex="-1">Max CPC <a class="header-anchor" href="#max-cpc" aria-label="Permalink to &quot;Max CPC&quot;">​</a></h2><p>Amazon&#39;s bidding features — dynamic bid increases, placement and audience adjustments, automated rules — can push your effective CPC far above the base bid you set. The <strong>Max CPC</strong> tab on the campaign detail page makes one ceiling explicit and enforceable.</p><p>The tab reads the campaign&#39;s live bid controls and reports a coverage status:</p><ul><li><code>not_configured</code> — no ceiling set.</li><li><code>pending</code> — a policy exists but has not been enforced yet.</li><li><code>covered</code> — all base bids are within the ceiling and every known Amazon-side bid increase is neutralized.</li><li><code>drifted</code> — live state no longer matches the policy (for example dynamic bid increases re-enabled, active placement adjustments, Amazon bid rules, or base bids above the ceiling), with each issue listed.</li></ul><p>Setting a ceiling (<code>POST /api/campaigns/:id/max-cpc</code>, recent-auth required) creates a <code>max_cpc</code> change set containing one action per bid that exceeds the ceiling — keyword and target bids, ad group default bids, and the bidding adjustments that inflate them. The policy is tracked in <code>campaign_bid_policies</code> (active, then drifted if live coverage diverges) and any later recommendation whose proposed bid exceeds an active ceiling fails apply with <code>409 MAX_CPC_EXCEEDED</code>.</p><p>Because a ceiling batch can touch every bid in a campaign and only ever <em>reduces</em> monetary exposure, <code>max_cpc</code> sets apply with relaxed guardrails: up to 5,000 actions per set, reductions only (raises are forbidden by construction), no bid cooldown, and a 1-day staleness window instead of the usual three. Campaigns with more than 5,000 bid controls are rejected at draft time (<code>409 TOO_MANY_ACTIONS</code>) — split them first.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/campaign-tools.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const campaignTools = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  campaignTools as default
};
