import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Key Concepts","description":"The core vocabulary of Amazon King — logins, profiles, syncs, recommendations, change sets, guardrails, and book economics — and why each one exists.","frontmatter":{"title":"Key Concepts","description":"The core vocabulary of Amazon King — logins, profiles, syncs, recommendations, change sets, guardrails, and book economics — and why each one exists."},"headers":[],"relativePath":"guide/key-concepts.md","filePath":"guide/key-concepts.md","lastUpdated":1787252819000}');
const _sfc_main = { name: "guide/key-concepts.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="key-concepts" tabindex="-1">Key concepts <a class="header-anchor" href="#key-concepts" aria-label="Permalink to &quot;Key concepts&quot;">​</a></h1><p>These are the terms you will see across the dashboard, the API, and these docs. Each is a deliberate design decision; understanding them makes the rest of the system predictable.</p><h2 id="login-a-vs-login-b" tabindex="-1">Login A vs Login B <a class="header-anchor" href="#login-a-vs-login-b" aria-label="Permalink to &quot;Login A vs Login B&quot;">​</a></h2><p>Amazon King has two completely separate logins. <strong>Login A</strong> is how you sign in to the app: passwordless email (a magic link), an <code>HttpOnly</code>, <code>SameSite=Lax</code> session cookie (<code>ak_session</code>) with a rolling 7-day lifetime, CSRF protection, and rate limiting. <strong>Login B</strong> is the OAuth connection from the server to your Amazon Ads account (Login With Amazon, scope <code>advertising::campaign_management</code>). The app session never contains an Amazon token, and Amazon credentials are never sent to the browser.</p><p><em>Why it matters:</em> compromising your dashboard session does not leak Amazon credentials, and disconnecting Amazon does not lock you out of the app. See <a href="/amazon-king/guide/connecting-amazon">Connecting Amazon Ads</a> and the <a href="/amazon-king/architecture/security">security model</a>.</p><h2 id="profiles-and-marketplaces" tabindex="-1">Profiles and marketplaces <a class="header-anchor" href="#profiles-and-marketplaces" aria-label="Permalink to &quot;Profiles and marketplaces&quot;">​</a></h2><p>An Amazon Ads <strong>profile</strong> maps to one marketplace (country) of your ad account — Amazon.com (US), Amazon.co.uk (UK), and so on. Profiles are discovered automatically after you connect. Each profile has two owner-controlled switches: <code>enabled</code> (include it in syncs and analysis) and <code>writeEnabled</code> (allow guarded writes against it). Both are opt-in.</p><p><em>Why it matters:</em> you can import and analyze a marketplace for weeks before trusting the system to write to it, and every monetary figure stays in the profile&#39;s own currency — Amazon King never aggregates across currencies.</p><h2 id="syncs" tabindex="-1">Syncs <a class="header-anchor" href="#syncs" aria-label="Permalink to &quot;Syncs&quot;">​</a></h2><p>The worker keeps a local mirror of your Amazon account in PostgreSQL. <strong>Structure syncs</strong> import campaigns, ad groups, targets, and ads every 45 minutes per enabled profile. <strong>Metrics syncs</strong> import performance reports once daily after 05:00 UTC (when Amazon&#39;s data for yesterday has settled), followed by a trailing 14-day <strong>recent-window re-sync</strong> to absorb attribution lag. Metrics cover four report families: campaigns, targeting, search terms, and advertised products. A self-rescheduling <code>schedule_tick</code> job (every 15 minutes) enqueues whatever is due; a successful metrics sync chains a recommendation run.</p><p><em>Why it matters:</em> the dashboard and the optimizer always read local, validated data — no waiting on Amazon&#39;s API, and every import is idempotent and reconciled before it is marked complete. See the <a href="/amazon-king/architecture/data-pipeline">data pipeline</a>.</p><h2 id="the-gateway-boundary" tabindex="-1">The gateway boundary <a class="header-anchor" href="#the-gateway-boundary" aria-label="Permalink to &quot;The gateway boundary&quot;">​</a></h2><p>Every call to the Amazon Ads API goes through a single internal <code>AmazonAdsGateway</code> interface. Amazon payloads are strictly validated (zod schemas) and translated into internal domain models at the boundary; the optimizer never sees raw Amazon field names. The gateway also owns token refresh, regional routing, <code>Retry-After</code> handling, and backoff.</p><p><em>Why it matters:</em> when Amazon changes an API response, exactly one layer breaks — with a clear error — instead of corrupting facts downstream. See <a href="/amazon-king/architecture/overview">system overview</a>.</p><h2 id="recommendations" tabindex="-1">Recommendations <a class="header-anchor" href="#recommendations" aria-label="Permalink to &quot;Recommendations&quot;">​</a></h2><p>A <strong>recommendation</strong> is one deterministic finding from one of the nine optimization rules: a wasteful search term to negate, an expensive target to bid down, a profitable target to bid up, a search term worth harvesting, and so on. Each carries a <strong>priority</strong> (P1–P5, where P1 is most urgent), a <strong>confidence</strong> score (0–1, derived from evidence volume and smoothed conversion rates), the <strong>evidence window</strong> (the exact 7/14/30/60-day date range the rule ran over), and an <strong>expiry</strong> — when the underlying data goes stale, the recommendation expires rather than acting on old numbers.</p><p><em>Why it matters:</em> you can audit exactly why anything was suggested, rules can&#39;t fire on thin data, and nothing lingers past its shelf life. See <a href="/amazon-king/guide/recommendations">reviewing recommendations</a> and the <a href="/amazon-king/reference/optimization-rules">optimization rules reference</a>.</p><h2 id="change-sets" tabindex="-1">Change sets <a class="header-anchor" href="#change-sets" aria-label="Permalink to &quot;Change sets&quot;">​</a></h2><p>A <strong>change set</strong> is an immutable, human-approved bundle of writes to Amazon. Kinds: <code>recommendation</code> (created by approving recommendations), <code>max_cpc</code> (bulk bid ceilings), <code>rollback</code> (a compensating action for a previous set), <code>campaign_creation</code> (creating a new campaign and its contents), and <code>campaign_update</code> (one-click pause/enable or rename from the campaign page). A change set moves through statuses: <code>draft</code> → <code>previewed</code> → <code>applying</code> → <code>applied</code>, <code>partially_applied</code>, <code>failed</code>, or <code>blocked</code>. Applying re-reads Amazon state, compares it against the stored before-snapshot, re-checks guardrails, applies item by item (handling Amazon&#39;s per-item 207 results), and verifies with a post-write re-read.</p><p><em>Why it matters:</em> there is no &quot;edit Amazon&quot; button anywhere — every write is an inspectable, auditable object with a lifecycle, and a stale before-state blocks the apply instead of blindly overwriting. See <a href="/amazon-king/guide/applying-changes">applying &amp; rolling back changes</a>.</p><h2 id="guardrails" tabindex="-1">Guardrails <a class="header-anchor" href="#guardrails" aria-label="Permalink to &quot;Guardrails&quot;">​</a></h2><p><strong>Guardrails</strong> are hard limits re-checked at apply time, independent of the rules: bid changes are clamped to ±15% per cooldown period, per-book ceilings you set (max bid, max daily budget, max spend without a sale) are enforced, and entities on your protected lists (<code>protectedCampaignIds</code>, <code>protectedSearchTerms</code> — empty by default) are left alone. Recently changed targets are also skipped while their bid cooldown runs. Violations block the affected items, not silently adjust them.</p><p><em>Why it matters:</em> even if a rule misfires or your data is misleading, the guardrails bound how much damage one approved change set can do. See the <a href="/amazon-king/reference/optimization-rules">optimization rules reference</a> for the exact thresholds.</p><h2 id="the-kill-switch" tabindex="-1">The kill switch <a class="header-anchor" href="#the-kill-switch" aria-label="Permalink to &quot;The kill switch&quot;">​</a></h2><p><code>KILL_SWITCH</code> is a global, environment-level flag that disables every Amazon write immediately. It defaults to <code>true</code> — writes fail closed until you explicitly set it to <code>false</code>, and per-profile <code>writeEnabled</code> is a second, independent gate on top of it.</p><p><em>Why it matters:</em> it is the incident-response lever. If anything looks wrong, one environment change stops all writes without touching the database or the Amazon connection. See the <a href="/amazon-king/guide/operations">operations runbook</a>.</p><h2 id="book-economics" tabindex="-1">Book economics <a class="header-anchor" href="#book-economics" aria-label="Permalink to &quot;Book economics&quot;">​</a></h2><p><strong>Book economics</strong> are the KDP numbers Amazon never sees, entered per book and marketplace, effective-dated: list price, <strong>estimated royalty per sale</strong>, <strong>target ACoS</strong> (a 0–1 fraction of revenue, not author profit), optional spend/bid/budget ceilings, and a <strong>goal mode</strong>:</p><ul><li><code>profit</code> — optimize for estimated ad profit within target ACoS.</li><li><code>balanced</code> — weigh profit against volume.</li><li><code>launch</code> — tolerate higher spend to gather data and velocity on a new title.</li><li><code>visibility</code> — maximize impressions.</li></ul><p>Currently only <code>launch</code> changes optimizer behavior — it suppresses the spend-cutting rules while the title gathers data. <code>profit</code>, <code>balanced</code>, and <code>visibility</code> are recorded but treated identically by the rules for now.</p><p><em>Why it matters:</em> profit recommendations are <em>disabled, not guessed</em>, when economics are missing — the dashboard shows an amber banner instead of inventing a royalty. See the <a href="/amazon-king/guide/book-economics">book economics guide</a>.</p><h2 id="acos-vs-estimated-ad-profit" tabindex="-1">ACoS vs estimated ad profit <a class="header-anchor" href="#acos-vs-estimated-ad-profit" aria-label="Permalink to &quot;ACoS vs estimated ad profit&quot;">​</a></h2><p><strong>ACoS</strong> is ad spend divided by attributed retail revenue — Amazon&#39;s metric, computed over what shoppers paid. <strong>Estimated ad profit</strong> is what you actually care about: <code>max(orders, units) × royalty per sale − ad cost</code> — royalty is earned per copy sold, so a three-copy order earns three royalties. Because KDP royalty rates (35%/70% ebook, fixed minus printing cost for print) mean you keep only part of each retail dollar, a &quot;good&quot; ACoS can still be unprofitable and a &quot;high&quot; ACoS can be fine.</p><p><em>Why it matters:</em> it is the core reason Amazon King exists. Every KPI, chart, and profit-based rule works in estimated ad profit once you&#39;ve entered book economics — and refuses to guess when you haven&#39;t.</p><h2 id="where-to-go-next" tabindex="-1">Where to go next <a class="header-anchor" href="#where-to-go-next" aria-label="Permalink to &quot;Where to go next&quot;">​</a></h2><ul><li><a href="/amazon-king/guide/quickstart">Quickstart</a> — see these concepts in a working session</li><li><a href="/amazon-king/architecture/overview">Architecture overview</a> — how the pieces are deployed</li><li><a href="/amazon-king/reference/api">HTTP API reference</a> — the exact endpoints and payloads</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/key-concepts.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const keyConcepts = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  keyConcepts as default
};
