import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Error Codes","description":"Every error code returned by the amazon-king API — the error envelope, HTTP status grouping, OAuth redirect errors, and Amazon-side adapter errors.","frontmatter":{"title":"Error Codes","description":"Every error code returned by the amazon-king API — the error envelope, HTTP status grouping, OAuth redirect errors, and Amazon-side adapter errors."},"headers":[],"relativePath":"reference/errors.md","filePath":"reference/errors.md","lastUpdated":1787307183000}');
const _sfc_main = { name: "reference/errors.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="error-codes" tabindex="-1">Error Codes <a class="header-anchor" href="#error-codes" aria-label="Permalink to &quot;Error Codes&quot;">​</a></h1><h2 id="error-envelope" tabindex="-1">Error envelope <a class="header-anchor" href="#error-envelope" aria-label="Permalink to &quot;Error envelope&quot;">​</a></h2><p>Every non-2xx API response carries the same JSON envelope:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;error&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;code&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;STALE_BEFORE_STATE&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;message&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;…human-readable explanation…&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;details&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><ul><li><code>code</code> — stable machine-readable identifier (this page).</li><li><code>message</code> — human-readable explanation; safe to show but not to match on.</li><li><code>details</code> — optional structured context (e.g. zod issues for <code>VALIDATION_ERROR</code>).</li></ul><p>The response also echoes the <code>x-request-id</code> header for log correlation.</p><h2 id="_400-—-bad-request" tabindex="-1">400 — Bad request <a class="header-anchor" href="#_400-—-bad-request" aria-label="Permalink to &quot;400 — Bad request&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>BAD_REQUEST</code></td><td>The request is malformed beyond schema validation (e.g. an empty <code>recommendationIds</code> list).</td><td>Client sent a semantically empty request.</td><td>Fix the payload; see the endpoint in <a href="/amazon-king/reference/api">API reference</a>.</td></tr><tr><td><code>VALIDATION_ERROR</code></td><td>The body or query failed its zod schema. <code>details.issues</code> holds the zod issue list.</td><td>Wrong types, bad money/date format, missing required fields.</td><td>Inspect <code>details.issues</code>; money is a string decimal <code>/^-?\\d+(\\.\\d{1,4})?$/</code>, dates are <code>YYYY-MM-DD</code>.</td></tr></tbody></table><h2 id="_401-—-authentication" tabindex="-1">401 — Authentication <a class="header-anchor" href="#_401-—-authentication" aria-label="Permalink to &quot;401 — Authentication&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>UNAUTHENTICATED</code></td><td>No valid session.</td><td>Missing/expired <code>ak_session</code> cookie.</td><td>Sign in via <code>POST /api/session/login</code> + the magic link.</td></tr><tr><td><code>REAUTH_REQUIRED</code></td><td>The action changes spend and requires a sign-in within the last 15 minutes.</td><td>Session is older than the recent-auth window.</td><td>Log in again, then retry. Retrying a <code>failed</code> change set is exempt.</td></tr></tbody></table><h2 id="_403-—-forbidden" tabindex="-1">403 — Forbidden <a class="header-anchor" href="#_403-—-forbidden" aria-label="Permalink to &quot;403 — Forbidden&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>CSRF_MISMATCH</code></td><td>The <code>x-csrf-token</code> header is missing or does not match the session-derived token.</td><td>Stale session info, or a client that did not echo <code>csrfToken</code> from <code>GET /api/session</code>.</td><td>Fetch <code>GET /api/session</code> and send its <code>csrfToken</code> on every POST/PATCH/DELETE.</td></tr><tr><td><code>WRITES_DISABLED</code></td><td>Amazon writes are blocked.</td><td>The global kill switch is on (<code>KILL_SWITCH</code> not exactly <code>&quot;false&quot;</code>), or the profile has <code>writeEnabled: false</code>.</td><td>Unset the kill switch only after live validation; enable writes per profile via <code>PATCH /api/profiles/:profileId</code>.</td></tr></tbody></table><h2 id="_404-—-not-found" tabindex="-1">404 — Not found <a class="header-anchor" href="#_404-—-not-found" aria-label="Permalink to &quot;404 — Not found&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>NOT_FOUND</code></td><td>The addressed entity does not exist in this workspace.</td><td>Unknown campaign, recommendation, sync run, change action, book, or search term.</td><td>Re-list the parent collection; ids may have rotated after a re-sync.</td></tr></tbody></table><h2 id="_409-—-conflict" tabindex="-1">409 — Conflict <a class="header-anchor" href="#_409-—-conflict" aria-label="Permalink to &quot;409 — Conflict&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>INVALID_STATE</code></td><td>The entity is in a state that forbids the operation.</td><td>Drafting a change set from a recommendation that is not <code>pending</code>.</td><td>Only <code>pending</code> recommendations can enter a change set.</td></tr><tr><td><code>RECOMMENDATION_EXPIRED</code></td><td>The recommendation&#39;s evidence went stale (expires 3 days after creation).</td><td>Acting on an old finding.</td><td>Wait for the next <code>recommendation_run</code> (after the next complete metrics sync) to regenerate fresh findings.</td></tr><tr><td><code>RECOMMENDATION_NOT_WRITABLE</code></td><td>The recommendation type is advisory-only.</td><td>Trying to create a change set from e.g. <code>search_term_harvest</code>, <code>budget_constrained_winner</code>, a diagnostic, or <code>cannibalization_conflict</code>.</td><td>Only <code>wasteful_search_term</code>, <code>expensive_target</code>, and <code>profitable_target</code> produce automatic actions; resolve others manually or via the dedicated flows.</td></tr><tr><td><code>INVALID_RECOMMENDATION_TYPE</code></td><td>A type-specific endpoint received the wrong type.</td><td>Calling the cannibalization resolution endpoint on a non-cannibalization recommendation.</td><td>Use the endpoint matching the recommendation&#39;s <code>type</code>.</td></tr><tr><td><code>INCOMPLETE_EVIDENCE</code></td><td>The finding&#39;s stored evidence is incomplete for the requested resolution.</td><td>Cannibalization resolution where an affected campaign is no longer present in the finding&#39;s profile.</td><td>Re-sync structure, then wait for a fresh finding.</td></tr><tr><td><code>STALE_BEFORE_STATE</code></td><td>The fresh re-read of Amazon state does not match the recorded before-state.</td><td>Someone changed the entity in the Amazon console between preview and apply.</td><td>Nothing was written for that action; re-preview the change set and apply again.</td></tr><tr><td><code>GUARDRAIL_VIOLATION</code></td><td>One or more guardrails failed at apply time.</td><td>Bid change too large, cooldown active, stale evidence, protected entity, exposure/budget limits.</td><td>The <code>details</code>/preview list the violations (<code>BID_CHANGE_TOO_LARGE</code>, <code>BID_COOLDOWN_ACTIVE</code>, <code>STALE_EVIDENCE</code>, <code>PROTECTED_ENTITY</code>, …). Adjust the set or wait out the cooldown.</td></tr><tr><td><code>MAX_CPC_EXCEEDED</code></td><td>The proposed bid exceeds the campaign&#39;s enforced Max CPC ceiling.</td><td>Applying a bid-up recommendation on a campaign with a Max CPC policy.</td><td>Raise the ceiling (<code>POST /api/campaigns/:campaignId/max-cpc</code>) or drop the action.</td></tr><tr><td><code>APPLY_IN_PROGRESS</code></td><td>The change set is already being applied.</td><td>Concurrent apply of the same set.</td><td>Poll <code>GET /api/change-sets</code> until the set leaves <code>applying</code>.</td></tr><tr><td><code>CHANGE_SET_BLOCKED</code></td><td>The change set is in <code>blocked</code> state and cannot be applied.</td><td>An earlier validation failure marked it blocked.</td><td>Draft a new change set.</td></tr><tr><td><code>DEPENDENCY_NOT_APPLIED</code></td><td>The set depends on another change set that is not <code>applied</code> yet.</td><td>Applying cannibalization negatives before their destination campaign creation set finished.</td><td>Apply the referenced creation set first (<code>dependsOnChangeSetId</code>).</td></tr><tr><td><code>NOT_ROLLBACKABLE</code></td><td>The action has no verified compensating Amazon operation.</td><td>Rolling back an action that is not <code>applied</code>, a campaign-creation chain, or a negative ASIN target.</td><td>Rollback only covers applied <code>update_bid</code> and verified <code>add_negative_exact</code> actions; undo anything else manually in the Amazon console.</td></tr><tr><td><code>TOO_MANY_ACTIONS</code></td><td>The change set exceeds the action limit.</td><td>More than 20 actions in one set, or a Max CPC submission on a campaign with more than 5,000 bid controls.</td><td>Split the work into smaller sets or smaller campaigns.</td></tr><tr><td><code>BOOK_PROFILE_NOT_LINKED</code></td><td>The book is not linked to the requested profile.</td><td>Saving economics or creating a campaign for a profile the book&#39;s ASIN is not mapped to.</td><td>Link the ASIN first (<code>POST /api/books/:bookId/profile-links</code>, or <code>POST /api/books/mappings</code> for advertised ASINs).</td></tr><tr><td><code>ASIN_ALREADY_LINKED</code></td><td>Another catalog book already uses this ASIN in one of the selected markets.</td><td>Linking a marketplace ASIN that is already claimed.</td><td>Use the book that already owns the ASIN, or pick a different ASIN.</td></tr><tr><td><code>BOOK_PROFILE_ASIN_MISMATCH</code></td><td>The book is already linked to a selected profile with a different ASIN.</td><td>Retrying a marketplace link with a new ASIN.</td><td>Keep the existing marketplace ASIN, or use a market that is not yet linked.</td></tr><tr><td><code>PROFILE_DISABLED</code></td><td>The profile is disabled.</td><td>Requesting a sync for a profile with <code>enabled: false</code>.</td><td>Enable the profile via <code>PATCH /api/profiles/:profileId</code>.</td></tr><tr><td><code>MIXED_CURRENCY</code></td><td>A view would aggregate money across currencies.</td><td>Dashboard/search-term query spanning marketplaces with different currencies.</td><td>Narrow the query with the <code>country</code> parameter — or use <code>country=all</code> on the dashboard summary, which converts explicitly.</td></tr><tr><td><code>FX_RATES_INCOMPLETE</code></td><td>A requested currency conversion lacks stored FX rates for part of the window.</td><td><code>country=all</code> (or an explicit <code>currency</code>) on <code>GET /api/dashboard/summary</code> while <code>fx_rates</code> does not cover every fact date.</td><td>Wait for the daily <code>fx_sync</code> job (after 17:00 UTC) or check its health on the overview&#39;s Sync status card; nothing unconverted is ever returned.</td></tr></tbody></table><h2 id="_429-—-rate-limited" tabindex="-1">429 — Rate limited <a class="header-anchor" href="#_429-—-rate-limited" aria-label="Permalink to &quot;429 — Rate limited&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>RATE_LIMITED</code></td><td>The per-minute tier for the route was exceeded (global 200/min; STRICT 10/min; WRITE 20/min; PREVIEW 120/min).</td><td>Burst of writes or previews.</td><td>Back off and retry; the tiers reset each minute.</td></tr></tbody></table><h2 id="_502-—-amazon-write-failed" tabindex="-1">502 — Amazon write failed <a class="header-anchor" href="#_502-—-amazon-write-failed" aria-label="Permalink to &quot;502 — Amazon write failed&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>AMAZON_APPLY_FAILED</code></td><td>The Amazon call behind an apply failed before a usable result came back.</td><td>Amazon outage, unexpected transport failure.</td><td>The set is <code>failed</code>; retrying it is safe (idempotency fingerprints prevent duplicates) and does not require recent re-auth.</td></tr></tbody></table><h3 id="per-action-failure-codes" tabindex="-1">Per-action failure codes <a class="header-anchor" href="#per-action-failure-codes" aria-label="Permalink to &quot;Per-action failure codes&quot;">​</a></h3><p>When a set applies partially, individual failed actions carry one of these codes in their result payload (the set ends <code>partially_applied</code> or <code>failed</code>):</p><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th></tr></thead><tbody><tr><td><code>AMAZON_HTTP_&lt;status&gt;</code></td><td>Amazon returned that HTTP status (e.g. <code>AMAZON_HTTP_400</code>); <code>details</code> holds the sanitized Amazon response and <code>amazonRequestId</code> the support trace id.</td></tr><tr><td><code>AMAZON_RESPONSE_INVALID</code></td><td>Amazon&#39;s response did not match the adapter schema (missing/wrong-typed required fields).</td></tr><tr><td><code>AMAZON_NETWORK_ERROR</code></td><td>Network-level failure (DNS, socket, timeout) before any HTTP status.</td></tr><tr><td><code>PARENT_FAILED</code></td><td>A creation-chain action was skipped because an earlier phase (e.g. the campaign) failed, orphaning the dependent action.</td></tr><tr><td><code>MISSING_RESULT</code></td><td>Amazon&#39;s batch response contained no result entry for a requested entity (adapter-level).</td></tr><tr><td><code>NO_RESULT</code></td><td>The apply pipeline found no per-item result to record for an action.</td></tr><tr><td><code>ALREADY_PRESENT</code> / <code>ALREADY_ABSENT</code></td><td>Verification found the desired end state already in place (e.g. the negative already exists); treated as satisfied, not an error.</td></tr></tbody></table><h2 id="_5xx-—-server-errors" tabindex="-1">5xx — Server errors <a class="header-anchor" href="#_5xx-—-server-errors" aria-label="Permalink to &quot;5xx — Server errors&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>Typical cause</th><th>What to do</th></tr></thead><tbody><tr><td><code>INTERNAL</code></td><td>Unhandled server-side failure.</td><td>Database outage, invariant violation.</td><td>Check the api logs with the echoed <code>x-request-id</code>; report persistent cases with the id.</td></tr></tbody></table><p>Unexpected non-API errors below 500 (e.g. malformed JSON bodies rejected by the framework) use the generic code <code>REQUEST_ERROR</code> with the original status.</p><h2 id="oauth-redirect-errors" tabindex="-1">OAuth redirect errors <a class="header-anchor" href="#oauth-redirect-errors" aria-label="Permalink to &quot;OAuth redirect errors&quot;">​</a></h2><p>The OAuth callback (<code>GET /api/integrations/amazon/callback</code>) and session verify never return JSON — failures redirect the browser instead.</p><p><code>{WEB_ORIGIN}/connect?error=&lt;code&gt;</code>:</p><table tabindex="0"><thead><tr><th>Code</th><th>Meaning</th><th>What to do</th></tr></thead><tbody><tr><td><code>invalid_callback</code></td><td>The callback query was malformed (no usable <code>state</code>/<code>code</code>).</td><td>Restart the connection from the dashboard.</td></tr><tr><td><code>invalid_state</code></td><td>The state is unknown, expired (10-minute TTL), or already used (single-use, marked used before code exchange).</td><td>Restart the connection; do not reuse or bookmark the Amazon URL.</td></tr><tr><td><code>session_required</code></td><td>No valid app session accompanied the callback.</td><td>Sign in first, then connect Amazon.</td></tr><tr><td><code>foreign_state</code></td><td>The state belongs to a different user/session.</td><td>Sign in as the same user who started the connection and retry.</td></tr><tr><td><code>exchange_failed</code></td><td>The authorization-code exchange with LWA failed.</td><td>Retry; check <code>LWA_CLIENT_ID</code>/<code>LWA_CLIENT_SECRET</code> and that <code>AMAZON_REDIRECT_URI</code> matches the LWA app registration exactly.</td></tr><tr><td><code>profile_discovery_failed</code></td><td>Token exchange succeeded but the profile list could not be fetched/imported.</td><td>Check the api logs; verify the Amazon account has Advertising profiles.</td></tr></tbody></table><p>Login verify failures redirect to <code>{WEB_ORIGIN}/login?error=invalid_token</code>: the magic link is unknown, expired (15-minute TTL), or already consumed — request a fresh link.</p><h2 id="amazon-side-adapter-errors" tabindex="-1">Amazon-side adapter errors <a class="header-anchor" href="#amazon-side-adapter-errors" aria-label="Permalink to &quot;Amazon-side adapter errors&quot;">​</a></h2><p>These originate in <code>packages/amazon-ads</code> and surface in api/worker logs and connection status rather than as API response codes.</p><table tabindex="0"><thead><tr><th>Error / code</th><th>Meaning</th><th>Consequence</th></tr></thead><tbody><tr><td><code>AmazonAuthError</code> <code>invalid_grant</code>, <code>revoked</code>, <code>unauthorized_client</code></td><td>The refresh grant is dead (unrecoverable).</td><td>The connection is marked <code>reconnect_required</code> and its pending jobs are dead-lettered; the owner must reconnect Amazon.</td></tr><tr><td><code>AmazonAuthError</code> <code>reconnect_required</code></td><td>Token refresh attempted on a connection already marked reconnect-required, or the circuit breaker is open.</td><td>Jobs fail fast until the owner reconnects.</td></tr><tr><td><code>AmazonAuthError</code> <code>missing_lwa_credentials</code></td><td>The worker has no <code>LWA_CLIENT_ID</code>/<code>LWA_CLIENT_SECRET</code>.</td><td>Recoverable: jobs needing tokens fail until the credentials are configured. No reconnect is forced.</td></tr><tr><td><code>AmazonAuthError</code> <code>token_request_failed</code>, <code>invalid_token_response</code>, <code>missing_refresh_token</code></td><td>Transient or malformed LWA token responses.</td><td>Retried with backoff by the token manager.</td></tr><tr><td><code>AmazonApiError</code></td><td>Amazon returned an HTTP error status; carries <code>status</code>, <code>requestId</code>, <code>retryable</code>, sanitized <code>details</code>.</td><td>429 honors <code>Retry-After</code>; other retryables use exponential backoff with full jitter.</td></tr><tr><td><code>AmazonNetworkError</code></td><td>DNS/socket/timeout before any HTTP status; always retryable.</td><td>Retried with backoff.</td></tr><tr><td><code>AdapterValidationError</code></td><td>An Amazon payload failed strict schema validation at the gateway boundary; carries <code>context</code> and <code>issues</code>.</td><td>The sync/job fails loudly instead of importing corrupt data.</td></tr></tbody></table><h2 id="related-reading" tabindex="-1">Related reading <a class="header-anchor" href="#related-reading" aria-label="Permalink to &quot;Related reading&quot;">​</a></h2><ul><li><a href="/amazon-king/reference/api">API reference</a> — endpoint-by-endpoint error listings</li><li><a href="/amazon-king/troubleshooting">Troubleshooting</a> — operational fixes</li><li><a href="/amazon-king/architecture/security">Architecture: security</a> — the guarded-write design behind these codes</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/errors.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const errors = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  errors as default
};
