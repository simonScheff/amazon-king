import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"MCP Server","description":"","frontmatter":{},"headers":[],"relativePath":"reference/mcp-server.md","filePath":"reference/mcp-server.md","lastUpdated":1787865254000}');
const _sfc_main = { name: "reference/mcp-server.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="mcp-server" tabindex="-1">MCP Server <a class="header-anchor" href="#mcp-server" aria-label="Permalink to &quot;MCP Server&quot;">​</a></h1><p>amazon-king ships an <a href="https://modelcontextprotocol.io" target="_blank" rel="noreferrer">MCP</a> server (<code>apps/mcp</code>) so any MCP-compatible AI agent — Claude, Kimi, Cursor, or your own scripts — can query your advertising data and the optimizer&#39;s research directly: campaign performance, search-term analysis, the negatives inventory, recommendations with their evidence, and sync freshness.</p><p>The server is <strong>read-only by design</strong>. It has no apply, rollback, or sync tools; applying a change always requires you in the <a href="./../guide/applying-changes">dashboard</a> with a recent sign-in. Agents get the same numbers the dashboard shows because both are served by the same read layer.</p><h2 id="running-it" tabindex="-1">Running it <a class="header-anchor" href="#running-it" aria-label="Permalink to &quot;Running it&quot;">​</a></h2><h3 id="local-agents-stdio-default" tabindex="-1">Local agents (stdio, default) <a class="header-anchor" href="#local-agents-stdio-default" aria-label="Permalink to &quot;Local agents (stdio, default)&quot;">​</a></h3><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> mcp</span></span></code></pre></div><p>Register the server in your agent client, e.g.:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">claude</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> mcp</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> amazon-king</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> make</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -C</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /path/to/amazon-king</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> mcp</span></span></code></pre></div><p><code>make mcp</code> loads your <code>.env</code> and starts the server quietly over stdio — do not register <code>pnpm --filter @amazon-king/mcp start</code> directly, because the pnpm script banner would corrupt the stdio protocol.</p><p>stdio needs no tokens or network exposure — the client launches the process and talks to it over standard I/O. <code>DATABASE_URL</code> from your <code>.env</code> is the only requirement.</p><h3 id="remote-agents-streamable-http" tabindex="-1">Remote agents (Streamable HTTP) <a class="header-anchor" href="#remote-agents-streamable-http" aria-label="Permalink to &quot;Remote agents (Streamable HTTP)&quot;">​</a></h3><ol><li><p>Set <code>MCP_TRANSPORT=http</code> (optionally <code>MCP_HOST</code> / <code>MCP_PORT</code>, default <code>127.0.0.1:3100</code>).</p></li><li><p>Issue a machine token — the plaintext is shown once:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> tsx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> scripts/mcp-token.ts</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> issue</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> my-agent</span></span></code></pre></div></li><li><p>Point the client at <code>http://127.0.0.1:3100/mcp</code> with an <code>Authorization: Bearer &lt;token&gt;</code> header.</p></li></ol><p>Manage tokens with <code>scripts/mcp-token.ts list</code> and <code>scripts/mcp-token.ts revoke &lt;id&gt;</code>. Only SHA-256 hashes are stored (<code>api_tokens</code> table), every token is limited to 120 requests per minute, and every tool call is written to the <a href="./../guide/operations">audit log</a>. To expose the endpoint beyond localhost, put it behind your own HTTPS-terminating reverse proxy.</p><h2 id="tools" tabindex="-1">Tools <a class="header-anchor" href="#tools" aria-label="Permalink to &quot;Tools&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>What it returns</th></tr></thead><tbody><tr><td><code>list_profiles</code></td><td>Connected Amazon Ads profiles (marketplace, currency, write-enabled flag).</td></tr><tr><td><code>get_dashboard_summary</code></td><td>Overview KPIs and daily trend for a window; <code>country=&quot;all&quot;</code> gives the FX-converted all-market view.</td></tr><tr><td><code>get_country_spend</code></td><td>Spend per marketplace, optionally converted to a display currency.</td></tr><tr><td><code>list_campaigns</code></td><td>Campaigns with metrics for a window.</td></tr><tr><td><code>get_campaign</code></td><td>One campaign: metrics, trend, ad groups, targets, negatives.</td></tr><tr><td><code>list_search_terms</code></td><td>Shopper search terms with aggregated performance — the main research surface.</td></tr><tr><td><code>get_search_term</code></td><td>One term broken down by campaign, with trend and negative coverage.</td></tr><tr><td><code>list_negatives</code></td><td>Synced negative keywords and product ASINs: blocking coverage, still-serving leaks, and whether the value sold before we first saw it.</td></tr><tr><td><code>get_negative</code></td><td>One negative: search-term evidence, campaigns it runs on, and campaigns that still serve it.</td></tr><tr><td><code>list_books</code></td><td>Books with KDP economics and advertised-product mappings.</td></tr><tr><td><code>list_recommendations</code></td><td>Optimizer findings filterable by type and state, with rationale and evidence windows.</td></tr><tr><td><code>get_recommendation</code></td><td>One finding in full, including cannibalization or conversion context.</td></tr><tr><td><code>list_change_sets</code></td><td>Guarded change sets and their status (read-only view of the write pipeline).</td></tr><tr><td><code>get_sync_status</code></td><td>Recent sync runs and per-dataset data freshness.</td></tr></tbody></table><p>All monetary values are decimal strings in the marketplace&#39;s native currency, and ACoS is ad spend over ad-attributed retail revenue — not author profit. Profit figures exist only where <a href="./../guide/book-economics">book economics</a> are entered. Tool descriptions tell agents the same, and <code>get_sync_status</code> lets an agent state how fresh its answers are.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/mcp-server.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const mcpServer = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  mcpServer as default
};
