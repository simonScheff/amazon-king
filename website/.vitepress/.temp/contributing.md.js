import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Contributing","description":"How to contribute to amazon-king — dev setup, repository layout, testing layers, style rules, DCO sign-off, and pull request expectations.","frontmatter":{"title":"Contributing","description":"How to contribute to amazon-king — dev setup, repository layout, testing layers, style rules, DCO sign-off, and pull request expectations."},"headers":[],"relativePath":"contributing.md","filePath":"contributing.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "contributing.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="contributing" tabindex="-1">Contributing <a class="header-anchor" href="#contributing" aria-label="Permalink to &quot;Contributing&quot;">​</a></h1><p>Thank you for helping improve amazon-king. The project is an early alpha and prioritizes advertiser safety, deterministic behavior, and data privacy over feature breadth. This page adapts the repository&#39;s <code>CONTRIBUTING.md</code> and <code>AGENTS.md</code>; when in doubt, those files are authoritative.</p><h2 id="before-you-start" tabindex="-1">Before you start <a class="header-anchor" href="#before-you-start" aria-label="Permalink to &quot;Before you start&quot;">​</a></h2><ul><li>Read the <code>README.md</code>, the specification in <code>docs/plan.md</code>, and <code>AGENTS.md</code> at the repository root. <code>docs/plan.md</code> is the authoritative spec — read it before changing behavior.</li><li>Discuss large behavioral or architectural changes in an issue before writing them.</li><li>Never attach real Amazon Ads data, credentials, report URLs, account IDs, or unsanitized logs to an issue or pull request.</li><li>Do not test write operations against real campaigns. Tests must use injected fakes, synthetic fixtures, or an Amazon-provided test environment.</li></ul><h2 id="development-setup" tabindex="-1">Development setup <a class="header-anchor" href="#development-setup" aria-label="Permalink to &quot;Development setup&quot;">​</a></h2><p>Requirements: Node.js 20 or newer, pnpm 10, Docker, and GNU Make.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> setup</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # install dependencies, create .env from .env.example</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> db-up</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # start PostgreSQL via docker compose</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">make</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> migrate</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # apply SQL migrations</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> check</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # typecheck + lint + tests across the workspace</span></span></code></pre></div><p><code>make run</code> goes further and starts the full app (API on :3000, worker, web on :5173). Other Make targets: <code>db-up</code>, <code>migrate</code>, <code>test</code>, <code>typecheck</code>, <code>lint</code>, <code>build</code>, <code>stop</code>, <code>clean</code>. See the <a href="/amazon-king/reference/commands">command reference</a>.</p><h2 id="repository-layout" tabindex="-1">Repository layout <a class="header-anchor" href="#repository-layout" aria-label="Permalink to &quot;Repository layout&quot;">​</a></h2><p>The monorepo is a modular monolith: the API and worker are separate processes but one deployable product.</p><table tabindex="0"><thead><tr><th>Path</th><th>Package</th><th>Purpose</th></tr></thead><tbody><tr><td><code>apps/web</code></td><td><code>@amazon-king/web</code></td><td>React + Vite dashboard (TanStack Router/Query, Tailwind)</td></tr><tr><td><code>apps/api</code></td><td><code>@amazon-king/api</code></td><td>Fastify backend: auth, OAuth, read routes, guarded change service</td></tr><tr><td><code>apps/worker</code></td><td><code>@amazon-king/worker</code></td><td>Job loop, sync pipeline, recommendation runs</td></tr><tr><td><code>packages/amazon-ads</code></td><td><code>@amazon-king/amazon-ads</code></td><td>OAuth client, token manager, regional gateway, API adapters</td></tr><tr><td><code>packages/optimizer</code></td><td><code>@amazon-king/optimizer</code></td><td>Pure, deterministic rules, guardrails, ranking (no I/O)</td></tr><tr><td><code>packages/database</code></td><td><code>@amazon-king/database</code></td><td>SQL migrations, repositories, PostgreSQL job queue</td></tr><tr><td><code>packages/contracts</code></td><td><code>@amazon-king/contracts</code></td><td>Shared zod-validated request/response types</td></tr><tr><td><code>packages/observability</code></td><td><code>@amazon-king/observability</code></td><td>Logging, metrics, error reporting</td></tr><tr><td><code>packages/crypto</code></td><td><code>@amazon-king/crypto</code></td><td>AES-256-GCM envelope encryption for refresh tokens</td></tr><tr><td><code>website/</code></td><td>—</td><td>This documentation site (VitePress)</td></tr></tbody></table><h2 id="testing-layers" tabindex="-1">Testing layers <a class="header-anchor" href="#testing-layers" aria-label="Permalink to &quot;Testing layers&quot;">​</a></h2><p>Match your tests to the layer you changed:</p><ul><li><strong>Unit</strong> — the default for most work: optimizer math and rules at thresholds and edge cases, guardrails and cooldowns, Amazon payload translation, OAuth state validation, token redaction. Run per package with <code>pnpm --filter &lt;pkg&gt; test</code>.</li><li><strong>Contract/fixture</strong> — sanitized Amazon API response fixtures live in <code>packages/amazon-ads/test/fixtures/</code>; adapters must tolerate unknown additive fields and fail clearly on required-field changes. Tests use injected fetch — never the network.</li><li><strong>Integration</strong> — migrations, idempotent upserts, queue lease semantics, serialized token refresh. These run only when <code>TEST_DATABASE_URL</code> points at a <strong>disposable</strong> PostgreSQL database; the suite drops and recreates the <code>public</code> schema, so never point it at data you care about.</li><li><strong>End-to-end</strong> — sign-in, OAuth callback edge cases, the review → preview → apply → verify flow, kill switch, disconnect.</li><li><strong>Live API validation</strong> — a dedicated low-risk campaign, one profile, one manually approved action, small bid. This comes last, after everything above is green, and is a maintainer-led acceptance gate rather than a contributor task.</li></ul><p>API and worker services take injected dependencies; tests use the in-memory <code>FakeDb</code> (<code>apps/api/src/test/fake-db.ts</code>) and fake worker stores — no real database or network is needed for the unit suites.</p><h2 id="style" tabindex="-1">Style <a class="header-anchor" href="#style" aria-label="Permalink to &quot;Style&quot;">​</a></h2><ul><li>Strict TypeScript, formatted with the repository Prettier configuration.</li><li>Run <code>pnpm lint</code> (prettier check) and <code>pnpm check</code> before opening a pull request; <code>pnpm exec prettier --write .</code> fixes formatting.</li><li>Code, comments, and docs are written in English.</li><li>Keep changes minimal and consistent with the surrounding code; validation happens at every API boundary with the shared contract schemas.</li></ul><h2 id="dco-sign-off" tabindex="-1">DCO sign-off <a class="header-anchor" href="#dco-sign-off" aria-label="Permalink to &quot;DCO sign-off&quot;">​</a></h2><p>Every commit must be signed off to certify the Developer Certificate of Origin (<code>DCO.txt</code> at the repository root):</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> commit</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --signoff</span></span></code></pre></div><p>By contributing, you agree that your contribution is licensed under the Apache License 2.0.</p><h2 id="pull-request-expectations" tabindex="-1">Pull request expectations <a class="header-anchor" href="#pull-request-expectations" aria-label="Permalink to &quot;Pull request expectations&quot;">​</a></h2><ul><li>Keep changes focused — one concern per pull request.</li><li>Include tests at the appropriate layer (see above).</li><li>Run <code>pnpm check</code> before opening the pull request; CI runs the full suite with PostgreSQL.</li><li>Update documentation when behavior, configuration, or project status changes — including <code>AGENTS.md</code> when build commands, structure, or conventions change.</li><li>Commit messages should explain the intent of the change.</li></ul><h2 id="project-governance" tabindex="-1">Project governance <a class="header-anchor" href="#project-governance" aria-label="Permalink to &quot;Project governance&quot;">​</a></h2><ul><li><code>CODE_OF_CONDUCT.md</code> — expected behavior in project spaces.</li><li><code>GOVERNANCE.md</code> — how decisions and maintainership work.</li><li><code>SECURITY.md</code> — how to report vulnerabilities (privately, via GitHub security advisories).</li></ul><h2 id="editing-these-docs" tabindex="-1">Editing these docs <a class="header-anchor" href="#editing-these-docs" aria-label="Permalink to &quot;Editing these docs&quot;">​</a></h2><p>The documentation site lives in <code>website/</code> and is built with VitePress. Pages are plain Markdown with YAML frontmatter (<code>title</code> and <code>description</code>); the sidebar and navigation live in <code>website/.vitepress/config.mts</code>.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> website</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">       # local dev server with hot reload</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # production build</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> preview</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # serve the production build locally</span></span></code></pre></div><p>The site deploys to GitHub Pages via GitHub Actions on merge to <code>main</code>, so no manual publishing step is needed — a merged pull request is enough.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("contributing.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const contributing = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  contributing as default
};
