import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Environment Variables","description":"Every environment variable read by the amazon-king API, worker, and crypto package — defaults, validation rules, and production requirements.","frontmatter":{"title":"Environment Variables","description":"Every environment variable read by the amazon-king API, worker, and crypto package — defaults, validation rules, and production requirements."},"headers":[],"relativePath":"reference/environment-variables.md","filePath":"reference/environment-variables.md","lastUpdated":1787307183000}');
const _sfc_main = { name: "reference/environment-variables.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="environment-variables" tabindex="-1">Environment Variables <a class="header-anchor" href="#environment-variables" aria-label="Permalink to &quot;Environment Variables&quot;">​</a></h1><p>Configuration comes from the process environment only — secrets are never read from files committed to the repo or stored per-user. Locally, <code>make setup</code> creates <code>.env</code> from <code>.env.example</code> and the Makefile loads it into every recipe; the production compose stack loads <code>.env.production</code> (see <a href="/amazon-king/guide/self-hosting">Self-hosting</a>).</p><p>Only the <code>api</code> and <code>worker</code> processes and the crypto package read environment variables. The <code>web</code> app has no custom environment — it is a static build that talks to the API on the same origin. The <code>database</code>, <code>optimizer</code>, <code>amazon-ads</code>, <code>contracts</code>, and <code>observability</code> packages read no environment of their own.</p><h2 id="api-process-apps-api" tabindex="-1">API process (<code>apps/api</code>) <a class="header-anchor" href="#api-process-apps-api" aria-label="Permalink to &quot;API process (\`apps/api\`)&quot;">​</a></h2><p>Parsed by a zod schema at boot; invalid values abort startup.</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Required</th><th>Notes</th></tr></thead><tbody><tr><td><code>NODE_ENV</code></td><td><code>development</code></td><td>no</td><td><code>development</code> | <code>test</code> | <code>production</code>. Production turns on the <code>Secure</code> cookie flag and the stricter requirements below.</td></tr><tr><td><code>PORT</code></td><td><code>3000</code></td><td>no</td><td>Positive integer HTTP port.</td></tr><tr><td><code>DATABASE_URL</code></td><td>—</td><td><strong>always</strong></td><td>PostgreSQL connection URL.</td></tr><tr><td><code>SESSION_SECRET</code></td><td>—</td><td><strong>always</strong></td><td>HMAC secret for stateless CSRF tokens. Min 16 chars; in production min 32 chars and must not contain <code>change-me</code>.</td></tr><tr><td><code>WEB_ORIGIN</code></td><td>—</td><td><strong>always</strong></td><td>Dashboard origin (URL). Used for CORS and post-login/OAuth redirects.</td></tr><tr><td><code>LWA_CLIENT_ID</code></td><td>—</td><td><strong>always</strong></td><td>Login With Amazon app credential (Login B).</td></tr><tr><td><code>LWA_CLIENT_SECRET</code></td><td>—</td><td><strong>always</strong></td><td>Never leaves the server.</td></tr><tr><td><code>AMAZON_REDIRECT_URI</code></td><td>—</td><td><strong>always</strong></td><td>URL registered with LWA; must point at this API&#39;s callback route exactly: <code>&lt;api origin&gt;/api/integrations/amazon/callback</code>.</td></tr><tr><td><code>KILL_SWITCH</code></td><td><code>true</code></td><td>no</td><td>Global write kill switch. Only the exact value <code>&quot;false&quot;</code> disables it; unset or any other value keeps all Amazon writes disabled. Fail closed.</td></tr><tr><td><code>TRUST_PROXY</code></td><td><code>false</code></td><td>no</td><td><code>&quot;true&quot;</code> makes Fastify trust <code>X-Forwarded-*</code> headers. Set only behind your own reverse proxy.</td></tr><tr><td><code>OWNER_EMAIL</code></td><td>—</td><td><strong>production</strong></td><td>Single-owner lock: only this email may sign in.</td></tr><tr><td><code>API_PUBLIC_URL</code></td><td>—</td><td><strong>production</strong></td><td>Public base URL of the API; magic login links point here (verify then redirects to <code>WEB_ORIGIN</code>). Falls back to the origin of <code>AMAZON_REDIRECT_URI</code> in dev.</td></tr><tr><td><code>SMTP_HOST</code></td><td>—</td><td><strong>production</strong></td><td>Magic-link delivery. Without SMTP in development, the link is logged and returned as <code>devLoginUrl</code>.</td></tr><tr><td><code>SMTP_PORT</code></td><td><code>587</code></td><td>no</td><td>Integer 1–65535.</td></tr><tr><td><code>SMTP_SECURE</code></td><td><code>false</code></td><td>no</td><td><code>&quot;true&quot;</code> for implicit TLS.</td></tr><tr><td><code>SMTP_USER</code></td><td>—</td><td>with <code>SMTP_PASSWORD</code></td><td>The two must be set together or both unset (e.g. a trusted local relay).</td></tr><tr><td><code>SMTP_PASSWORD</code></td><td>—</td><td>with <code>SMTP_USER</code></td><td>See above.</td></tr><tr><td><code>SMTP_FROM</code></td><td>—</td><td><strong>production</strong></td><td>Sender header, e.g. <code>&quot;amazon-king &lt;no-reply@example.com&gt;&quot;</code>.</td></tr></tbody></table><p>Derived constants (not configurable): session TTL ~7 days rolling, login token TTL 15 minutes, OAuth state TTL 10 minutes, recent-auth window 15 minutes.</p><h2 id="worker-process-apps-worker" tabindex="-1">Worker process (<code>apps/worker</code>) <a class="header-anchor" href="#worker-process-apps-worker" aria-label="Permalink to &quot;Worker process (\`apps/worker\`)&quot;">​</a></h2><p>Every tunable has a safe default; only <code>DATABASE_URL</code> is strictly required. Integer tunables must be positive numbers or the worker refuses to boot. LWA credentials are needed for token refresh — the worker boots without them so non-Amazon jobs still run, but any job needing an Amazon access token fails with a <code>missing_lwa_credentials</code> auth error until they are set.</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>DATABASE_URL</code></td><td>— (<strong>required</strong>)</td><td>PostgreSQL connection URL.</td></tr><tr><td><code>REPORT_STORAGE_DIR</code></td><td><code>./.data/reports</code></td><td>Local stand-in for S3-compatible raw report storage (gzip artifacts + sha256).</td></tr><tr><td><code>LOG_LEVEL</code></td><td><code>info</code></td><td>Pino level.</td></tr><tr><td><code>KILL_SWITCH</code></td><td><code>true</code></td><td>Same semantics as the API: only <code>&quot;false&quot;</code> disables. Wired for the future apply worker; the MVP worker is read-only against Amazon.</td></tr><tr><td><code>LWA_CLIENT_ID</code></td><td>—</td><td>Needed for token refresh.</td></tr><tr><td><code>LWA_CLIENT_SECRET</code></td><td>—</td><td>Needed for token refresh.</td></tr><tr><td><code>WORKER_POLL_INTERVAL_MS</code></td><td><code>2000</code></td><td>Queue poll interval when no job is available.</td></tr><tr><td><code>WORKER_LEASE_SECONDS</code></td><td><code>120</code></td><td>Lease per claimed job; heartbeats extend it.</td></tr><tr><td><code>WORKER_HEARTBEAT_MS</code></td><td><code>30000</code></td><td>Lease heartbeat interval.</td></tr><tr><td><code>WORKER_REAP_INTERVAL_MS</code></td><td><code>60000</code></td><td>Interval for reaping expired leases.</td></tr><tr><td><code>REPORT_POLL_INITIAL_DELAY_MS</code></td><td><code>5000</code></td><td>First delay when polling a Reporting v3 report.</td></tr><tr><td><code>REPORT_POLL_MAX_DELAY_MS</code></td><td><code>60000</code></td><td>Maximum report-poll backoff.</td></tr><tr><td><code>REPORT_POLL_TIMEOUT_MS</code></td><td><code>2700000</code> (45 min)</td><td>Overall report-poll timeout. Amazon typically needs 19–21 minutes per daily report.</td></tr><tr><td><code>RECENT_WINDOW_DAYS</code></td><td><code>14</code></td><td>Days re-imported by <code>recent_window_resync</code> to absorb attribution lag.</td></tr><tr><td><code>RECOMMENDATION_FRESHNESS_HOURS</code></td><td><code>48</code></td><td>Recommendation runs skip when the last complete metrics sync is older than this.</td></tr><tr><td><code>SCHEDULE_TICK_MS</code></td><td><code>900000</code> (15 min)</td><td><code>schedule_tick</code> self-rescheduling interval.</td></tr><tr><td><code>FX_RATES_BASE_URL</code></td><td><code>https://api.frankfurter.dev</code></td><td>Base URL of the Frankfurter-compatible FX API the daily <code>fx_sync</code> job fetches (<code>GET {base}/v2/rates?base=USD&amp;from=…</code>). Keyless and quota-free; point at a self-hosted Frankfurter container to avoid the public instance.</td></tr></tbody></table><h2 id="token-encryption-packages-crypto" tabindex="-1">Token encryption (<code>packages/crypto</code>) <a class="header-anchor" href="#token-encryption-packages-crypto" aria-label="Permalink to &quot;Token encryption (\`packages/crypto\`)&quot;">​</a></h2><p>Used by both the API (writing tokens) and the worker (refreshing them).</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Required</th><th>Notes</th></tr></thead><tbody><tr><td><code>TOKEN_ENCRYPTION_KEY</code></td><td>—</td><td>when a connection exists</td><td>AES-256-GCM master key (key version 1) as exactly 64 hex characters (32 bytes). Generate with <code>openssl rand -hex 32</code>.</td></tr><tr><td><code>TOKEN_ENCRYPTION_KEY_V2</code> …</td><td>—</td><td>no</td><td>Rotation: adding <code>TOKEN_ENCRYPTION_KEY_V&lt;N&gt;</code> registers key version N. New encryptions use the current version; old rows decrypt via the version embedded in the ciphertext header, so rotation does not require re-encrypting existing rows.</td></tr></tbody></table><p>The variable is read lazily at encrypt/decrypt time, and the process throws if it is missing or malformed when token work actually happens.</p><h2 id="compose-production-stack-only" tabindex="-1">Compose production stack only <a class="header-anchor" href="#compose-production-stack-only" aria-label="Permalink to &quot;Compose production stack only&quot;">​</a></h2><p>These are consumed by <code>compose.production.yml</code> / the Makefile, not by application code. Set them in <code>.env.production</code> (created by <code>make prod-config</code>).</p><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Notes</th></tr></thead><tbody><tr><td><code>POSTGRES_PASSWORD</code></td><td>— (<strong>required</strong>)</td><td>Password for the bundled <code>postgres:16-alpine</code> service (user/db <code>amazon_king</code>). Compose refuses to start without it. Also interpolated into the services&#39; <code>DATABASE_URL</code>.</td></tr><tr><td><code>APP_ENV_FILE</code></td><td><code>.env.production</code></td><td>Env file passed to the api/worker/migrate containers.</td></tr><tr><td><code>APP_BIND_ADDRESS</code></td><td><code>127.0.0.1</code></td><td>Bind address for the web proxy port. Keep loopback and terminate HTTPS in your own host reverse proxy.</td></tr><tr><td><code>APP_PORT</code></td><td><code>8080</code></td><td>Host port mapped to the web container&#39;s 8080.</td></tr></tbody></table><p>The compose stack also sets <code>NODE_ENV=production</code>, <code>PORT=3000</code>, <code>TRUST_PROXY=true</code> (api) and <code>REPORT_STORAGE_DIR=/app/.data/reports</code> (worker) itself.</p><h2 id="test-only" tabindex="-1">Test only <a class="header-anchor" href="#test-only" aria-label="Permalink to &quot;Test only&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Variable</th><th>Notes</th></tr></thead><tbody><tr><td><code>TEST_DATABASE_URL</code></td><td>Enables the <code>packages/database</code> integration tests. <strong>Must point at a disposable database</strong>: the suite drops and recreates the <code>public</code> schema. Unset, the integration tests are skipped and <code>pnpm -r test</code> still passes.</td></tr></tbody></table><h2 id="related-reading" tabindex="-1">Related reading <a class="header-anchor" href="#related-reading" aria-label="Permalink to &quot;Related reading&quot;">​</a></h2><ul><li><a href="/amazon-king/guide/configuration">Configuration guide</a> — what to set and why</li><li><a href="/amazon-king/guide/self-hosting">Self-hosting</a> — the production compose stack</li><li><a href="/amazon-king/reference/commands">Commands</a> — <code>make</code> targets that consume these files</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/environment-variables.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const environmentVariables = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  environmentVariables as default
};
