import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Configuration","description":"Task-oriented guide to configuring amazon-king for local development and production — validation rules, SMTP, secrets, and the kill switch.","frontmatter":{"title":"Configuration","description":"Task-oriented guide to configuring amazon-king for local development and production — validation rules, SMTP, secrets, and the kill switch."},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1786897054000}');
const _sfc_main = { name: "guide/configuration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h1><p>All runtime configuration lives in environment variables, loaded from <code>.env</code> in development (created by <code>make setup</code> from <code>.env.example</code>) or from <code>.env.production</code> for the production Compose stack (<code>make prod-config</code>).</p><p>Both processes validate their configuration at boot:</p><ul><li>The API parses its environment with a Zod schema in <code>apps/api/src/config.ts</code>. Invalid or missing values fail startup with a validation error — the server never runs on a half-valid config.</li><li>The worker does the same in <code>apps/worker/src/config.ts</code>. Only <code>DATABASE_URL</code> is strictly required there; every worker tunable has a safe default. The worker boots without LWA credentials so non-Amazon jobs still run, but any Amazon call (including token refresh) fails until they are set.</li></ul><p>This page covers the decisions you need to make. For the complete, variable-by-variable table with defaults, see <a href="/amazon-king/reference/environment-variables">Environment variables</a>.</p><h2 id="minimal-development-setup" tabindex="-1">Minimal development setup <a class="header-anchor" href="#minimal-development-setup" aria-label="Permalink to &quot;Minimal development setup&quot;">​</a></h2><p>The smallest working <code>.env</code> for <code>make run</code>:</p><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">DATABASE_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=postgres://postgres:postgres@localhost:5432/amazon_king</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SESSION_SECRET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=dev-only-change-me-session-secret</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">WEB_ORIGIN</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=http://localhost:5173</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">AMAZON_REDIRECT_URI</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=http://localhost:3000/api/integrations/amazon/callback</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">LWA_CLIENT_ID</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=amzn1.application-oa2-client.xxxx</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">LWA_CLIENT_SECRET</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=your-lwa-client-secret</span></span></code></pre></div><p>Notes:</p><ul><li><code>SESSION_SECRET</code> must be at least 16 characters (32+ and non-default in production — see below).</li><li>Without SMTP, magic sign-in links are printed to the API log in development only. Production requires SMTP.</li><li><code>TOKEN_ENCRYPTION_KEY</code> is required to store Amazon refresh tokens. The example file ships an all-zeros key that is fine for a disposable dev database; generate a real one for anything you care about (see <a href="#token-encryption-key">Token encryption key</a>).</li><li>Leave <code>KILL_SWITCH=true</code> while developing.</li></ul><h2 id="production-only-requirements" tabindex="-1">Production-only requirements <a class="header-anchor" href="#production-only-requirements" aria-label="Permalink to &quot;Production-only requirements&quot;">​</a></h2><p>Setting <code>NODE_ENV=production</code> turns on <code>Secure</code> session cookies (HTTPS is mandatory) and makes the boot validation stricter. The API refuses to start unless:</p><ul><li><code>OWNER_EMAIL</code> is set — the only address allowed to sign in (single-owner lock),</li><li><code>API_PUBLIC_URL</code> is set — the public base URL of the API, used as the target of magic sign-in links,</li><li><code>SMTP_HOST</code> and <code>SMTP_FROM</code> are set, and</li><li><code>SESSION_SECRET</code> is at least 32 characters and does not contain <code>change-me</code> (case-insensitive).</li></ul><p>Also set <code>WEB_ORIGIN</code> and <code>AMAZON_REDIRECT_URI</code> to your public HTTPS origin, and <code>TRUST_PROXY=true</code> if the API sits behind your reverse proxy (see below).</p><h2 id="smtp" tabindex="-1">SMTP <a class="header-anchor" href="#smtp" aria-label="Permalink to &quot;SMTP&quot;">​</a></h2><p>Magic-link sign-in is the only login mechanism, so working email delivery is a hard requirement in production.</p><ul><li><code>SMTP_PORT=465</code> with <code>SMTP_SECURE=true</code> — implicit TLS.</li><li><code>SMTP_PORT=587</code> (the default) with <code>SMTP_SECURE=false</code> — STARTTLS. Use the port your provider documents for STARTTLS.</li></ul><p>Authentication is optional for a trusted local relay. If you use credentials, <code>SMTP_USER</code> and <code>SMTP_PASSWORD</code> must be set <strong>together</strong> — setting only one fails boot validation.</p><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SMTP_HOST</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=smtp.example.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SMTP_PORT</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=465</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SMTP_SECURE</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SMTP_USER</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=amazon-king@example.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SMTP_PASSWORD</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=...</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">SMTP_FROM</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;amazon-king &lt;no-reply@example.com&gt;&quot;</span></span></code></pre></div><h2 id="reverse-proxy-trust-proxy" tabindex="-1">Reverse proxy: <code>TRUST_PROXY</code> <a class="header-anchor" href="#reverse-proxy-trust-proxy" aria-label="Permalink to &quot;Reverse proxy: \`TRUST_PROXY\`&quot;">​</a></h2><p><code>TRUST_PROXY=true</code> tells Fastify to trust <code>X-Forwarded-*</code> headers, which the API needs for correct client IPs (rate limiting, audit logs) when it sits behind your HTTPS terminator.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Only enable this when the API is reachable <strong>exclusively</strong> through your own reverse proxy. On a directly exposed API, trusting forwarding headers lets clients spoof their IP and defeat rate limits. The production Compose stack binds its web entrypoint to <code>127.0.0.1:8080</code> by default for this reason; see <a href="/amazon-king/guide/self-hosting">Self-hosting</a>.</p></div><h2 id="kill-switch-semantics" tabindex="-1">Kill switch semantics <a class="header-anchor" href="#kill-switch-semantics" aria-label="Permalink to &quot;Kill switch semantics&quot;">​</a></h2><p><code>KILL_SWITCH</code> is the global write kill switch, honored by both the API and the worker:</p><ul><li><strong>Fail-closed by default.</strong> Unset means <code>true</code> — writes disabled.</li><li><strong>Only the exact string <code>&quot;false&quot;</code> disables it.</strong> Any other value — <code>0</code>, <code>no</code>, <code>off</code>, a typo like <code>flase</code> — leaves writes disabled.</li></ul><div class="language-ini vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ini</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># Writes enabled (only when you have completed live validation):</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">KILL_SWITCH</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">=false</span></span></code></pre></div><p>This is deliberate: a misconfiguration can never silently turn writes on. Keep it <code>true</code> until you have completed the read-only validation checklist and the live-write validation sequence with a test account or a dedicated low-risk campaign. See <a href="/amazon-king/guide/applying-changes">Applying &amp; rolling back changes</a>.</p><h2 id="token-encryption-key" tabindex="-1">Token encryption key <a class="header-anchor" href="#token-encryption-key" aria-label="Permalink to &quot;Token encryption key&quot;">​</a></h2><p><code>TOKEN_ENCRYPTION_KEY</code> is the master key for envelope-encrypting Amazon refresh tokens (AES-256-GCM) before they are stored in the database. It must be <strong>64 hex characters</strong> (32 bytes), for example from:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">openssl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> rand</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -hex</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> 32</span></span></code></pre></div><p>Key rotation is supported without decrypting everything at once: set <code>TOKEN_ENCRYPTION_KEY_V2</code> (then <code>_V3</code>, …) to the new key. New encryptions use the latest version; the ciphertext embeds the key version, so older rows stay decryptable as long as their version&#39;s key is still configured. The incident runbook uses this when rotating keys after a suspected exposure — see <a href="/amazon-king/guide/operations#suspected-credential-or-data-exposure">Operations</a>.</p><div class="danger custom-block"><p class="custom-block-title">DANGER</p><p>Losing every configured key version makes all stored Amazon refresh tokens unrecoverable — every connection must be reconnected through OAuth. Back up keys separately in a secret manager, never beside database backups.</p></div></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/configuration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const configuration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  configuration as default
};
